---
uid: web-pages/overview/ui-layouts-and-themes/9-working-with-images
title: Bir ASP.NET Web sayfaları (Razor) sitesinde görüntüleri ile çalışma | Microsoft Docs
author: Rick-Anderson
description: Bu bölümde, ekleyebilir, görüntüleyebilir ve görüntüleri işlemek nasıl gösterir (yeniden boyutlandırma, çevirme ve filigranlar ekleyin), Web sitenizin.
ms.author: riande
ms.date: 02/20/2014
ms.assetid: 778c4e58-4372-4d25-bab9-aec4a8d8e38d
msc.legacyurl: /web-pages/overview/ui-layouts-and-themes/9-working-with-images
msc.type: authoredcontent
ms.openlocfilehash: fedd1013c036ebdb85877a868aaaa172733e5b8a
ms.sourcegitcommit: 0f1119340e4464720cfd16d0ff15764746ea1fea
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 04/17/2019
ms.locfileid: "59394711"
---
# <a name="working-with-images-in-an-aspnet-web-pages-razor-site"></a><span data-ttu-id="accee-103">Bir ASP.NET Web sayfaları (Razor) sitesinde görüntülerle çalışma</span><span class="sxs-lookup"><span data-stu-id="accee-103">Working with Images in an ASP.NET Web Pages (Razor) Site</span></span>

<span data-ttu-id="accee-104">tarafından [Tom FitzMacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="accee-104">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="accee-105">Bu makalede ekleyebilir, görüntüleyebilir ve görüntüleri işlemek gösterilmektedir (yeniden boyutlandırma, çevirme ve filigranlar eklemek) bir ASP.NET Web sayfaları (Razor) Web sitesinde.</span><span class="sxs-lookup"><span data-stu-id="accee-105">This article shows you how to add, display, and manipulate images (resize, flip, and add watermarks) in an ASP.NET Web Pages (Razor) website.</span></span>
> 
> <span data-ttu-id="accee-106">Öğrenecekleriniz:</span><span class="sxs-lookup"><span data-stu-id="accee-106">What you'll learn:</span></span>
> 
> - <span data-ttu-id="accee-107">Görüntüyü bir sayfaya dinamik olarak ekleme.</span><span class="sxs-lookup"><span data-stu-id="accee-107">How to add an image to a page dynamically.</span></span>
> - <span data-ttu-id="accee-108">Kullanıcıların bir görüntüyü karşıya yükleme yapma.</span><span class="sxs-lookup"><span data-stu-id="accee-108">How to let users upload an image.</span></span>
> - <span data-ttu-id="accee-109">Bir görüntüyü yeniden boyutlandırmak nasıl.</span><span class="sxs-lookup"><span data-stu-id="accee-109">How to resize an image.</span></span>
> - <span data-ttu-id="accee-110">Görüntü döndürme veya nasıl.</span><span class="sxs-lookup"><span data-stu-id="accee-110">How to flip or rotate an image.</span></span>
> - <span data-ttu-id="accee-111">Resme bir filigran ekleme yapma.</span><span class="sxs-lookup"><span data-stu-id="accee-111">How to add a watermark to an image.</span></span>
> - <span data-ttu-id="accee-112">Filigran olarak görüntü kullanma</span><span class="sxs-lookup"><span data-stu-id="accee-112">How to use an image as a watermark.</span></span>
> 
> <span data-ttu-id="accee-113">ASP.NET programlama makalesinde sunulan özellikler şunlardır:</span><span class="sxs-lookup"><span data-stu-id="accee-113">These are the ASP.NET programming features introduced in the article:</span></span>
> 
> - <span data-ttu-id="accee-114">`WebImage` Yardımcısı.</span><span class="sxs-lookup"><span data-stu-id="accee-114">The `WebImage` helper.</span></span>
> - <span data-ttu-id="accee-115">`Path` Nesne yolu ve dosya adlarını değiştirmek olanak tanıyan yöntemler sağlar.</span><span class="sxs-lookup"><span data-stu-id="accee-115">The `Path` object, which provides methods that let you manipulate path and file names.</span></span>
>   
> 
> ## <a name="software-versions-used-in-the-tutorial"></a><span data-ttu-id="accee-116">Bu öğreticide kullanılan yazılım sürümleri</span><span class="sxs-lookup"><span data-stu-id="accee-116">Software versions used in the tutorial</span></span>
> 
> 
> - <span data-ttu-id="accee-117">ASP.NET Web sayfaları (Razor) 2</span><span class="sxs-lookup"><span data-stu-id="accee-117">ASP.NET Web Pages (Razor) 2</span></span>
> - <span data-ttu-id="accee-118">WebMatrix 2</span><span class="sxs-lookup"><span data-stu-id="accee-118">WebMatrix 2</span></span>
>   
> 
> <span data-ttu-id="accee-119">Bu öğreticide, WebMatrix 3'ile de çalışır.</span><span class="sxs-lookup"><span data-stu-id="accee-119">This tutorial also works with WebMatrix 3.</span></span>


<a id="Adding_an_Image"></a>
## <a name="adding-an-image-to-a-web-page-dynamically"></a><span data-ttu-id="accee-120">Görüntü bir Web sayfasına dinamik olarak ekleme</span><span class="sxs-lookup"><span data-stu-id="accee-120">Adding an Image to a Web Page Dynamically</span></span>

<span data-ttu-id="accee-121">Web sitesi geliştirme yaptığınız sırada Web sitenize ve tek tek sayfaları görüntüleri ekleyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="accee-121">You can add images to your website and to individual pages while you're developing the website.</span></span> <span data-ttu-id="accee-122">Ayrıca kullanıcılar, bunları profil fotoğrafı ekleme izin vererek gibi görevler için yararlı olabilir, görüntüleri karşıya sağlayabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="accee-122">You can also let users upload images, which might be useful for tasks like letting them add a profile photo.</span></span>

<span data-ttu-id="accee-123">Bir görüntü zaten sitenizde kullanılabilir ve yalnızca bir sayfada görüntülemek istiyorsanız, bir HTML kullandığınız `<img>` öğesi şöyle:</span><span class="sxs-lookup"><span data-stu-id="accee-123">If an image is already available on your site and you just want to display it on a page, you use an HTML `<img>` element like this:</span></span>

[!code-html[Main](9-working-with-images/samples/sample1.html)]

<span data-ttu-id="accee-124">Bazı durumlarda, ancak dinamik olarak görüntüler ihtiyacınız &#8212; diğer bir deyişle, hangi sayfanın çalışıncaya kadar görüntülenecek görüntü bilmiyorum.</span><span class="sxs-lookup"><span data-stu-id="accee-124">Sometimes, though, you need to be able to display images dynamically &#8212; that is, you don't know what image to display until the page is running.</span></span>

<span data-ttu-id="accee-125">Bu bölümdeki yordamda bir görüntüyü burada kullanıcıları görüntü adları listesinden görüntü dosya adı belirtin hareket halindeyken gösterilmektedir.</span><span class="sxs-lookup"><span data-stu-id="accee-125">The procedure in this section shows how to display an image on the fly where users specify the image file name from a list of image names.</span></span> <span data-ttu-id="accee-126">Aşağı açılan listeden, görüntünün adını seçin ve bunlar sayfası gönderdiğinizde, seçili görüntü görüntülenir.</span><span class="sxs-lookup"><span data-stu-id="accee-126">They select the name of the image from a drop-down list, and when they submit the page, the image they selected is displayed.</span></span>

<span data-ttu-id="accee-127">![[Görüntü]](9-working-with-images/_static/image1.jpg "ch9images 1.jpg")</span><span class="sxs-lookup"><span data-stu-id="accee-127">![[image]](9-working-with-images/_static/image1.jpg "ch9images-1.jpg")</span></span>

1. <span data-ttu-id="accee-128">Webmatrix'te, yeni bir Web sitesi oluşturun.</span><span class="sxs-lookup"><span data-stu-id="accee-128">In WebMatrix, create a new website.</span></span>
2. <span data-ttu-id="accee-129">Adlı yeni bir sayfa ekleyin *DynamicImage.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="accee-129">Add a new page named *DynamicImage.cshtml*.</span></span>
3. <span data-ttu-id="accee-130">Web sitesinin kök klasöründe yeni bir klasör ekleyin ve adlandırın *görüntüleri*.</span><span class="sxs-lookup"><span data-stu-id="accee-130">In the root folder of the website, add a new folder and name it *images*.</span></span>
4. <span data-ttu-id="accee-131">Dört yansımalarına Ekle *görüntüleri* oluşturduğunuz klasör.</span><span class="sxs-lookup"><span data-stu-id="accee-131">Add four images to the *images* folder you just created.</span></span> <span data-ttu-id="accee-132">(Herhangi bir görüntü elinizde yapmak kullanışlı olacaktır, ancak bir sayfaya sığması.) Görüntüleri yeniden adlandır *Photo1.jpg*, *Photo2.jpg*, *Photo3.jpg*, ve *Photo4.jpg*.</span><span class="sxs-lookup"><span data-stu-id="accee-132">(Any images you have handy will do, but they should fit onto a page.) Rename the images *Photo1.jpg*, *Photo2.jpg*, *Photo3.jpg*, and *Photo4.jpg*.</span></span> <span data-ttu-id="accee-133">(Kullanmayacağınız *Photo4.jpg* Bu yordam, ancak bu makalenin sonraki bölümlerinde kullanacaksınız.)</span><span class="sxs-lookup"><span data-stu-id="accee-133">(You won't use *Photo4.jpg* in this procedure, but you'll use it later in the article.)</span></span>
5. <span data-ttu-id="accee-134">Dört görüntü, salt okunur olarak işaretlenmemiş olduğunu doğrulayın.</span><span class="sxs-lookup"><span data-stu-id="accee-134">Verify that the four images are not marked as read-only.</span></span>
6. <span data-ttu-id="accee-135">Sayfanın mevcut içeriği aşağıdakiyle değiştirin:</span><span class="sxs-lookup"><span data-stu-id="accee-135">Replace the existing content in the page with the following:</span></span>

    [!code-cshtml[Main](9-working-with-images/samples/sample2.cshtml)]

    <span data-ttu-id="accee-136">Sayfanın gövdesi bir açılan liste vardır (bir `<select>` öğesi) adlandırılan `photoChoice`.</span><span class="sxs-lookup"><span data-stu-id="accee-136">The body of the page has a drop-down list (a `<select>` element) that's named `photoChoice`.</span></span> <span data-ttu-id="accee-137">Liste Üç seçenek vardır ve `value` her liste seçeneği özniteliğine sahip içine girdiğiniz görüntülerden birini adını *görüntüleri* klasör.</span><span class="sxs-lookup"><span data-stu-id="accee-137">The list has three options, and the `value` attribute of each list option has the name of one of the images that you put in the *images* folder.</span></span> <span data-ttu-id="accee-138">Esas olarak, listenin gibi kolay bir ad seçmesine izin verir &quot;fotoğraf 1&quot;, ve ardından geçirir *.jpg* sayfa gönderildiğinde dosya adı.</span><span class="sxs-lookup"><span data-stu-id="accee-138">Essentially, the list lets the user select a friendly name like &quot;Photo 1&quot;, and it then passes the *.jpg* file name when the page is submitted.</span></span>

    <span data-ttu-id="accee-139">Kodda, kullanıcının seçimini (diğer bir deyişle, görüntü dosyası adı) listeden okuyarak alabileceğiniz `Request["photoChoice"]`.</span><span class="sxs-lookup"><span data-stu-id="accee-139">In the code, you can get the user's selection (in other words, the image file name) from the list by reading `Request["photoChoice"]`.</span></span> <span data-ttu-id="accee-140">İlk olup olmadığını bir seçim hiç görürsünüz.</span><span class="sxs-lookup"><span data-stu-id="accee-140">You first see if there's a selection at all.</span></span> <span data-ttu-id="accee-141">Varsa, klasörün adını görüntüler ve kullanıcının resim dosyası adı oluşan bir görüntü için bir yol oluşturun.</span><span class="sxs-lookup"><span data-stu-id="accee-141">If there is, you construct a path for the image that consists of the name of the folder for the images and the user's image file name.</span></span> <span data-ttu-id="accee-142">(Bir yol oluşturulmaya çalışıldı ancak hiçbir oluştu `Request["photoChoice"]`, bir hata elde edersiniz.) Bu göreli bir yol şöyle olur:</span><span class="sxs-lookup"><span data-stu-id="accee-142">(If you tried to construct a path but there was nothing in `Request["photoChoice"]`, you'd get an error.) This results in a relative path like this:</span></span>

    <span data-ttu-id="accee-143">*görüntüleri/Photo1.jpg*</span><span class="sxs-lookup"><span data-stu-id="accee-143">*images/Photo1.jpg*</span></span>

    <span data-ttu-id="accee-144">Yolun adlı bir değişkende depolanır `imagePath` bu sayfada daha sonra ihtiyacınız olacak.</span><span class="sxs-lookup"><span data-stu-id="accee-144">The path is stored in variable named `imagePath` that you'll need later in the page.</span></span>

    <span data-ttu-id="accee-145">Gövdesinde vardır, ayrıca bir `<img>` kullanıcının seçtiği görüntüyü görüntülemek için kullanılan bir öğe.</span><span class="sxs-lookup"><span data-stu-id="accee-145">In the body, there's also an `<img>` element that's used to display the image that the user picked.</span></span> <span data-ttu-id="accee-146">`src` Özniteliği ayarlanmamış bir dosya adı veya URL'si, statik bir öğe görüntülemek için yaptığınız gibi.</span><span class="sxs-lookup"><span data-stu-id="accee-146">The `src` attribute isn't set to a file name or URL, like you'd do to display a static element.</span></span> <span data-ttu-id="accee-147">Bunun yerine, kümesine `@imagePath`, yani bu kodda ayarladığınız yolundan değerini alır.</span><span class="sxs-lookup"><span data-stu-id="accee-147">Instead, it's set to `@imagePath`, meaning that it gets its value from the path you set in code.</span></span>

    <span data-ttu-id="accee-148">Sayfa çalıştığında, ilk kez yine de görüntülemek için resim yok kullanıcı hiçbir şey seçili taşınmadığından için.</span><span class="sxs-lookup"><span data-stu-id="accee-148">The first time that the page runs, though, there's no image to display, because the user hasn't selected anything.</span></span> <span data-ttu-id="accee-149">Bu normalde, açardı `src` öznitelik boş olur ve kırmızı bir resim gösterebilir &quot;x&quot; (veya bir görüntü bulamadığında ne olursa olsun tarayıcı işler).</span><span class="sxs-lookup"><span data-stu-id="accee-149">This would normally mean that the `src` attribute would be empty and the image would show up as a red &quot;x&quot; (or whatever the browser renders when it can't find an image).</span></span> <span data-ttu-id="accee-150">Bunu önlemek için eklediğiniz `<img>` öğesinde bir `if` görmek için sınar blok olmadığını `imagePath` değişkeni olan her şeyi içinde.</span><span class="sxs-lookup"><span data-stu-id="accee-150">To prevent this, you put the `<img>` element in an `if` block that tests to see whether the `imagePath` variable has anything in it.</span></span> <span data-ttu-id="accee-151">Kullanıcının bir seçimi yaptıysanız `imagePath` yol içeriyor.</span><span class="sxs-lookup"><span data-stu-id="accee-151">If the user made a selection, `imagePath` contains the path.</span></span> <span data-ttu-id="accee-152">Kullanıcı bir görüntü seçin olmadı veya bu ilk kez kullanıyorsanız, sayfa görüntülenir, `<img>` öğe değilse bile çizilir.</span><span class="sxs-lookup"><span data-stu-id="accee-152">If the user didn't pick an image or if this is the first time the page is displayed, the `<img>` element isn't even rendered.</span></span>
7. <span data-ttu-id="accee-153">Dosyayı kaydedin ve sayfanın tarayıcıda çalıştırın.</span><span class="sxs-lookup"><span data-stu-id="accee-153">Save the file and run the page in a browser.</span></span> <span data-ttu-id="accee-154">(Emin sayfanın içinde seçili **dosyaları** çalıştırmadan önce çalışma alanı.)</span><span class="sxs-lookup"><span data-stu-id="accee-154">(Make sure the page is selected in the **Files** workspace before you run it.)</span></span>
8. <span data-ttu-id="accee-155">Aşağı açılan listeden bir görüntü seçin ve ardından **örnek görüntü**.</span><span class="sxs-lookup"><span data-stu-id="accee-155">Select an image from the drop-down list and then click **Sample Image**.</span></span> <span data-ttu-id="accee-156">Farklı görüntüler için farklı seçimler gördüğünüzden emin olun.</span><span class="sxs-lookup"><span data-stu-id="accee-156">Make sure that you see different images for different choices.</span></span>

<a id="Uploading_an_Image"></a>
## <a name="uploading-an-image"></a><span data-ttu-id="accee-157">Görüntüyü karşıya yükleme</span><span class="sxs-lookup"><span data-stu-id="accee-157">Uploading an Image</span></span>

<span data-ttu-id="accee-158">Önceki örnekte bir görüntüyü dinamik olarak nasıl oluşturulacağını gösterir, ancak Web sitenizde bulunan görüntüleriyle birlikte çalıştı.</span><span class="sxs-lookup"><span data-stu-id="accee-158">The previous example showed you how to display an image dynamically, but it worked only with images that were already on your website.</span></span> <span data-ttu-id="accee-159">Bu yordam, kullanıcıların daha sonra sayfada görüntülenen bir görüntüyü karşıya yükleme işlemi gösterilmektedir.</span><span class="sxs-lookup"><span data-stu-id="accee-159">This procedure shows how to let users upload an image, which is then displayed on the page.</span></span> <span data-ttu-id="accee-160">ASP.NET'te, görüntüleri kullanarak hareket halindeyken işleyebileceğiniz `WebImage` Yardımcısı, oluşturmak, yönetmek ve görüntüleri kaydetme olanak sağlayan yöntemleri vardır.</span><span class="sxs-lookup"><span data-stu-id="accee-160">In ASP.NET, you can manipulate images on the fly using the `WebImage` helper, which has methods that let you create, manipulate, and save images.</span></span> <span data-ttu-id="accee-161">`WebImage` Yardımcısı gibi tüm yaygın web görüntü dosya türlerini destekler, *.jpg*, *.png*, ve *.bmp*.</span><span class="sxs-lookup"><span data-stu-id="accee-161">The `WebImage` helper supports all the common web image file types, including *.jpg*, *.png*, and *.bmp*.</span></span> <span data-ttu-id="accee-162">Bu makale boyunca kullanacağınız *.jpg* görüntüler, ancak herhangi birini kullanabilirsiniz resim türleri.</span><span class="sxs-lookup"><span data-stu-id="accee-162">Throughout this article, you'll use *.jpg* images, but you can use any of the image types.</span></span>

<span data-ttu-id="accee-163">![[Görüntü]](9-working-with-images/_static/image2.jpg "ch9images 2.jpg")</span><span class="sxs-lookup"><span data-stu-id="accee-163">![[image]](9-working-with-images/_static/image2.jpg "ch9images-2.jpg")</span></span>

1. <span data-ttu-id="accee-164">Yeni bir sayfa ekleyin ve adlandırın *UploadImage.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="accee-164">Add a new page and name it *UploadImage.cshtml*.</span></span>
2. <span data-ttu-id="accee-165">Sayfanın mevcut içeriği aşağıdakiyle değiştirin:</span><span class="sxs-lookup"><span data-stu-id="accee-165">Replace the existing content in the page with the following:</span></span> 

    [!code-cshtml[Main](9-working-with-images/samples/sample3.cshtml)]

    <span data-ttu-id="accee-166">Metin gövdesi olan bir `<input type="file">` , kullanıcıların karşıya yüklenecek dosyayı seçin olanak sağlayan bir öğe.</span><span class="sxs-lookup"><span data-stu-id="accee-166">The body of the text has an `<input type="file">` element, which lets users select a file to upload.</span></span> <span data-ttu-id="accee-167">Simgeye **Gönder**, dosyanın çekilen form ile birlikte gönderilir.</span><span class="sxs-lookup"><span data-stu-id="accee-167">When they click **Submit**, the file they picked is submitted along with the form.</span></span>

    <span data-ttu-id="accee-168">Karşıya yüklenen görüntüyü almak için kullandığınız `WebImage` görüntülerle çalışma için kullanışlı bir yöntem her türlü olan Yardımcısı.</span><span class="sxs-lookup"><span data-stu-id="accee-168">To get the uploaded image, you use the `WebImage` helper, which has all sorts of useful methods for working with images.</span></span> <span data-ttu-id="accee-169">Özellikle, kullandığınız `WebImage.GetImageFromRequest` (varsa) karşıya yüklenen görüntüyü almak ve adlı bir değişkende depolamak için `photo`.</span><span class="sxs-lookup"><span data-stu-id="accee-169">Specifically, you use `WebImage.GetImageFromRequest` to get the uploaded image (if any) and store it in a variable named `photo`.</span></span>

    <span data-ttu-id="accee-170">Birçok iş Bu örnekte, alma ve ayarlama dosya ve yol adlarını içerir.</span><span class="sxs-lookup"><span data-stu-id="accee-170">A lot of the work in this example involves getting and setting file and path names.</span></span> <span data-ttu-id="accee-171">Kullanıcı karşıya yüklenen görüntünün adını (ve yalnızca adı) alın ve sonra görüntüyü depolamak için nereye gideceğinizi için yeni bir yol oluşturmak istediğiniz sorunudur.</span><span class="sxs-lookup"><span data-stu-id="accee-171">The issue is that you want to get the name (and just the name) of the image that the user uploaded, and then create a new path for where you're going to store the image.</span></span> <span data-ttu-id="accee-172">Kullanıcılar, potansiyel olarak aynı ada sahip birden fazla görüntü karşıya yüklenemedi çünkü benzersiz adlar oluşturmak ve mevcut resimleri kullanıcıları eskisinin üzerine yazmayın emin olmak için biraz ek bir kod kullanın.</span><span class="sxs-lookup"><span data-stu-id="accee-172">Because users could potentially upload multiple images that have the same name, you use a bit of extra code to create unique names and make sure that users don't overwrite existing pictures.</span></span>

    <span data-ttu-id="accee-173">Görüntü gerçekten karşıya yüklediyseniz (test `if (photo != null)`), görüntünün görüntü adı alınmaya `FileName` özelliği.</span><span class="sxs-lookup"><span data-stu-id="accee-173">If an image actually has been uploaded (the test `if (photo != null)`), you get the image name from the image's `FileName` property.</span></span> <span data-ttu-id="accee-174">Kullanıcı görüntü yüklediğinde `FileName` kullanıcının bilgisayarında yolundan içerir kullanıcının özgün adı içeriyor.</span><span class="sxs-lookup"><span data-stu-id="accee-174">When the user uploads the image, `FileName` contains the user's original name, which includes the path from the user's computer.</span></span> <span data-ttu-id="accee-175">Şuna benzeyebilir:</span><span class="sxs-lookup"><span data-stu-id="accee-175">It might look like this:</span></span>

    <span data-ttu-id="accee-176">*C:\Users\Joe\Pictures\SamplePhoto1.jpg*</span><span class="sxs-lookup"><span data-stu-id="accee-176">*C:\Users\Joe\Pictures\SamplePhoto1.jpg*</span></span>

    <span data-ttu-id="accee-177">Bu yol bilgisi ancak istemediğiniz &#8212; gerçek dosya adı yalnızca istiyorsanız (*SamplePhoto1.jpg*).</span><span class="sxs-lookup"><span data-stu-id="accee-177">You don't want all that path information, though &#8212; you just want the actual file name (*SamplePhoto1.jpg*).</span></span> <span data-ttu-id="accee-178">Yalnızca yoldan dosyasını kullanarak çıkarmanız `Path.GetFileName` böyle bir yöntemi:</span><span class="sxs-lookup"><span data-stu-id="accee-178">You can strip out just the file from a path by using the `Path.GetFileName` method, like this:</span></span>

    [!code-csharp[Main](9-working-with-images/samples/sample4.cs)]

    <span data-ttu-id="accee-179">Ardından özgün adına bir GUID ekleyerek yeni bir benzersiz bir dosya adı oluşturun.</span><span class="sxs-lookup"><span data-stu-id="accee-179">You then create a new unique file name by adding a GUID to the original name.</span></span> <span data-ttu-id="accee-180">(GUID'ler hakkında daha fazla bilgi için bkz. [hakkında GUID'leri](#SB_AboutGUIDs) bu makalenin ilerleyen bölümlerinde.) Ardından, kaydetmek için kullanabileceğiniz tam yolunu oluşturun.</span><span class="sxs-lookup"><span data-stu-id="accee-180">(For more about GUIDs, see [About GUIDs](#SB_AboutGUIDs) later in this article.) Then you construct a complete path that you can use to save the image.</span></span> <span data-ttu-id="accee-181">Kaydetme yolu oluşur yeni dosya adı, ' % s'klasörü (görüntüler) ve geçerli Web sitesi konumu.</span><span class="sxs-lookup"><span data-stu-id="accee-181">The save path is made up of the new file name, the folder (images), and the current website location.</span></span>

    > [!NOTE]
    > <span data-ttu-id="accee-182">Dosyaları kaydetmek, kodunuzda sırayla *görüntüleri* klasörü, uygulamanın okuma-yazma bu klasörün izinlerini.</span><span class="sxs-lookup"><span data-stu-id="accee-182">In order for your code to save files in the *images* folder, the application needs read-write permissions for that folder.</span></span> <span data-ttu-id="accee-183">Geliştirme bilgisayarınızda bu genellikle bir sorun değildir.</span><span class="sxs-lookup"><span data-stu-id="accee-183">On your development computer this is not typically an issue.</span></span> <span data-ttu-id="accee-184">Ancak, bir barındırma sağlayıcısının web sunucusuna sitenizi yayımladığınızda, açıkça bu izinleri ayarlamak gerekebilir.</span><span class="sxs-lookup"><span data-stu-id="accee-184">However, when you publish your site to a hosting provider's web server, you might need to explicitly set those permissions.</span></span> <span data-ttu-id="accee-185">Bu kod bir barındırma sağlayıcısının sunucusunda çalıştırın ve hata iletisi alıyorum, bu izinleri ayarlama konusunda bilgi edinmek için barındırma sağlayıcısı ile kontrol edin.</span><span class="sxs-lookup"><span data-stu-id="accee-185">If you run this code on a hosting provider's server and get errors, check with the hosting provider to find out how to set those permissions.</span></span>

    <span data-ttu-id="accee-186">Son olarak, kaydetme geçirdiğiniz yolu `Save` yöntemi `WebImage` Yardımcısı.</span><span class="sxs-lookup"><span data-stu-id="accee-186">Finally, you pass the save path to the `Save` method of the `WebImage` helper.</span></span> <span data-ttu-id="accee-187">Bu, karşıya yüklenen görüntüyü yeni adıyla depolar.</span><span class="sxs-lookup"><span data-stu-id="accee-187">This stores the uploaded image under its new name.</span></span> <span data-ttu-id="accee-188">Kayıt yöntemi şu şekilde görünür: `photo.Save(@"~\" + imagePath)`.</span><span class="sxs-lookup"><span data-stu-id="accee-188">The save method looks like this: `photo.Save(@"~\" + imagePath)`.</span></span> <span data-ttu-id="accee-189">Tam yol eklenir `@"~\"`, geçerli Web sitesi konumu olduğu.</span><span class="sxs-lookup"><span data-stu-id="accee-189">The complete path is appended to `@"~\"`, which is the current website location.</span></span> <span data-ttu-id="accee-190">(Hakkında bilgi için `~` işleci bkz [giriş kullanımına ASP.NET Web programlama Razor söz dizimi](https://go.microsoft.com/fwlink/?LinkId=202890#ID_WorkingWithFileAndFolderPaths).)</span><span class="sxs-lookup"><span data-stu-id="accee-190">(For information about the `~` operator, see [Introduction to ASP.NET Web Programming Using the Razor Syntax](https://go.microsoft.com/fwlink/?LinkId=202890#ID_WorkingWithFileAndFolderPaths).)</span></span>

    <span data-ttu-id="accee-191">Önceki örnekte olduğu gibi sayfasının gövdesinde bir `<img>` görüntüyü görüntülemek için öğesi.</span><span class="sxs-lookup"><span data-stu-id="accee-191">As in the previous example, the body of the page contains an `<img>` element to display the image.</span></span> <span data-ttu-id="accee-192">Varsa `imagePath` ayarlandı, `<img>` öğesi işlenir ve kendi `src` özniteliği `imagePath` değeri.</span><span class="sxs-lookup"><span data-stu-id="accee-192">If `imagePath` has been set, the `<img>` element is rendered and its `src` attribute is set to the `imagePath` value.</span></span>
3. <span data-ttu-id="accee-193">Sayfanın tarayıcıda çalıştırın.</span><span class="sxs-lookup"><span data-stu-id="accee-193">Run the page in a browser.</span></span>
4. <span data-ttu-id="accee-194">Bir görüntüyü karşıya yükleme ve sayfa içinde görüntülendiğinden emin olun.</span><span class="sxs-lookup"><span data-stu-id="accee-194">Upload an image and make sure it's displayed in the page.</span></span>
5. <span data-ttu-id="accee-195">Sitenizi açın *görüntüleri* klasör.</span><span class="sxs-lookup"><span data-stu-id="accee-195">In your site, open the *images* folder.</span></span> <span data-ttu-id="accee-196">Dosya adı şuna benzer yeni bir dosya eklendiğini görürsünüz:</span><span class="sxs-lookup"><span data-stu-id="accee-196">You see that a new file has been added whose file name looks something like this::</span></span> 

    <span data-ttu-id="accee-197">*45ea4527-7ddd-4965-b9ca-c6444982b342\_MyPhoto.png*</span><span class="sxs-lookup"><span data-stu-id="accee-197">*45ea4527-7ddd-4965-b9ca-c6444982b342\_MyPhoto.png*</span></span>

    <span data-ttu-id="accee-198">Adının önüne bir GUID ile karşıya yüklediğiniz görüntüyü budur.</span><span class="sxs-lookup"><span data-stu-id="accee-198">This is the image that you uploaded with a GUID prefixed to the name.</span></span> <span data-ttu-id="accee-199">(Kendi dosya farklı bir GUID olacaktır ve büyük olasılıkla farklı bir şey adlı *MyPhoto.png*.)</span><span class="sxs-lookup"><span data-stu-id="accee-199">(Your own file will have a different GUID and probably is named something different than *MyPhoto.png*.)</span></span>

> [!TIP] 
> 
> <a id="SB_AboutGUIDs"></a>
> ### <a name="about-guids"></a><span data-ttu-id="accee-200">GUID'leri hakkında</span><span class="sxs-lookup"><span data-stu-id="accee-200">About GUIDs</span></span>
> 
> <span data-ttu-id="accee-201">Bir GUID (genel benzersiz Tanımlayıcı) genellikle bunun gibi bir biçimde işlenen bir tanımlayıcıdır: `936DA01F-9ABD-4d9d-80C7-02AF85C822A8`.</span><span class="sxs-lookup"><span data-stu-id="accee-201">A GUID (globally-unique ID) is an identifier that's usually rendered in a format like this: `936DA01F-9ABD-4d9d-80C7-02AF85C822A8`.</span></span> <span data-ttu-id="accee-202">Sayı ve harf (A-F) gelen her GUID için farklılık gösterir ancak tüm 8-4-4-4-12 karakter grupları kullanmanın desenini izler.</span><span class="sxs-lookup"><span data-stu-id="accee-202">The numbers and letters (from A-F) differ for each GUID, but they all follow the pattern of using groups of 8-4-4-4-12 characters.</span></span> <span data-ttu-id="accee-203">(Teknik olarak, bir GUID bir 16-baytlık/128-bit sayıdır.) Bir GUID gerektiğinde bir GUID sizin için oluşturduğu özel kodu çağırabilir.</span><span class="sxs-lookup"><span data-stu-id="accee-203">(Technically, a GUID is a 16-byte/128-bit number.) When you need a GUID, you can call specialized code that generates a GUID for you.</span></span> <span data-ttu-id="accee-204">Sayı devasa boyutu arasındaki fikir GUID'leri arkasında olan (3.4 x 10<sup>38</sup>) ve öğeyi oluşturan algoritma, sonuçta elde edilen sayı neredeyse bir tür olması garanti edilir.</span><span class="sxs-lookup"><span data-stu-id="accee-204">The idea behind GUIDs is that between the enormous size of the number (3.4 x 10<sup>38</sup>) and the algorithm for generating it, the resulting number is virtually guaranteed to be one of a kind.</span></span> <span data-ttu-id="accee-205">Bu nedenle aynı adı iki kez kullanmayacağınız garanti etmeniz gerekir, herhangi bir şeyi adları oluşturmaya yönelik en iyi yolu guıd'lerdir.</span><span class="sxs-lookup"><span data-stu-id="accee-205">GUIDs therefore are a good way to generate names for things when you must guarantee that you won't use the same name twice.</span></span> <span data-ttu-id="accee-206">Dezavantajı, adı yalnızca kodunda kullanılırken kullanılacak eğilimi gösterir şekilde GUID'leri özellikle kullanıcı dostu olmayan olur.</span><span class="sxs-lookup"><span data-stu-id="accee-206">The downside, of course, is that GUIDs aren't particularly user friendly, so they tend to be used when the name is used only in code.</span></span>


<a id="Resizing_an_Image"></a>
## <a name="resizing-an-image"></a><span data-ttu-id="accee-207">Görüntüyü Yeniden Boyutlandırma</span><span class="sxs-lookup"><span data-stu-id="accee-207">Resizing an Image</span></span>

<span data-ttu-id="accee-208">Web sitenizi bir kullanıcı görüntülerden kabul ederse, görüntülemek veya kaydetmek için önce görüntüleri yeniden boyutlandırma isteyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="accee-208">If your website accepts images from a user, you might want to resize the images before you display or save them.</span></span> <span data-ttu-id="accee-209">Yeniden kullanabileceğiniz `WebImage` bu Yardımcısı.</span><span class="sxs-lookup"><span data-stu-id="accee-209">You can again use the `WebImage` helper for this.</span></span>

<span data-ttu-id="accee-210">Bu yordam, bir küçük resim oluşturma ve ardından Web sitesi küçük resim ve özgün resmin kaydetmek için karşıya yüklenen bir görüntüyü yeniden boyutlandırma işlemi gösterilmektedir.</span><span class="sxs-lookup"><span data-stu-id="accee-210">This procedure shows how to resize an uploaded image to create a thumbnail and then save the thumbnail and original image in the website.</span></span> <span data-ttu-id="accee-211">Küçük Resim sayfada görüntülemenize ve kullanıcıları tam boyutlu görüntüyü yeniden yönlendirmek için bir köprü kullanın.</span><span class="sxs-lookup"><span data-stu-id="accee-211">You display the thumbnail on the page and use a hyperlink to redirect users to the full-sized image.</span></span>

<span data-ttu-id="accee-212">![[Görüntü]](9-working-with-images/_static/image3.jpg "ch9images 3.jpg")</span><span class="sxs-lookup"><span data-stu-id="accee-212">![[image]](9-working-with-images/_static/image3.jpg "ch9images-3.jpg")</span></span>

1. <span data-ttu-id="accee-213">Adlı yeni bir sayfa ekleyin *Thumbnail.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="accee-213">Add a new page named *Thumbnail.cshtml*.</span></span>
2. <span data-ttu-id="accee-214">İçinde *görüntüleri* klasöründe adlı bir alt klasör oluşturun *thumbs*.</span><span class="sxs-lookup"><span data-stu-id="accee-214">In the *images* folder, create a subfolder named *thumbs*.</span></span>
3. <span data-ttu-id="accee-215">Sayfanın mevcut içeriği aşağıdakiyle değiştirin:</span><span class="sxs-lookup"><span data-stu-id="accee-215">Replace the existing content in the page with the following:</span></span> 

    [!code-cshtml[Main](9-working-with-images/samples/sample5.cshtml)]

    <span data-ttu-id="accee-216">Bu kod, önceki örnekte koda benzer.</span><span class="sxs-lookup"><span data-stu-id="accee-216">This code is similar to the code from the previous example.</span></span> <span data-ttu-id="accee-217">Bu kod resmi kaydeder, iki kez, normalde bir kez fark ve görüntüyü küçük resim bir kopyasını oluşturduktan sonra bir kez.</span><span class="sxs-lookup"><span data-stu-id="accee-217">The difference is that this code saves the image twice, once normally and once after you create a thumbnail copy of the image.</span></span> <span data-ttu-id="accee-218">Önce karşıya yüklenen görüntüyü almak ve kaydedin *görüntüleri* klasör.</span><span class="sxs-lookup"><span data-stu-id="accee-218">First you get the uploaded image and save it in the *images* folder.</span></span> <span data-ttu-id="accee-219">Ardından, küçük resim görüntüsü için yeni bir yol de oluşturur.</span><span class="sxs-lookup"><span data-stu-id="accee-219">You then construct a new path for the thumbnail image.</span></span> <span data-ttu-id="accee-220">Gerçekten küçük resim oluşturmak için arama `WebImage` yardımcının `Resize` 60 piksel 60-piksel görüntüsü oluşturmak için yöntemi.</span><span class="sxs-lookup"><span data-stu-id="accee-220">To actually create the thumbnail, you call the `WebImage` helper's `Resize` method to create a 60-pixel by 60-pixel image.</span></span> <span data-ttu-id="accee-221">Bu örnek nasıl, en boy oranını korumak ve durumunda (yeni boyutu gerçekten görüntüyü daha büyük hale getirir) genişletilmesini görüntünün nasıl engelleyebilir gösterir.</span><span class="sxs-lookup"><span data-stu-id="accee-221">The example shows how you preserve the aspect ratio and how you can prevent the image from being enlarged (in case the new size would actually make the image larger).</span></span> <span data-ttu-id="accee-222">Yeniden boyutlandırılan resim ardından kaydedilir *thumbs* alt.</span><span class="sxs-lookup"><span data-stu-id="accee-222">The resized image is then saved in the *thumbs* subfolder.</span></span>

    <span data-ttu-id="accee-223">Biçimlendirme sonunda, aynı kullandığınız `<img>` dinamik öğeyle `src` görüntü koşullu olarak göstermek için önceki örneklerde gördünüz özniteliği.</span><span class="sxs-lookup"><span data-stu-id="accee-223">At the end of the markup, you use the same `<img>` element with the dynamic `src` attribute that you've seen in the previous examples to conditionally show the image.</span></span> <span data-ttu-id="accee-224">Bu durumda, küçük resim görüntüler.</span><span class="sxs-lookup"><span data-stu-id="accee-224">In this case, you display the thumbnail.</span></span> <span data-ttu-id="accee-225">Ayrıca bir `<a>` büyük görüntünün sürümünü bir köprü oluşturmak için öğesi.</span><span class="sxs-lookup"><span data-stu-id="accee-225">You also use an `<a>` element to create a hyperlink to the big version of the image.</span></span> <span data-ttu-id="accee-226">Olduğu gibi `src` özniteliği `<img>` öğesi, ayarladığınız `href` özniteliği `<a>` öğesi içinde ne olursa olsun dinamik olarak `imagePath`.</span><span class="sxs-lookup"><span data-stu-id="accee-226">As with the `src` attribute of the `<img>` element, you set the `href` attribute of the `<a>` element dynamically to whatever is in `imagePath`.</span></span> <span data-ttu-id="accee-227">Yolun bir URL olarak çalışabilir emin olmak için geçirdiğiniz `imagePath` için `Html.AttributeEncode` yöntemi URL'de kullanılabilir bir karakter yolunda ayrılmış karakterleri dönüştürür.</span><span class="sxs-lookup"><span data-stu-id="accee-227">To make sure that the path can work as a URL, you pass `imagePath` to the `Html.AttributeEncode` method, which converts reserved characters in the path to characters that are ok in a URL.</span></span>
4. <span data-ttu-id="accee-228">Sayfanın tarayıcıda çalıştırın.</span><span class="sxs-lookup"><span data-stu-id="accee-228">Run the page in a browser.</span></span>
5. <span data-ttu-id="accee-229">Fotoğrafı karşıya yükleme ve küçük resim gösterildiğini doğrulayın.</span><span class="sxs-lookup"><span data-stu-id="accee-229">Upload a photo and verify that the thumbnail is shown.</span></span>
6. <span data-ttu-id="accee-230">Tam boyutlu görüntüyü görmek için küçük resme tıklayın.</span><span class="sxs-lookup"><span data-stu-id="accee-230">Click the thumbnail to see the full-size image.</span></span>
7. <span data-ttu-id="accee-231">İçinde *görüntüleri* ve *görüntüleri/thumbs*, yeni dosyaları eklendiğini unutmayın.</span><span class="sxs-lookup"><span data-stu-id="accee-231">In the *images* and *images/thumbs*, note that new files have been added.</span></span>

<a id="Rotating_and_Flipping"></a>
## <a name="rotating-and-flipping-an-image"></a><span data-ttu-id="accee-232">Görüntüyü çevirme ve döndürme</span><span class="sxs-lookup"><span data-stu-id="accee-232">Rotating and Flipping an Image</span></span>

<span data-ttu-id="accee-233">`WebImage` Yardımcı ayrıca sağlar, çevirme ve döndürme görüntüleri.</span><span class="sxs-lookup"><span data-stu-id="accee-233">The `WebImage` helper also lets you flip and rotate images.</span></span> <span data-ttu-id="accee-234">Bu yordamda, sunucudan bir görüntüyü Al, görüntüyü (dikey) ters çevir, kaydedin ve ardından döndürülen resim sayfada görüntülemek gösterilmiştir.</span><span class="sxs-lookup"><span data-stu-id="accee-234">This procedure shows how to get an image from the server, flip the image upside down (vertically), save it, and then display the flipped image on the page.</span></span> <span data-ttu-id="accee-235">Bu örnekte, yalnızca sunucu üzerinde zaten sahip bir dosya kullanıyorsanız (*Photo2.jpg*).</span><span class="sxs-lookup"><span data-stu-id="accee-235">In this example, you're just using a file you already have on the server (*Photo2.jpg*).</span></span> <span data-ttu-id="accee-236">Gerçek bir uygulamada, büyük olasılıkla önceki örneklerde olduğu gibi adları dinamik olarak almak bir görüntüyü çevir.</span><span class="sxs-lookup"><span data-stu-id="accee-236">In a real application, you'd probably flip an image whose name you get dynamically, like you did in previous examples.</span></span>

<span data-ttu-id="accee-237">![[Görüntü]](9-working-with-images/_static/image4.jpg "ch9images 4.jpg")</span><span class="sxs-lookup"><span data-stu-id="accee-237">![[image]](9-working-with-images/_static/image4.jpg "ch9images-4.jpg")</span></span>

1. <span data-ttu-id="accee-238">Adlı yeni bir sayfa ekleyin *FlipImage.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="accee-238">Add a new page named *FlipImage.cshtml*.</span></span>
2. <span data-ttu-id="accee-239">Sayfanın mevcut içeriği aşağıdakiyle değiştirin:</span><span class="sxs-lookup"><span data-stu-id="accee-239">Replace the existing content in the page with the following:</span></span> 

    [!code-cshtml[Main](9-working-with-images/samples/sample6.cshtml)]

    <span data-ttu-id="accee-240">Kod `WebImage` görüntü sunucudan almak için yardımcı.</span><span class="sxs-lookup"><span data-stu-id="accee-240">The code uses the `WebImage` helper to get an image from the server.</span></span> <span data-ttu-id="accee-241">Görüntüleri kaydetmek için önceki örneklerde kullanılan aynı yöntemleri kullanarak bir resmin yolunu oluşturun ve bu yolu kullanarak bir görüntü oluşturduğunuzda geçirdiğiniz `WebImage`:</span><span class="sxs-lookup"><span data-stu-id="accee-241">You create the path to the image using the same technique you used in earlier examples for saving images, and you pass that path when you create an image using `WebImage`:</span></span>

    [!code-javascript[Main](9-working-with-images/samples/sample7.js)]

    <span data-ttu-id="accee-242">Görüntü bulunursa, önceki örneklerde olduğu gibi yeni bir yol ve dosya adı, oluşturun.</span><span class="sxs-lookup"><span data-stu-id="accee-242">If an image is found, you construct a new path and file name, like you did in earlier examples.</span></span> <span data-ttu-id="accee-243">Görüntü ters çevirmek için çağrı `FlipVertical` yöntemi ve ardından, kaydetmek görüntü yeniden.</span><span class="sxs-lookup"><span data-stu-id="accee-243">To flip the image, you call the `FlipVertical` method, and then you save the image again.</span></span>

    <span data-ttu-id="accee-244">Görüntüyü kullanarak sayfada yeniden görüntülenen `<img>` öğeyle `src` özniteliğini `imagePath`.</span><span class="sxs-lookup"><span data-stu-id="accee-244">The image is again displayed on the page by using the `<img>` element with the `src` attribute set to `imagePath`.</span></span>
3. <span data-ttu-id="accee-245">Sayfanın tarayıcıda çalıştırın.</span><span class="sxs-lookup"><span data-stu-id="accee-245">Run the page in a browser.</span></span> <span data-ttu-id="accee-246">Görüntüyü *Photo2.jpg* tersyüz gösterilir.</span><span class="sxs-lookup"><span data-stu-id="accee-246">The image for *Photo2.jpg* is shown upside down.</span></span>
4. <span data-ttu-id="accee-247">Sayfayı yenileyin veya yeniden görüntü çevrilen sağ tarafındaki yukarı yeniden görmek için sayfayı isteyin.</span><span class="sxs-lookup"><span data-stu-id="accee-247">Refresh the page or request the page again to see the image is flipped right side up again.</span></span>

<span data-ttu-id="accee-248">Görüntü döndürme için hariç çağırmak yerine, aynı kodu kullanın. `FlipVertical` veya `FlipHorizontal`, çağırmanızı `RotateLeft` veya `RotateRight`.</span><span class="sxs-lookup"><span data-stu-id="accee-248">To rotate an image, you use the same code, except that instead of calling the `FlipVertical` or `FlipHorizontal`, you call `RotateLeft` or `RotateRight`.</span></span>

<a id="Adding_a_Watermark"></a>
## <a name="adding-a-watermark-to-an-image"></a><span data-ttu-id="accee-249">Resme bir filigran ekleme</span><span class="sxs-lookup"><span data-stu-id="accee-249">Adding a Watermark to an Image</span></span>

<span data-ttu-id="accee-250">Web sitenize birini eklediğinizde, bu görüntüleri kaydedin veya bir sayfada görüntülemek için önce bir filigran görüntüye eklemek isteyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="accee-250">When you add images to your website, you might want to add a watermark to the image before you save it or display it on a page.</span></span> <span data-ttu-id="accee-251">Kişiler, filigranlar genellikle telif hakkı bilgileri görüntüye eklemek veya iş adlarına bildirmek için kullanın.</span><span class="sxs-lookup"><span data-stu-id="accee-251">People often use watermarks to add copyright information to an image or to advertise their business name.</span></span>

<span data-ttu-id="accee-252">![[Görüntü]](9-working-with-images/_static/image5.jpg "ch9images 5.jpg")</span><span class="sxs-lookup"><span data-stu-id="accee-252">![[image]](9-working-with-images/_static/image5.jpg "ch9images-5.jpg")</span></span>

1. <span data-ttu-id="accee-253">Adlı yeni bir sayfa ekleyin *Watermark.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="accee-253">Add a new page named *Watermark.cshtml*.</span></span>
2. <span data-ttu-id="accee-254">Sayfanın mevcut içeriği aşağıdakiyle değiştirin:</span><span class="sxs-lookup"><span data-stu-id="accee-254">Replace the existing content in the page with the following:</span></span> 

    [!code-cshtml[Main](9-working-with-images/samples/sample8.cshtml)]

    <span data-ttu-id="accee-255">Bu kod, kodda benzer *FlipImage.cshtml* sayfasından daha önce (bu sefer olsa da, kullandığı *Photo3.jpg* dosyası).</span><span class="sxs-lookup"><span data-stu-id="accee-255">This code is like the code in the *FlipImage.cshtml* page from earlier (although this time it uses the *Photo3.jpg* file).</span></span> <span data-ttu-id="accee-256">Filigran eklemek için çağrı `WebImage` yardımcının `AddTextWatermark` görüntü kaydetmeden önce yöntemi.</span><span class="sxs-lookup"><span data-stu-id="accee-256">To add the watermark, you call the `WebImage` helper's `AddTextWatermark` method before you save the image.</span></span> <span data-ttu-id="accee-257">Çağrısında `AddTextWatermark`, metin geçirdiğiniz &quot;My Filigran&quot;, yazı tipi rengi sarı olarak ayarlayın ve için Arial yazı tipi ailesini ayarlayın.</span><span class="sxs-lookup"><span data-stu-id="accee-257">In the call to `AddTextWatermark`, you pass the text &quot;My Watermark&quot;, set the font color to yellow, and set the font family to Arial.</span></span> <span data-ttu-id="accee-258">(Burada, gösterilmese `WebImage` yardımcı ayrıca opaklık, yazı tipi ailesi ve yazı tipi boyutu ve konumu Filigran metninin belirtmenizi sağlar.) Görüntüyü kaydederken salt okunur olmamalıdır.</span><span class="sxs-lookup"><span data-stu-id="accee-258">(Although it's not shown here, the `WebImage` helper also lets you specify opacity, font family and font size, and the position of the watermark text.) When you save the image it must not be read-only.</span></span>

    <span data-ttu-id="accee-259">Önce gördüğünüz gibi görüntü sayfasında kullanarak gösterilen `<img>` src özniteliği olan öğe kümesine `@imagePath`.</span><span class="sxs-lookup"><span data-stu-id="accee-259">As you've seen before, the image is displayed on the page by using the `<img>` element with the src attribute set to `@imagePath`.</span></span>
3. <span data-ttu-id="accee-260">Sayfanın tarayıcıda çalıştırın.</span><span class="sxs-lookup"><span data-stu-id="accee-260">Run the page in a browser.</span></span> <span data-ttu-id="accee-261">Metin "My Filigran" görüntüsünün sağ alt köşedeki dikkat edin.</span><span class="sxs-lookup"><span data-stu-id="accee-261">Notice the text "My Watermark" at the bottom-right corner of the image.</span></span>

<a id="Using_an_Image_as_a_Watermark"></a>
## <a name="using-an-image-as-a-watermark"></a><span data-ttu-id="accee-262">Filigran olarak görüntü kullanma</span><span class="sxs-lookup"><span data-stu-id="accee-262">Using an Image As a Watermark</span></span>

<span data-ttu-id="accee-263">Metin için Filigran kullanmak yerine, başka bir görüntü kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="accee-263">Instead of using text for a watermark, you can use another image.</span></span> <span data-ttu-id="accee-264">Kişiler bazen bir şirket logosu gibi görüntülerini filigran olarak veya bunların bir filigran resmi yerine metin için telif hakkı bilgileri kullanın.</span><span class="sxs-lookup"><span data-stu-id="accee-264">People sometimes use images like a company logo as a watermark, or they use a watermark image instead of text for copyright information.</span></span>

<span data-ttu-id="accee-265">![[Görüntü]](9-working-with-images/_static/image6.jpg "ch9images 6.jpg")</span><span class="sxs-lookup"><span data-stu-id="accee-265">![[image]](9-working-with-images/_static/image6.jpg "ch9images-6.jpg")</span></span>

1. <span data-ttu-id="accee-266">Adlı yeni bir sayfa ekleyin *ImageWatermark.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="accee-266">Add a new page named *ImageWatermark.cshtml*.</span></span>
2. <span data-ttu-id="accee-267">Görüntüye ekleme *görüntüleri* logo olarak kullanın ve görüntüyü tekrar adlandırmanız klasörü *MyCompanyLogo.jpg*.</span><span class="sxs-lookup"><span data-stu-id="accee-267">Add an image to the *images* folder that you can use as a logo, and rename the image *MyCompanyLogo.jpg*.</span></span> <span data-ttu-id="accee-268">Bu görüntü 80 piksel genişliğinde ve 20 piksel yüksekliğinde ayarlandığında açıkça görebileceğiniz bir görüntü olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="accee-268">This image should be an image that you can see clearly when it's set to 80 pixels wide and 20 pixels high.</span></span>
3. <span data-ttu-id="accee-269">Sayfanın mevcut içeriği aşağıdakiyle değiştirin:</span><span class="sxs-lookup"><span data-stu-id="accee-269">Replace the existing content in the page with the following:</span></span> 

    [!code-cshtml[Main](9-working-with-images/samples/sample9.cshtml)]

    <span data-ttu-id="accee-270">Önceki örneklerle kod başka bir değişim budur.</span><span class="sxs-lookup"><span data-stu-id="accee-270">This is another variation on the code from earlier examples.</span></span> <span data-ttu-id="accee-271">Bu durumda, çağırmanızı `AddImageWatermark` Filigran resminin hedef görüntüsüne eklemek için (*Photo3.jpg*) görüntü kaydetmeden önce.</span><span class="sxs-lookup"><span data-stu-id="accee-271">In this case, you call `AddImageWatermark` to add the watermark image to the target image (*Photo3.jpg*) before you save the image.</span></span> <span data-ttu-id="accee-272">Çağırdığınızda `AddImageWatermark`, 20 piksel yüksekliği ve 80 piksel genişliği ayarlayın.</span><span class="sxs-lookup"><span data-stu-id="accee-272">When you call `AddImageWatermark`, you set its width to 80 pixels and the height to 20 pixels.</span></span> <span data-ttu-id="accee-273">*MyCompanyLogo.jpg* görüntüsü Merkezi'nde yatay olarak Hizala ve hedef görüntü alt kısmında dikey hizalı.</span><span class="sxs-lookup"><span data-stu-id="accee-273">The *MyCompanyLogo.jpg* image is horizontally aligned in the center and vertically aligned at the bottom of the target image.</span></span> <span data-ttu-id="accee-274">Opaklık % 100'e ayarlanır ve doldurma 10 piksel olarak ayarlanır.</span><span class="sxs-lookup"><span data-stu-id="accee-274">The opacity is set to 100% and the padding is set to 10 pixels.</span></span> <span data-ttu-id="accee-275">Filigran resminin hedef görüntüden daha büyük ise, hiçbir şey olmaz.</span><span class="sxs-lookup"><span data-stu-id="accee-275">If the watermark image is bigger than the target image, nothing will happen.</span></span> <span data-ttu-id="accee-276">Filigran resminin hedef görüntüden daha büyüktür ve sıfır olarak görüntü Filigran doldurma ayarlarsanız, filigran göz ardı edilir.</span><span class="sxs-lookup"><span data-stu-id="accee-276">If the watermark image is bigger than the target image and you set the padding for the image watermark to zero, the watermark is ignored.</span></span>

    <span data-ttu-id="accee-277">Önce görüntü kullanarak görüntüleme gibi `<img>` öğesi ve dinamik `src` özniteliği.</span><span class="sxs-lookup"><span data-stu-id="accee-277">As before, you display the image using the `<img>` element and a dynamic `src` attribute.</span></span>
4. <span data-ttu-id="accee-278">Sayfanın tarayıcıda çalıştırın.</span><span class="sxs-lookup"><span data-stu-id="accee-278">Run the page in a browser.</span></span> <span data-ttu-id="accee-279">Filigran resminin ana görüntü en altında göründüğüne dikkat edin.</span><span class="sxs-lookup"><span data-stu-id="accee-279">Notice that the watermark image appears at the bottom of the main image.</span></span>

<a id="Additional_Resources"></a>
## <a name="additional-resources"></a><span data-ttu-id="accee-280">Ek Kaynaklar</span><span class="sxs-lookup"><span data-stu-id="accee-280">Additional Resources</span></span>


[<span data-ttu-id="accee-281">Bir ASP.NET Web sayfaları sitesinde dosyalarıyla çalışma</span><span class="sxs-lookup"><span data-stu-id="accee-281">Working with Files in an ASP.NET Web Pages Site</span></span>](https://go.microsoft.com/fwlink/?LinkId=202896)

[<span data-ttu-id="accee-282">ASP.NET Web sayfaları programlama Razor söz dizimini kullanarak giriş</span><span class="sxs-lookup"><span data-stu-id="accee-282">Introduction to ASP.NET Web Pages Programming Using the Razor Syntax</span></span>](https://go.microsoft.com/fwlink/?LinkID=251587)
