---
uid: web-pages/overview/getting-started/introducing-aspnet-web-pages-2/updating-data
title: ASP.NET Web sayfalarına giriş-veritabanı verilerini güncelleştirme | Microsoft Docs
author: Rick-Anderson
description: Bu öğreticide, ASP.NET Web Pages (Razor) kullandığınızda var olan bir veritabanı girişini güncelleştirme (değiştirme) gösterilmektedir. Seriyi tamamladığınız varsayılır...
ms.author: riande
ms.date: 01/02/2018
ms.assetid: ac86ec9c-6b69-485b-b9e0-8b9127b13e6b
msc.legacyurl: /web-pages/overview/getting-started/introducing-aspnet-web-pages-2/updating-data
msc.type: authoredcontent
ms.openlocfilehash: 8f8bcfb7d9d2416a2699776cadbdaae8e12415ba
ms.sourcegitcommit: e7e91932a6e91a63e2e46417626f39d6b244a3ab
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 03/06/2020
ms.locfileid: "78574230"
---
# <a name="introducing-aspnet-web-pages---updating-database-data"></a><span data-ttu-id="928a3-104">ASP.NET Web sayfalarına giriş-veritabanı verilerini güncelleştirme</span><span class="sxs-lookup"><span data-stu-id="928a3-104">Introducing ASP.NET Web Pages - Updating Database Data</span></span>

<span data-ttu-id="928a3-105">[Tom FitzMacken](https://github.com/tfitzmac) tarafından</span><span class="sxs-lookup"><span data-stu-id="928a3-105">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="928a3-106">Bu öğreticide, ASP.NET Web Pages (Razor) kullandığınızda var olan bir veritabanı girişini güncelleştirme (değiştirme) gösterilmektedir.</span><span class="sxs-lookup"><span data-stu-id="928a3-106">This tutorial shows you how to update (change) an existing database entry when you use ASP.NET Web Pages (Razor).</span></span> <span data-ttu-id="928a3-107">[ASP.NET Web sayfalarını kullanarak formları kullanarak seriyi, verileri girerek](entering-data.md)tamamlamış olduğunu varsayar.</span><span class="sxs-lookup"><span data-stu-id="928a3-107">It assumes you have completed the series through [Entering Data by Using Forms Using ASP.NET Web Pages](entering-data.md).</span></span>
> 
> <span data-ttu-id="928a3-108">Öğrenecekleriniz:</span><span class="sxs-lookup"><span data-stu-id="928a3-108">What you'll learn:</span></span>
> 
> - <span data-ttu-id="928a3-109">`WebGrid` Yardımcısı 'nda tek bir kayıt seçme.</span><span class="sxs-lookup"><span data-stu-id="928a3-109">How to select an individual record in the `WebGrid` helper.</span></span>
> - <span data-ttu-id="928a3-110">Veritabanından tek bir kayıt okuma.</span><span class="sxs-lookup"><span data-stu-id="928a3-110">How to read a single record from a database.</span></span>
> - <span data-ttu-id="928a3-111">Veritabanı kaydındaki değerlerle bir formu önceden yükleme.</span><span class="sxs-lookup"><span data-stu-id="928a3-111">How to preload a form with values from the database record.</span></span>
> - <span data-ttu-id="928a3-112">Veritabanında var olan bir kaydı güncelleştirme.</span><span class="sxs-lookup"><span data-stu-id="928a3-112">How to update an existing record in a database.</span></span>
> - <span data-ttu-id="928a3-113">Bilgiyi sayfada görüntülemeden depolama.</span><span class="sxs-lookup"><span data-stu-id="928a3-113">How to store information in the page without displaying it.</span></span>
> - <span data-ttu-id="928a3-114">Bilgileri depolamak için gizli bir alan kullanma.</span><span class="sxs-lookup"><span data-stu-id="928a3-114">How to use a hidden field to store information.</span></span>
>   
> 
> <span data-ttu-id="928a3-115">Ele alınan özellikler/teknolojiler:</span><span class="sxs-lookup"><span data-stu-id="928a3-115">Features/technologies discussed:</span></span>
> 
> - <span data-ttu-id="928a3-116">`WebGrid` Yardımcısı.</span><span class="sxs-lookup"><span data-stu-id="928a3-116">The `WebGrid` helper.</span></span>
> - <span data-ttu-id="928a3-117">SQL `Update` komutu.</span><span class="sxs-lookup"><span data-stu-id="928a3-117">The SQL `Update` command.</span></span>
> - <span data-ttu-id="928a3-118">`Database.Execute` yöntemi.</span><span class="sxs-lookup"><span data-stu-id="928a3-118">The `Database.Execute` method.</span></span>
> - <span data-ttu-id="928a3-119">Gizli alanlar (`<input type="hidden">`).</span><span class="sxs-lookup"><span data-stu-id="928a3-119">Hidden fields (`<input type="hidden">`).</span></span>

## <a name="what-youll-build"></a><span data-ttu-id="928a3-120">Ne oluşturacağız?</span><span class="sxs-lookup"><span data-stu-id="928a3-120">What You'll Build</span></span>

<span data-ttu-id="928a3-121">Önceki öğreticide, bir veritabanına kayıt eklemeyi öğrendiniz.</span><span class="sxs-lookup"><span data-stu-id="928a3-121">In the previous tutorial, you learned how to add a record to a database.</span></span> <span data-ttu-id="928a3-122">Burada, bir kaydın düzenlenmek üzere nasıl görüntüleneceği hakkında bilgi edineceksiniz.</span><span class="sxs-lookup"><span data-stu-id="928a3-122">Here, you'll learn how to display a record for editing.</span></span> <span data-ttu-id="928a3-123">*Filmler* sayfasında, her filmin yanında bir **düzenleme** bağlantısı görüntüleyecek şekilde `WebGrid` Yardımcısı 'nı güncelleştireceksiniz:</span><span class="sxs-lookup"><span data-stu-id="928a3-123">In the *Movies* page, you'll update the `WebGrid` helper so that it displays an **Edit** link next to each movie:</span></span>

![Her film için ' düzenleme ' bağlantısı dahil olmak üzere WebGrid görüntüsü](updating-data/_static/image1.png)

<span data-ttu-id="928a3-125">**Düzenleme** bağlantısına tıkladığınızda, sizi film bilgisinin zaten bir biçimde olduğu farklı bir sayfaya götürür:</span><span class="sxs-lookup"><span data-stu-id="928a3-125">When you click the **Edit** link, it takes you to a different page, where the movie information is already in a form:</span></span>

![Düzenlenecek filmi gösteren film sayfasını düzenleme](updating-data/_static/image2.png)

<span data-ttu-id="928a3-127">Değerlerden herhangi birini değiştirebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="928a3-127">You can change any of the values.</span></span> <span data-ttu-id="928a3-128">Değişiklikleri gönderdiğinizde, sayfadaki kod veritabanını güncelleştirir ve sizi film listesine geri götürür.</span><span class="sxs-lookup"><span data-stu-id="928a3-128">When you submit the changes, the code in the page updates the database and takes you back to the movie listing.</span></span>

<span data-ttu-id="928a3-129">İşlemin bu bölümü, önceki öğreticide oluşturduğunuz *Addmovie. cshtml* sayfası gibi neredeyse tam olarak çalışır, bu nedenle Bu öğreticinin çoğu tanıdık gelecektir.</span><span class="sxs-lookup"><span data-stu-id="928a3-129">This part of the process works almost exactly like the *AddMovie.cshtml* page you created in the previous tutorial, so much of this tutorial will be familiar.</span></span>

<span data-ttu-id="928a3-130">Tek bir filmi düzenlemek için bir yol uygulayabileceğiniz çeşitli yollar vardır.</span><span class="sxs-lookup"><span data-stu-id="928a3-130">There are several ways you could implement a way to edit an individual movie.</span></span> <span data-ttu-id="928a3-131">Gösterilen yaklaşım, kolayca uygulanması ve anlaşılması kolay olduğundan, seçilir.</span><span class="sxs-lookup"><span data-stu-id="928a3-131">The approach shown was chosen because it's easy to implement and easy to understand.</span></span>

## <a name="adding-an-edit-link-to-the-movie-listing"></a><span data-ttu-id="928a3-132">Film listesine düzenleme bağlantısı ekleme</span><span class="sxs-lookup"><span data-stu-id="928a3-132">Adding an Edit Link to the Movie Listing</span></span>

<span data-ttu-id="928a3-133">Başlamak için, *filmler* sayfasını her bir film listesinin de bir **düzenleme** bağlantısı içermesi için güncelleştireceksiniz.</span><span class="sxs-lookup"><span data-stu-id="928a3-133">To begin, you'll update the *Movies* page so that each movie listing also contains an **Edit** link.</span></span>

<span data-ttu-id="928a3-134">*Filmler. cshtml* dosyasını açın.</span><span class="sxs-lookup"><span data-stu-id="928a3-134">Open the *Movies.cshtml* file.</span></span>

<span data-ttu-id="928a3-135">Sayfanın gövdesinde, `WebGrid` işaretlemesini bir sütun ekleyerek değiştirin.</span><span class="sxs-lookup"><span data-stu-id="928a3-135">In the body of the page, change the `WebGrid` markup by adding a column.</span></span> <span data-ttu-id="928a3-136">Değiştirilen biçimlendirme şu şekildedir:</span><span class="sxs-lookup"><span data-stu-id="928a3-136">Here's the modified markup:</span></span>

[!code-html[Main](updating-data/samples/sample1.html?highlight=6)]

<span data-ttu-id="928a3-137">Yeni sütun budur:</span><span class="sxs-lookup"><span data-stu-id="928a3-137">The new column is this one:</span></span>

[!code-html[Main](updating-data/samples/sample2.html)]

<span data-ttu-id="928a3-138">Bu sütunun noktası, metni "Düzenle" olan bir bağlantı (`<a>` öğesi) göstermek içindir.</span><span class="sxs-lookup"><span data-stu-id="928a3-138">The point of this column is to show a link (`<a>` element) whose text says "Edit".</span></span> <span data-ttu-id="928a3-139">Ne yaptığımız, sayfa çalıştırıldığında aşağıdaki gibi görünen bir bağlantı oluşturmaktır, her film için `id` değeri farklıdır:</span><span class="sxs-lookup"><span data-stu-id="928a3-139">What we're after is to create a link that looks like the following when the page runs, with the `id` value different for each movie:</span></span>

[!code-css[Main](updating-data/samples/sample3.css)]

<span data-ttu-id="928a3-140">Bu bağlantı, *Editmovie*adlı bir sayfayı çağırır ve sorgu dizesi `?id=7` bu sayfaya geçilecektir.</span><span class="sxs-lookup"><span data-stu-id="928a3-140">This link will invoke a page named *EditMovie*, and it will pass the query string `?id=7` to that page.</span></span>

<span data-ttu-id="928a3-141">Yeni sütunun sözdizimi bir bit karmaşık görünebilir, ancak bu yalnızca birkaç öğe yerleştirdiğinden geçerlidir.</span><span class="sxs-lookup"><span data-stu-id="928a3-141">The syntax for the new column might look a bit complex, but that's only because it puts together several elements.</span></span> <span data-ttu-id="928a3-142">Her bireysel öğe basittir.</span><span class="sxs-lookup"><span data-stu-id="928a3-142">Each individual element is straightforward.</span></span> <span data-ttu-id="928a3-143">Yalnızca `<a>` öğesi üzerinde odaklandıysanız şu biçimlendirmeyi görürsünüz:</span><span class="sxs-lookup"><span data-stu-id="928a3-143">If you concentrate on just the `<a>` element, you see this markup:</span></span>

[!code-html[Main](updating-data/samples/sample4.html)]

<span data-ttu-id="928a3-144">Kılavuzun nasıl çalıştığı hakkında bazı arka plan: kılavuz, her veritabanı kaydı için bir tane olmak üzere satırları görüntüler ve veritabanı kaydındaki her bir alan için sütunları görüntüler.</span><span class="sxs-lookup"><span data-stu-id="928a3-144">Some background about how the grid works: the grid displays rows, one for each database record, and it displays columns for each field in the database record.</span></span> <span data-ttu-id="928a3-145">Her kılavuz satırı oluşturulurken, `item` nesnesi bu satırın veritabanı kaydını (öğe) içerir.</span><span class="sxs-lookup"><span data-stu-id="928a3-145">While each grid row is being constructed, the `item` object contains the database record (item) for that row.</span></span> <span data-ttu-id="928a3-146">Bu düzenleme, bu satır için verilere ulaşmak üzere kodda bir yol sağlar.</span><span class="sxs-lookup"><span data-stu-id="928a3-146">This arrangement gives you a way in code to get at the data for that row.</span></span> <span data-ttu-id="928a3-147">Burada gördüğünüz gibi: ifadesi `item.ID` geçerli veritabanı öğesinin KIMLIK değerini alıyor.</span><span class="sxs-lookup"><span data-stu-id="928a3-147">That's what you see here: the expression `item.ID` is getting the ID value of the current database item.</span></span> <span data-ttu-id="928a3-148">Veritabanı değerlerinden (başlık, tarz veya yıl) herhangi birini, `item.Title`, `item.Genre`veya `item.Year`kullanarak aynı şekilde alabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="928a3-148">You could get any of the database values (title, genre, or year) the same way by using `item.Title`, `item.Genre`, or `item.Year`.</span></span>

<span data-ttu-id="928a3-149">İfade `"~/EditMovie?id=@item.ID` hedef URL 'nin (`~/EditMovie?id=`) sabit kodlanmış bölümünü bu dinamik olarak türetilen KIMLIKLE birleştirir.</span><span class="sxs-lookup"><span data-stu-id="928a3-149">The expression `"~/EditMovie?id=@item.ID` combines the hard-coded part of the target URL (`~/EditMovie?id=`) with this dynamically derived ID.</span></span> <span data-ttu-id="928a3-150">(`~` işlecini önceki öğreticide gördünüz; bu, geçerli Web sitesi kökünü temsil eden bir ASP.NET işleçtir.)</span><span class="sxs-lookup"><span data-stu-id="928a3-150">(You saw the `~` operator in the previous tutorial; it's an ASP.NET operator that represents the current website root.)</span></span>

<span data-ttu-id="928a3-151">Sonuç olarak sütundaki biçimlendirmenin bu bölümü, çalışma zamanında aşağıdaki biçimlendirme gibi bir şey üretir:</span><span class="sxs-lookup"><span data-stu-id="928a3-151">The result is that this part of the markup in the column simply produces something like the following markup at run time:</span></span>

[!code-xml[Main](updating-data/samples/sample5.xml)]

<span data-ttu-id="928a3-152">Doğal olarak, `id` gerçek değeri her satır için farklı olacaktır.</span><span class="sxs-lookup"><span data-stu-id="928a3-152">Naturally, the actual value of `id` will be different for each row.</span></span>

## <a name="creating-a-custom-display-for-a-grid-column"></a><span data-ttu-id="928a3-153">Kılavuz sütunu için özel görüntü oluşturma</span><span class="sxs-lookup"><span data-stu-id="928a3-153">Creating a Custom Display for a Grid Column</span></span>

<span data-ttu-id="928a3-154">Şimdi kılavuz sütununa geri dönün.</span><span class="sxs-lookup"><span data-stu-id="928a3-154">Now back to the grid column.</span></span> <span data-ttu-id="928a3-155">Başlangıçta kılavuzda bulunan üç sütun yalnızca veri değerlerini (başlık, tarz ve yıl) görüntüdi.</span><span class="sxs-lookup"><span data-stu-id="928a3-155">The three columns you originally had in the grid displayed only data values (title, genre, and year).</span></span> <span data-ttu-id="928a3-156">Bu ekranı, veritabanı sütununun adını geçirerek belirttiniz &mdash; Örneğin, `grid.Column("Title")`.</span><span class="sxs-lookup"><span data-stu-id="928a3-156">You specified this display by passing the name of the database column &mdash; for example, `grid.Column("Title")`.</span></span>

<span data-ttu-id="928a3-157">Bu yeni **düzenleme** bağlantısı sütunu farklı.</span><span class="sxs-lookup"><span data-stu-id="928a3-157">This new **Edit** link column is different.</span></span> <span data-ttu-id="928a3-158">Bir sütun adı belirtmek yerine bir `format` parametresi geçiriyoruz.</span><span class="sxs-lookup"><span data-stu-id="928a3-158">Instead of specifying a column name, you're passing a `format` parameter.</span></span> <span data-ttu-id="928a3-159">Bu parametre, `WebGrid` Yardımcısı 'nın, sütun verilerini kalın veya yeşil ya da istediğiniz biçimde görüntülemek için `item` değeriyle birlikte işleneceğini belirten biçimlendirme tanımlamanızı sağlar.</span><span class="sxs-lookup"><span data-stu-id="928a3-159">This parameter lets you define markup that the `WebGrid` helper will render along with the `item` value to display the column data as bold or green or in whatever format that you want.</span></span> <span data-ttu-id="928a3-160">Örneğin, başlığın kalın görünmesini isterseniz, aşağıdaki örnekte olduğu gibi bir sütun oluşturabilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="928a3-160">For example, if you wanted the title to appear bold, you could create a column like this example:</span></span>

[!code-html[Main](updating-data/samples/sample6.html)]

<span data-ttu-id="928a3-161">(`format` özelliğinde gördüğünüz çeşitli `@` karakterleri, biçimlendirme ve bir kod değeri arasındaki geçişi işaret ediyor.)</span><span class="sxs-lookup"><span data-stu-id="928a3-161">(The various `@` characters you see in the `format` property mark the transition between markup and a code value.)</span></span>

<span data-ttu-id="928a3-162">`format` özelliği hakkında bilgi aldıktan sonra, yeni **düzenleme** bağlantısı sütununun nasıl bir araya yerleştirileceğini anlamak daha kolay olur:</span><span class="sxs-lookup"><span data-stu-id="928a3-162">Once you know about the `format` property, it's easier to understand how the new **Edit** link column is put together:</span></span>

[!code-html[Main](updating-data/samples/sample7.html)]

<span data-ttu-id="928a3-163">Sütun *yalnızca* bağlantıyı işleyen biçimlendirmeyi ve satırın veritabanı kaydından ayıklanan bazı BILGILERI (kimliği) içerir.</span><span class="sxs-lookup"><span data-stu-id="928a3-163">The column consists *only* of the markup that renders the link, plus some information (the ID) that's extracted from the database record for the row.</span></span>

> [!TIP]
> 
> <span data-ttu-id="928a3-164">**Bir yöntem için adlandırılmış parametreler ve Konumsal parametreler**</span><span class="sxs-lookup"><span data-stu-id="928a3-164">**Named Parameters and Positional Parameters for a Method**</span></span>
> 
> <span data-ttu-id="928a3-165">Bir yöntemi çağırdığınızda ve ona parametreler geçirdiyseniz, parametre değerleri yalnızca virgülle ayrılmış olarak listelenir.</span><span class="sxs-lookup"><span data-stu-id="928a3-165">Many times when you've called a method and passed parameters to it, you've simply listed the parameter values separated by commas.</span></span> <span data-ttu-id="928a3-166">İşte birkaç örnek:</span><span class="sxs-lookup"><span data-stu-id="928a3-166">Here are a couple of examples:</span></span>
> 
> `db.Execute(insertCommand, title, genre, year)`
> 
> `Validation.RequireField("title", "You must enter a title")`
> 
> <span data-ttu-id="928a3-167">Bu kodu ilk kez gördüğünüzde bu sorunla karşılaşmadınız, ancak her durumda, parametreleri belirli bir sırada yöntemlere geçirdiniz &mdash; Bu yöntemde parametrelerin tanımlandığı sıra.</span><span class="sxs-lookup"><span data-stu-id="928a3-167">We didn't mention the issue when you first saw this code, but in each case, you're passing parameters to the methods in a specific order &mdash; namely, the order in which the parameters are defined in that method.</span></span> <span data-ttu-id="928a3-168">`db.Execute` ve `Validation.RequireFields`için, geçirdiğiniz değerlerin sırasını karıştırdıysanız, sayfa çalıştırıldığında bir hata mesajı veya en az bir garip sonuç elde edersiniz.</span><span class="sxs-lookup"><span data-stu-id="928a3-168">For `db.Execute` and `Validation.RequireFields`, if you mixed up the order of the values you pass, you'd get an error message when the page runs, or at least some strange results.</span></span> <span data-ttu-id="928a3-169">Açık olarak, içindeki parametrelerin iletilmesi sırasını bilmeniz gerekir.</span><span class="sxs-lookup"><span data-stu-id="928a3-169">Clearly, you have to know the order to pass the parameters in.</span></span> <span data-ttu-id="928a3-170">(WebMatrix 'te IntelliSense, parametrelerin adını, türünü ve sırasını öğrenmenize yardımcı olabilir.)</span><span class="sxs-lookup"><span data-stu-id="928a3-170">(In WebMatrix, IntelliSense can help you learn figure out the name, type, and order of the parameters.)</span></span>
> 
> <span data-ttu-id="928a3-171">Değerleri sırasıyla geçirme alternatifi olarak *adlandırılmış parametreleri*kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="928a3-171">As an alternative to passing values in order, you can use *named parameters*.</span></span> <span data-ttu-id="928a3-172">(Parametreleri sırayla geçirme, *Konumsal parametreler*kullanma olarak bilinir.) Adlandırılmış parametreler için, değerini geçirirken parametrenin adını açıkça dahil edersiniz.</span><span class="sxs-lookup"><span data-stu-id="928a3-172">(Passing parameters in order is known as using *positional parameters*.) For named parameters, you explicitly include the name of the parameter when passing its value.</span></span> <span data-ttu-id="928a3-173">Adlandırılmış parametreleri bu öğreticilerde zaten birkaç kez kullandınız.</span><span class="sxs-lookup"><span data-stu-id="928a3-173">You've used named parameters already a number of times in these tutorials.</span></span> <span data-ttu-id="928a3-174">Örneğin:</span><span class="sxs-lookup"><span data-stu-id="928a3-174">For example:</span></span>
> 
> [!code-csharp[Main](updating-data/samples/sample8.cs)]
> 
> <span data-ttu-id="928a3-175">and</span><span class="sxs-lookup"><span data-stu-id="928a3-175">and</span></span>
> 
> [!code-css[Main](updating-data/samples/sample9.css)]
> 
> <span data-ttu-id="928a3-176">Adlandırılmış parametreler, özellikle bir yöntem birçok parametre alırsa, birkaç durum için kullanışlıdır.</span><span class="sxs-lookup"><span data-stu-id="928a3-176">Named parameters are handy for a couple of situations, especially when a method takes many parameters.</span></span> <span data-ttu-id="928a3-177">Bunlardan biri yalnızca bir veya iki parametre geçirmek istediğinizde, ancak geçirmek istediğiniz değerler parametre listesindeki ilk konumlar arasında değil.</span><span class="sxs-lookup"><span data-stu-id="928a3-177">One is when you want to pass only one or two parameters, but the values you want to pass are not among the first positions in the parameter list.</span></span> <span data-ttu-id="928a3-178">Diğer bir durum ise, parametreleri en anlamlı hale getiren sırada, kodunuzu daha okunaklı hale getirmek istediğinizde olur.</span><span class="sxs-lookup"><span data-stu-id="928a3-178">Another situation is when you want to make your code more readable by passing the parameters in the order that makes the most sense to you.</span></span>
> 
> <span data-ttu-id="928a3-179">Kuşkusuz, adlandırılmış parametreleri kullanmak için parametrelerin adlarını bilmeniz gerekir.</span><span class="sxs-lookup"><span data-stu-id="928a3-179">Obviously, to use named parameters, you have to know the names of the parameters.</span></span> <span data-ttu-id="928a3-180">WebMatrix IntelliSense, adları *gösterebilir* , ancak şu anda bunları sizin için dolduramaz.</span><span class="sxs-lookup"><span data-stu-id="928a3-180">WebMatrix IntelliSense can *show* you the names, but it cannot currently fill them in for you.</span></span>

## <a name="creating-the-edit-page"></a><span data-ttu-id="928a3-181">Düzenleme sayfası oluşturma</span><span class="sxs-lookup"><span data-stu-id="928a3-181">Creating the Edit Page</span></span>

<span data-ttu-id="928a3-182">Şimdi *Editmovie* sayfasını oluşturabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="928a3-182">Now you can create the *EditMovie* page.</span></span> <span data-ttu-id="928a3-183">Kullanıcılar **düzenleme** bağlantısına tıkladıklarında bu sayfada sona abileceksiniz.</span><span class="sxs-lookup"><span data-stu-id="928a3-183">When users click the **Edit** link, they'll end up on this page.</span></span>

<span data-ttu-id="928a3-184">*Editmovie. cshtml* adlı bir sayfa oluşturun ve dosyadaki nelerin aşağıdaki biçimlendirmeyle değiştirin:</span><span class="sxs-lookup"><span data-stu-id="928a3-184">Create a page named *EditMovie.cshtml* and replace what's in the file with the following markup:</span></span>

[!code-cshtml[Main](updating-data/samples/sample10.cshtml)]

<span data-ttu-id="928a3-185">Bu biçimlendirme ve kod, *Addmovie* sayfasında sahip olduğunuz şekilde benzerdir.</span><span class="sxs-lookup"><span data-stu-id="928a3-185">This markup and code is similar to what you have in the *AddMovie* page.</span></span> <span data-ttu-id="928a3-186">Gönder düğmesinin metninde küçük bir fark vardır.</span><span class="sxs-lookup"><span data-stu-id="928a3-186">There's a small difference in the text for the submit button.</span></span> <span data-ttu-id="928a3-187">*Addmovie* sayfasında olduğu gibi, varsa doğrulama hatalarını görüntüleyen bir `Html.ValidationSummary` çağrısı vardır.</span><span class="sxs-lookup"><span data-stu-id="928a3-187">As with the *AddMovie* page, there's an `Html.ValidationSummary` call that will display validation errors if there are any.</span></span> <span data-ttu-id="928a3-188">Bu süre, doğrulama özetinde hatalar görüntüleneceği için `Validation.Message`çağrıları terk ettik.</span><span class="sxs-lookup"><span data-stu-id="928a3-188">This time we're leaving out calls to `Validation.Message`, since errors will be displayed in the validation summary.</span></span> <span data-ttu-id="928a3-189">Önceki öğreticide belirtildiği gibi, çeşitli birleşimlerde doğrulama özetini ve bireysel hata iletilerini kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="928a3-189">As noted in the previous tutorial, you can use the validation summary and the individual error messages in various combinations.</span></span>

<span data-ttu-id="928a3-190">`<form>` öğesinin `method` özniteliği `post`olarak ayarlandığını unutmayın.</span><span class="sxs-lookup"><span data-stu-id="928a3-190">Notice again that the `method` attribute of the `<form>` element is set to `post`.</span></span> <span data-ttu-id="928a3-191">*Addmovie. cshtml* sayfasında olduğu gibi, Bu sayfa veritabanında değişiklikler yapar.</span><span class="sxs-lookup"><span data-stu-id="928a3-191">As with the *AddMovie.cshtml* page, this page makes changes to the database.</span></span> <span data-ttu-id="928a3-192">Bu nedenle, bu form `POST` bir işlem gerçekleştirmelidir.</span><span class="sxs-lookup"><span data-stu-id="928a3-192">Therefore, this form should perform a `POST` operation.</span></span> <span data-ttu-id="928a3-193">(`GET` ve `POST` işlemleri arasındaki fark hakkında daha fazla bilgi için, HTML formlarında öğreticideki [Get, post ve http fiil güvenliği](form-basics.md#GET,_POST,_and_HTTP_Verb_Safety) kenar çubuğu bölümüne bakın.)</span><span class="sxs-lookup"><span data-stu-id="928a3-193">(For more about the difference between `GET` and `POST` operations, see the [GET, POST, and HTTP Verb Safety](form-basics.md#GET,_POST,_and_HTTP_Verb_Safety) sidebar in the tutorial on HTML forms.)</span></span>

<span data-ttu-id="928a3-194">Önceki bir öğreticide gördüğünüz gibi, metin kutularının `value` öznitelikleri, bunları önceden yüklemek için Razor kodu ile ayarlanır.</span><span class="sxs-lookup"><span data-stu-id="928a3-194">As you saw in an earlier tutorial, the `value` attributes of the text boxes are being set with Razor code in order to preload them.</span></span> <span data-ttu-id="928a3-195">Bu kez, `Request.Form["title"]`yerine bu görev için `title` ve `genre` gibi değişkenler kullanıyorsunuz:</span><span class="sxs-lookup"><span data-stu-id="928a3-195">This time, though, you're using variables like `title` and `genre` for that task instead of `Request.Form["title"]`:</span></span>

`<input type="text" name="title" value="@title" />`

<span data-ttu-id="928a3-196">Daha önce olduğu gibi, bu biçimlendirme metin kutusu değerlerini film değerleriyle önyüklenir.</span><span class="sxs-lookup"><span data-stu-id="928a3-196">As before, this markup will preload the text box values with the movie values.</span></span> <span data-ttu-id="928a3-197">`Request` nesnesini kullanmak yerine, bu kez değişkenlerin nasıl kullanılacağı konusunda bir süre daha görürsünüz.</span><span class="sxs-lookup"><span data-stu-id="928a3-197">You'll see in a moment why it's handy to use variables this time instead of using the `Request` object.</span></span>

<span data-ttu-id="928a3-198">Bu sayfada Ayrıca bir `<input type="hidden">` öğesi vardır.</span><span class="sxs-lookup"><span data-stu-id="928a3-198">There's also a `<input type="hidden">` element on this page.</span></span> <span data-ttu-id="928a3-199">Bu öğe, film KIMLIĞINI sayfada görünür yapmadan depolar.</span><span class="sxs-lookup"><span data-stu-id="928a3-199">This element stores the movie ID without making it visible on the page.</span></span> <span data-ttu-id="928a3-200">KIMLIK, başlangıçta bir sorgu dizesi değeri (`?id=7` veya URL 'de benzer) kullanılarak sayfaya geçirilir.</span><span class="sxs-lookup"><span data-stu-id="928a3-200">The ID is initially passed to the page by using a query string value (`?id=7` or similar in the URL).</span></span> <span data-ttu-id="928a3-201">KIMLIK değerini gizli bir alana yerleştirerek, sayfanın ile birlikte çağrılan özgün URL 'ye artık erişemese de, form gönderildiğinde kullanılabilir olduğundan emin olabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="928a3-201">By putting the ID value into a hidden field, you can make sure that it's available when the form is submitted, even if you no longer have access to the original URL that the page was invoked with.</span></span>

<span data-ttu-id="928a3-202">*Addmovie* sayfasının aksine, *editmovie* sayfasının kodu iki ayrı işleve sahiptir.</span><span class="sxs-lookup"><span data-stu-id="928a3-202">Unlike the *AddMovie* page, the code for the *EditMovie* page has two distinct functions.</span></span> <span data-ttu-id="928a3-203">İlk işlev, sayfa ilk kez görüntülenirken (ve *yalnızca* ), kod sorgu DIZESINDEN film kimliğini alır.</span><span class="sxs-lookup"><span data-stu-id="928a3-203">The first function is that when the page is displayed for the first time (and *only* then), the code gets the movie ID from the query string.</span></span> <span data-ttu-id="928a3-204">Kod daha sonra KIMLIĞI, ilgili filmi veritabanından okumak ve metin kutularında (Önyükle) göstermek için kullanır.</span><span class="sxs-lookup"><span data-stu-id="928a3-204">The code then uses the ID to read the corresponding movie out of the database and display (preload) it in the text boxes.</span></span>

<span data-ttu-id="928a3-205">İkinci işlev, Kullanıcı **Değişiklikleri Gönder** düğmesine tıkladığında, kodun metin kutularının değerlerini okuması ve doğrulanması gerekir.</span><span class="sxs-lookup"><span data-stu-id="928a3-205">The second function is that when the user clicks the **Submit Changes** button, the code has to read the values of the text boxes and validate them.</span></span> <span data-ttu-id="928a3-206">Bu kodun Ayrıca, veritabanı öğesini yeni değerlerle güncelleştirmesi gerekir.</span><span class="sxs-lookup"><span data-stu-id="928a3-206">The code also has to update the database item with the new values.</span></span> <span data-ttu-id="928a3-207">Bu teknik, *Addfilmi*gördüğünüz gibi bir kayıt eklemeye benzer.</span><span class="sxs-lookup"><span data-stu-id="928a3-207">This technique is similar to adding a record, as you saw in *AddMovie*.</span></span>

## <a name="adding-code-to-read-a-single-movie"></a><span data-ttu-id="928a3-208">Tek bir filmi okumak için kod ekleme</span><span class="sxs-lookup"><span data-stu-id="928a3-208">Adding Code to Read a Single Movie</span></span>

<span data-ttu-id="928a3-209">İlk işlevi gerçekleştirmek için, bu kodu sayfanın en üstüne ekleyin:</span><span class="sxs-lookup"><span data-stu-id="928a3-209">To perform the first function, add this code to the top of the page:</span></span>

[!code-cshtml[Main](updating-data/samples/sample11.cshtml)]

<span data-ttu-id="928a3-210">Bu kodun çoğu `if(!IsPost)`başlayan bir bloğun içindedir.</span><span class="sxs-lookup"><span data-stu-id="928a3-210">Most of this code is inside a block that starts `if(!IsPost)`.</span></span> <span data-ttu-id="928a3-211">`!` işleci "Not" anlamına gelir; bu nedenle ifade, bu isteğin *Bu sayfa çalıştırıldığında ilk kez bu isteğin çalıştırılmasının*dolaylı bir yolu olan *gönderi gönderimi olmadığı*anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="928a3-211">The `!` operator means "not," so the expression means *if this request is not a post submission*, which is an indirect way of saying *if this request is the first time that this page has been run*.</span></span> <span data-ttu-id="928a3-212">Daha önce belirtildiği gibi, bu kod *yalnızca* sayfanın ilk kez çalıştırılması gerekir.</span><span class="sxs-lookup"><span data-stu-id="928a3-212">As noted earlier, this code should run *only* the first time the page runs.</span></span> <span data-ttu-id="928a3-213">Kodu `if(!IsPost)`içine almadıysanız, sayfa her çağrıldığında, bir düğmeye ilk kez veya yanıt olarak bir düğme tıklamadığınız her seferinde çalıştırılır.</span><span class="sxs-lookup"><span data-stu-id="928a3-213">If you didn't enclose the code in `if(!IsPost)`, it would run every time the page is invoked, whether the first time or in response to a button click.</span></span>

<span data-ttu-id="928a3-214">Kodun bu kez `else` bloğunu içerdiğine dikkat edin.</span><span class="sxs-lookup"><span data-stu-id="928a3-214">Notice that the code includes an `else` block this time.</span></span> <span data-ttu-id="928a3-215">`if` blokları tanıdığımızda söylediğimiz gibi, bazen test ettiğiniz koşul doğru değilse alternatif kodu çalıştırmak istersiniz.</span><span class="sxs-lookup"><span data-stu-id="928a3-215">As we said when we introduced `if` blocks, sometimes you want to run alternative code if the condition you're testing isn't true.</span></span> <span data-ttu-id="928a3-216">Burada bu durum söz konusu şekildedir.</span><span class="sxs-lookup"><span data-stu-id="928a3-216">That's the case here.</span></span> <span data-ttu-id="928a3-217">Koşul geçerse (yani, sayfaya geçirilen KIMLIĞI tamam ise), veritabanından bir satır okur.</span><span class="sxs-lookup"><span data-stu-id="928a3-217">If the condition passes (that is, if the ID passed to the page is ok), you read a row from the database.</span></span> <span data-ttu-id="928a3-218">Ancak, koşul geçmezse `else` bloğu çalışır ve kod bir hata mesajı ayarlar.</span><span class="sxs-lookup"><span data-stu-id="928a3-218">However, if the condition doesn't pass, the `else` block runs and the code sets an error message.</span></span>

## <a name="validating-a-value-passed-to-the-page"></a><span data-ttu-id="928a3-219">Sayfaya geçirilen bir değer doğrulanıyor</span><span class="sxs-lookup"><span data-stu-id="928a3-219">Validating a Value Passed to the Page</span></span>

<span data-ttu-id="928a3-220">Kod, sayfaya geçirilen KIMLIĞI almak için `Request.QueryString["id"]` kullanır.</span><span class="sxs-lookup"><span data-stu-id="928a3-220">The code uses `Request.QueryString["id"]` to get the ID that's passed to the page.</span></span> <span data-ttu-id="928a3-221">Kod, KIMLIK için bir değerin gerçekten geçirildiğinden emin olmanızı sağlar.</span><span class="sxs-lookup"><span data-stu-id="928a3-221">The code makes sure that a value was actually passed for the ID.</span></span> <span data-ttu-id="928a3-222">Değer geçirilmemişse, kod bir doğrulama hatası ayarlar.</span><span class="sxs-lookup"><span data-stu-id="928a3-222">If no value was passed, the code sets a validation error.</span></span>

<span data-ttu-id="928a3-223">Bu kod, bilgileri doğrulamak için farklı bir yol gösterir.</span><span class="sxs-lookup"><span data-stu-id="928a3-223">This code shows a different way to validate information.</span></span> <span data-ttu-id="928a3-224">Önceki öğreticide `Validation` Yardımcısı ile çalıştık.</span><span class="sxs-lookup"><span data-stu-id="928a3-224">In the previous tutorial, you worked with the `Validation` helper.</span></span> <span data-ttu-id="928a3-225">Doğrulanacak alanları kaydettiniz ve ASP.NET `Html.ValidationMessage` ve `Html.ValidationSummary`kullanarak doğrulama ve görüntülenme hatalarını otomatik olarak yaptınız.</span><span class="sxs-lookup"><span data-stu-id="928a3-225">You registered fields to validate, and ASP.NET automatically did the validation and displayed errors by using `Html.ValidationMessage` and `Html.ValidationSummary`.</span></span> <span data-ttu-id="928a3-226">Ancak, bu durumda Kullanıcı girişini gerçekten doğrulamayız.</span><span class="sxs-lookup"><span data-stu-id="928a3-226">In this case, however, you're not really validating user input.</span></span> <span data-ttu-id="928a3-227">Bunun yerine, başka bir yere sayfaya geçirilmiş bir değeri doğruluyoruz.</span><span class="sxs-lookup"><span data-stu-id="928a3-227">Instead, you're validating a value that was passed to the page from elsewhere.</span></span> <span data-ttu-id="928a3-228">`Validation` Yardımcısı sizin için bunu yapmaz.</span><span class="sxs-lookup"><span data-stu-id="928a3-228">The `Validation` helper doesn't do that for you.</span></span>

<span data-ttu-id="928a3-229">Bu nedenle, değeri `if(!Request.QueryString["ID"].IsEmpty()`) ile test ederek kendiniz kontrol edersiniz.</span><span class="sxs-lookup"><span data-stu-id="928a3-229">Therefore, you check the value yourself, by testing it with `if(!Request.QueryString["ID"].IsEmpty()`).</span></span> <span data-ttu-id="928a3-230">Bir sorun varsa, `Validation` Yardımcısı ile yaptığınız gibi `Html.ValidationSummary`kullanarak hatayı görüntüleyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="928a3-230">If there's a problem, you can display the error by using `Html.ValidationSummary`, as you did with the `Validation` helper.</span></span> <span data-ttu-id="928a3-231">Bunu yapmak için `Validation.AddFormError` çağırır ve bunu görüntülenecek bir ileti iletirsiniz.</span><span class="sxs-lookup"><span data-stu-id="928a3-231">To do that, you call `Validation.AddFormError` and pass it a message to display.</span></span> <span data-ttu-id="928a3-232">`Validation.AddFormError`, zaten bildiğiniz doğrulama sistemiyle ilgili özel iletiler tanımlamanıza olanak sağlayan yerleşik bir yöntemdir.</span><span class="sxs-lookup"><span data-stu-id="928a3-232">`Validation.AddFormError` is a built-in method that lets you define custom messages that tie in with the validation system you're already familiar with.</span></span> <span data-ttu-id="928a3-233">(Bu öğreticide daha sonra bu doğrulama işlemini biraz daha sağlam hale getirmek için konuşacağız.)</span><span class="sxs-lookup"><span data-stu-id="928a3-233">(Later in this tutorial we'll talk about how to make this validation process a little more robust.)</span></span>

<span data-ttu-id="928a3-234">Film için bir KIMLIK olduğundan emin olduktan sonra kod, yalnızca tek bir veritabanı öğesini arayarak veritabanını okur.</span><span class="sxs-lookup"><span data-stu-id="928a3-234">After making sure that there's an ID for the movie, the code reads the database, looking for only a single database item.</span></span> <span data-ttu-id="928a3-235">(Muhtemelen veritabanı işlemleri için genel bir model olduğunu fark etmiş olabilirsiniz: veritabanını açın, bir SQL ifadesini tanımlayın ve ifadesini çalıştırın.) Bu kez, SQL `Select` deyimleri `WHERE ID = @0`içerir.</span><span class="sxs-lookup"><span data-stu-id="928a3-235">(You probably have noticed the general pattern for database operations: open the database, define a SQL statement, and run the statement.) This time, the SQL `Select` statement includes `WHERE ID = @0`.</span></span> <span data-ttu-id="928a3-236">KIMLIK benzersiz olduğundan, yalnızca bir kayıt döndürülebilir.</span><span class="sxs-lookup"><span data-stu-id="928a3-236">Because the ID is unique, only one record can be returned.</span></span>

<span data-ttu-id="928a3-237">Sorgu, film listesi için kullandığınız `db.QuerySingle` (`db.Query`değil) kullanılarak gerçekleştirilir ve kod sonucu `row` değişkenine koyar.</span><span class="sxs-lookup"><span data-stu-id="928a3-237">The query is performed by using `db.QuerySingle` (not `db.Query`, as you used for the movie listing), and the code puts the result into the `row` variable.</span></span> <span data-ttu-id="928a3-238">`row` adı rastgele; değişkenleri istediğiniz herhangi bir şekilde adlandırın.</span><span class="sxs-lookup"><span data-stu-id="928a3-238">The name `row` is arbitrary; you can name the variables anything you like.</span></span> <span data-ttu-id="928a3-239">En üstte başlatılan değişkenler, bu değerlerin metin kutularında görüntülenebilmesi için film ayrıntıları ile doldurulur.</span><span class="sxs-lookup"><span data-stu-id="928a3-239">The variables initialized at the top are then filled with the movie details so that these values can be displayed in the text boxes.</span></span>

## <a name="testing-the-edit-page-so-far"></a><span data-ttu-id="928a3-240">Düzenleme sayfasını test etme (şimdiye kadar)</span><span class="sxs-lookup"><span data-stu-id="928a3-240">Testing the Edit Page (So Far)</span></span>

<span data-ttu-id="928a3-241">Sayfanızı test etmek isterseniz, *filmler* sayfasını hemen çalıştırın ve herhangi bir filmin yanındaki bir **düzenleme** bağlantısına tıklayın.</span><span class="sxs-lookup"><span data-stu-id="928a3-241">If you'd like to test your page, run the *Movies* page now and click an **Edit** link next to any movie.</span></span> <span data-ttu-id="928a3-242">Seçtiğiniz filmle ilgili ayrıntıların bulunduğu *Editmovie* sayfasını görürsünüz:</span><span class="sxs-lookup"><span data-stu-id="928a3-242">You'll see the *EditMovie* page with the details filled in for the movie you selected:</span></span>

![Düzenlenecek filmi gösteren film sayfasını düzenleme](updating-data/_static/image3.png)

<span data-ttu-id="928a3-244">Sayfanın URL 'sinin `?id=10` (veya başka bir sayı) gibi bir şey içerdiğine dikkat edin.</span><span class="sxs-lookup"><span data-stu-id="928a3-244">Notice that the URL of the page includes something like `?id=10` (or some other number).</span></span> <span data-ttu-id="928a3-245">Bu nedenle, *film* sayfasındaki bağlantıları **Düzenle** ' yi test ettiğiniz için, sayfanızın sorgu dizesinden kimliği okumasını ve tek bir film kaydı almak için veritabanı sorgusunun çalıştığını test edersiniz.</span><span class="sxs-lookup"><span data-stu-id="928a3-245">So far you've tested that **Edit** links in the *Movie* page work, that your page is reading the ID from the query string, and that the database query to get a single movie record is working.</span></span>

<span data-ttu-id="928a3-246">Film bilgilerini değiştirebilirsiniz, ancak **Değişiklikleri Gönder**' e tıkladığınızda hiçbir şey olmaz.</span><span class="sxs-lookup"><span data-stu-id="928a3-246">You can change the movie information, but nothing happens when you click **Submit Changes**.</span></span>

## <a name="adding-code-to-update-the-movie-with-the-users-changes"></a><span data-ttu-id="928a3-247">Filmi kullanıcının değişiklikleriyle güncelleştirmek için kod ekleme</span><span class="sxs-lookup"><span data-stu-id="928a3-247">Adding Code to Update the Movie with the User's Changes</span></span>

<span data-ttu-id="928a3-248">*Editmovie. cshtml* dosyasında, ikinci işlevi uygulamak için (değişiklikleri kaydetme), aşağıdaki kodu `@` bloğunun kapatma ayracı içine ekleyin.</span><span class="sxs-lookup"><span data-stu-id="928a3-248">In the *EditMovie.cshtml* file, to implement the second function (saving changes), add the following code just inside the closing brace of the `@` block.</span></span> <span data-ttu-id="928a3-249">(Kodu tam olarak nereye koyduğunuzdan emin değilseniz, Bu öğreticinin sonunda görüntülenen [filmi Düzenle sayfası için tam kod listesine](#Complete_Page_Listing_for_EditMovie) bakabilirsiniz.)</span><span class="sxs-lookup"><span data-stu-id="928a3-249">(If you're not sure exactly where to put the code, you can look at the [complete code listing for the Edit Movie page](#Complete_Page_Listing_for_EditMovie) that appears at the end of this tutorial.)</span></span>

[!code-csharp[Main](updating-data/samples/sample12.cs)]

<span data-ttu-id="928a3-250">Bu biçimlendirme ve kod, *Addmovie*içindeki koda benzer.</span><span class="sxs-lookup"><span data-stu-id="928a3-250">Again, this markup and code is similar to the code in *AddMovie*.</span></span> <span data-ttu-id="928a3-251">Kod `if(IsPost)` bir blokta, bu kod yalnızca Kullanıcı **Değişiklikleri Gönder** düğmesine tıkladığında, (ve yalnızca form nakledildiğinde) &mdash; çalışır.</span><span class="sxs-lookup"><span data-stu-id="928a3-251">The code is in an `if(IsPost)` block, because this code runs only when the user clicks the **Submit Changes** button &mdash; that is, when (and only when) the form has been posted.</span></span> <span data-ttu-id="928a3-252">Bu durumda, `if(IsPost && Validation.IsValid())`gibi bir test kullanmadığınızı — Yani, ve kullanarak her iki testi birleştirmediniz.</span><span class="sxs-lookup"><span data-stu-id="928a3-252">In this case, you're not using a test like `if(IsPost && Validation.IsValid())`— that is, you're not combining both tests by using AND.</span></span> <span data-ttu-id="928a3-253">Bu sayfada, önce bir form gönderimi (`if(IsPost)`) olup olmadığını ve yalnızca doğrulama için alanları kaydetmeyi belirlersiniz.</span><span class="sxs-lookup"><span data-stu-id="928a3-253">In this page, you first determine whether there's a form submission (`if(IsPost)`), and only then register the fields for validation.</span></span> <span data-ttu-id="928a3-254">Daha sonra doğrulama sonuçlarını (`if(Validation.IsValid()`) test edebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="928a3-254">Then you can test the validation results (`if(Validation.IsValid()`).</span></span> <span data-ttu-id="928a3-255">Akış, *Addmovie. cshtml* sayfasından biraz farklıdır, ancak efekt aynıdır.</span><span class="sxs-lookup"><span data-stu-id="928a3-255">The flow is slightly different than in the *AddMovie.cshtml* page, but the effect is the same.</span></span>

<span data-ttu-id="928a3-256">`Request.Form["title"]` ve diğer `<input>` öğeleri için benzer kodu kullanarak metin kutularının değerlerini alırsınız.</span><span class="sxs-lookup"><span data-stu-id="928a3-256">You get the values of the text boxes by using `Request.Form["title"]` and similar code for the other `<input>` elements.</span></span> <span data-ttu-id="928a3-257">Bu zaman, kodun film KIMLIĞINI gizli alandan (`<input type="hidden">`) aldığından emin olun.</span><span class="sxs-lookup"><span data-stu-id="928a3-257">Notice that this time, the code gets the movie ID out of the hidden field (`<input type="hidden">`).</span></span> <span data-ttu-id="928a3-258">Sayfa ilk kez çalıştırıldığında, kod sorgu dizesinden KIMLIĞI aldı.</span><span class="sxs-lookup"><span data-stu-id="928a3-258">When the page ran the first time, the code got the ID out of the query string.</span></span> <span data-ttu-id="928a3-259">Sorgu dizesinin daha sonra bir şekilde değiştirilmiş olması durumunda, başlangıçta görüntülenen filmin KIMLIĞINI aldığınızdan emin olmak için gizli alandan değeri alırsınız.</span><span class="sxs-lookup"><span data-stu-id="928a3-259">You get the value from the hidden field to make sure that you're getting the ID of the movie that was originally displayed, in case the query string was somehow altered since then.</span></span>

<span data-ttu-id="928a3-260">*Addfilm* kodu ve bu kod arasındaki gerçek önemli fark, bu kodda `Insert Into` DEYIMIN yerine SQL `Update` ifadesini kullanmanızdır.</span><span class="sxs-lookup"><span data-stu-id="928a3-260">The really important difference between the *AddMovie* code and this code is that in this code you use the SQL `Update` statement instead of the `Insert Into` statement.</span></span> <span data-ttu-id="928a3-261">Aşağıdaki örnekte, SQL `Update` ifadesinin sözdizimi gösterilmektedir:</span><span class="sxs-lookup"><span data-stu-id="928a3-261">The following example shows the syntax of the SQL `Update` statement:</span></span>

`UPDATE table SET col1="value", col2="value", col3="value" ... WHERE ID = value`

<span data-ttu-id="928a3-262">Herhangi bir sırada sütun belirtebilir ve bir `Update` işlemi sırasında her sütunu güncelleştirmeniz gerekmez.</span><span class="sxs-lookup"><span data-stu-id="928a3-262">You can specify any columns in any order, and you don't necessarily have to update every column during an `Update` operation.</span></span> <span data-ttu-id="928a3-263">(KIMLIĞI güncellenemez, çünkü bu, kaydı yeni bir kayıt olarak kaydeder ve bir `Update` işlemi için izin verilmez.)</span><span class="sxs-lookup"><span data-stu-id="928a3-263">(You cannot update the ID itself, because that would in effect save the record as a new record, and that's not allowed for an `Update` operation.)</span></span>

> [!NOTE] 
> 
> <span data-ttu-id="928a3-264">**Önemli** KIMLIĞE sahip `Where` yan tümcesi, veritabanının hangi veritabanı kaydını güncelleştirmek istediğinizi bildiği için çok önemlidir.</span><span class="sxs-lookup"><span data-stu-id="928a3-264">**Important** The `Where` clause with the ID is very important, because that's how the database knows which database record you want to update.</span></span> <span data-ttu-id="928a3-265">`Where` yan tümcesini kapatırsanız, veritabanı veritabanındaki *her* kaydı güncelleştirir.</span><span class="sxs-lookup"><span data-stu-id="928a3-265">If you left off the `Where` clause, the database would update *every* record in the database.</span></span> <span data-ttu-id="928a3-266">Çoğu durumda, bu bir olağanüstü durum olacaktır.</span><span class="sxs-lookup"><span data-stu-id="928a3-266">In most cases, that would be a disaster.</span></span>

<span data-ttu-id="928a3-267">Kodda, güncelleştirilecek değerler yer tutucuları kullanılarak SQL ifadesine geçirilir.</span><span class="sxs-lookup"><span data-stu-id="928a3-267">In the code, the values to update are passed to the SQL statement by using placeholders.</span></span> <span data-ttu-id="928a3-268">Daha önce söylediğimiz öğeleri yinelemek için: güvenlik nedenleriyle, *yalnızca* bir SQL ifadesine değer geçirmek için yer tutucuları kullanın.</span><span class="sxs-lookup"><span data-stu-id="928a3-268">To repeat what we've said before: for security reasons, *only* use placeholders to pass values to a SQL statement.</span></span>

<span data-ttu-id="928a3-269">Kod, `Update` ifadesini çalıştırmak için `db.Execute` kullandıktan sonra, değişiklikleri görebileceğiniz şekilde liste sayfasına yeniden yönlendirilir.</span><span class="sxs-lookup"><span data-stu-id="928a3-269">After the code uses `db.Execute` to run the `Update` statement, it redirects back to the listing page, where you can see the changes.</span></span>

> [!TIP] 
> 
> <span data-ttu-id="928a3-270">**Farklı SQL deyimleri, farklı Yöntemler**</span><span class="sxs-lookup"><span data-stu-id="928a3-270">**Different SQL Statements, Different Methods**</span></span>
> 
> <span data-ttu-id="928a3-271">Farklı SQL deyimlerini çalıştırmak için biraz farklı yöntemler kullanacağınızı fark etmiş olabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="928a3-271">You might have noticed that you use slightly different methods to run different SQL statements.</span></span> <span data-ttu-id="928a3-272">Birden çok kayıt döndüren `Select` bir sorgu çalıştırmak için `Query` yöntemini kullanırsınız.</span><span class="sxs-lookup"><span data-stu-id="928a3-272">To run a `Select` query that potentially returns multiple records, you use the `Query` method.</span></span> <span data-ttu-id="928a3-273">Yalnızca bir veritabanı öğesi döndüreceğimizi bildiğiniz bir `Select` sorgusu çalıştırmak için `QuerySingle` yöntemini kullanırsınız.</span><span class="sxs-lookup"><span data-stu-id="928a3-273">To run a `Select` query that you know will return only one database item, you use the `QuerySingle` method.</span></span> <span data-ttu-id="928a3-274">Değişiklik yapan ancak veritabanı öğeleri döndürmeyen komutları çalıştırmak için `Execute` yöntemini kullanırsınız.</span><span class="sxs-lookup"><span data-stu-id="928a3-274">To run commands that make changes but that don't return database items, you use the `Execute` method.</span></span>
> 
> <span data-ttu-id="928a3-275">`Query` ve `QuerySingle`arasında zaten gördüğünüz gibi, her biri farklı sonuçlar döndürdüğünden, farklı yöntemlere sahip olmanız gerekir.</span><span class="sxs-lookup"><span data-stu-id="928a3-275">You have to have different methods because each of them returns different results, as you saw already in the difference between `Query` and `QuerySingle`.</span></span> <span data-ttu-id="928a3-276">(`Execute` yöntemi gerçekten &mdash; de bir değer döndürür &mdash; Komutun etkilediği veritabanı satırı sayısı, ancak şu ana kadar yoksayıyorsunuz.)</span><span class="sxs-lookup"><span data-stu-id="928a3-276">(The `Execute` method actually returns a value also &mdash; namely, the number of database rows that were affected by the command &mdash; but you've been ignoring that so far.)</span></span>
> 
> <span data-ttu-id="928a3-277">Kuşkusuz `Query` yöntemi yalnızca bir veritabanı satırı döndürebilir.</span><span class="sxs-lookup"><span data-stu-id="928a3-277">Of course, the `Query` method might return only one database row.</span></span> <span data-ttu-id="928a3-278">Ancak, ASP.NET `Query` yönteminin sonuçlarını her zaman bir koleksiyon olarak değerlendirir.</span><span class="sxs-lookup"><span data-stu-id="928a3-278">However, ASP.NET always treats the results of the `Query` method as a collection.</span></span> <span data-ttu-id="928a3-279">Yöntem yalnızca bir satır döndürürse bile, bu tek satırı koleksiyondan ayıklamanız gerekir.</span><span class="sxs-lookup"><span data-stu-id="928a3-279">Even if the method returns just one row, you have to extract that single row from the collection.</span></span> <span data-ttu-id="928a3-280">Bu nedenle, yalnızca bir satırın geri alınacağını *bildiğiniz* durumlarda `QuerySingle`kullanımı biraz daha uygundur.</span><span class="sxs-lookup"><span data-stu-id="928a3-280">Therefore, in situations where you *know* you'll get back only one row, it's a bit more convenient to use `QuerySingle`.</span></span>
> 
> <span data-ttu-id="928a3-281">Belirli veritabanı işlemleri türlerini gerçekleştiren birkaç yöntem vardır.</span><span class="sxs-lookup"><span data-stu-id="928a3-281">There are a few other methods that perform specific types of database operations.</span></span> <span data-ttu-id="928a3-282">Veritabanı yöntemlerinin bir listesini [ASP.NET Web Pages API hızlı başvurusu](../../api-reference/asp-net-web-pages-api-reference.md#Data)' nda bulabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="928a3-282">You can find a listing of database methods in the [ASP.NET Web Pages API Quick Reference](../../api-reference/asp-net-web-pages-api-reference.md#Data).</span></span>

## <a name="making-validation-for-the-id-more-robust"></a><span data-ttu-id="928a3-283">KIMLIK doğrulaması daha sağlam yapılıyor</span><span class="sxs-lookup"><span data-stu-id="928a3-283">Making Validation for the ID More Robust</span></span>

<span data-ttu-id="928a3-284">Sayfa ilk kez çalıştığında, filmi veritabanından almak için sorgu dizesinden film KIMLIĞINI alırsınız.</span><span class="sxs-lookup"><span data-stu-id="928a3-284">The first time that the page runs, you get the movie ID from the query string so that you can go get that movie from the database.</span></span> <span data-ttu-id="928a3-285">Bu kodu kullanarak yaptığınız, gerçekte gidilecek bir değer olduğundan emin olursunuz:</span><span class="sxs-lookup"><span data-stu-id="928a3-285">You made sure that there actually was a value to go look for, which you did by using this code:</span></span>

[!code-csharp[Main](updating-data/samples/sample13.cs)]

<span data-ttu-id="928a3-286">Bu kodu, bir Kullanıcı *film sayfasında bir* filmi seçmeden *editfilmlerini* sayfasına aldığından emin olmak için kullandınız, sayfada Kullanıcı dostu bir hata iletisi görüntülenir.</span><span class="sxs-lookup"><span data-stu-id="928a3-286">You used this code to make sure that if a user gets to the *EditMovies* page without first selecting a movie in the *Movies* page, the page would display a user-friendly error message.</span></span> <span data-ttu-id="928a3-287">(Aksi takdirde, kullanıcılar büyük olasılıkla yalnızca onları şaşırtabileceği bir hata görür.)</span><span class="sxs-lookup"><span data-stu-id="928a3-287">(Otherwise, users would see an error that would probably just confuse them.)</span></span>

<span data-ttu-id="928a3-288">Ancak, bu doğrulama çok sağlam değildir.</span><span class="sxs-lookup"><span data-stu-id="928a3-288">However, this validation isn't very robust.</span></span> <span data-ttu-id="928a3-289">Sayfa şu hatalarla de çağrılabilir:</span><span class="sxs-lookup"><span data-stu-id="928a3-289">The page might also be invoked with these errors:</span></span>

- <span data-ttu-id="928a3-290">KIMLIK bir sayı değil.</span><span class="sxs-lookup"><span data-stu-id="928a3-290">The ID isn't a number.</span></span> <span data-ttu-id="928a3-291">Örneğin, sayfa `http://localhost:nnnnn/EditMovie?id=abc`gibi bir URL ile çağrılabilir.</span><span class="sxs-lookup"><span data-stu-id="928a3-291">For example, the page could be invoked with a URL like `http://localhost:nnnnn/EditMovie?id=abc`.</span></span>
- <span data-ttu-id="928a3-292">KIMLIK bir sayıdır, ancak mevcut olmayan bir filme (örneğin, `http://localhost:nnnnn/EditMovie?id=100934`) başvurur.</span><span class="sxs-lookup"><span data-stu-id="928a3-292">The ID is a number, but it references a movie that doesn't exist (for example, `http://localhost:nnnnn/EditMovie?id=100934`).</span></span>

<span data-ttu-id="928a3-293">Bu URL 'lerden kaynaklanan hataları görmeyi merak ediyorsanız, *filmler* sayfasını çalıştırın.</span><span class="sxs-lookup"><span data-stu-id="928a3-293">If you're curious to see the errors that result from these URLs, run the *Movies* page.</span></span> <span data-ttu-id="928a3-294">Düzenlenecek filmi seçin ve ardından *Editmovie* sayfasının URL 'sini ALFABETIK bir kimlik veya mevcut olmayan BIR filmin kimliği IÇEREN bir URL 'ye değiştirin.</span><span class="sxs-lookup"><span data-stu-id="928a3-294">Select a movie to edit, and then change the URL of the *EditMovie* page to a URL that contains an alphabetic ID or the ID of a non-existent movie.</span></span>

<span data-ttu-id="928a3-295">Ne yapmalısınız?</span><span class="sxs-lookup"><span data-stu-id="928a3-295">So what should you do?</span></span> <span data-ttu-id="928a3-296">İlk düzeltmeler yalnızca sayfaya geçirilen bir KIMLIK olmadığından ve KIMLIğIN bir tamsayı olduğundan emin olmak içindir.</span><span class="sxs-lookup"><span data-stu-id="928a3-296">The first fix is to make sure that not only is an ID passed to the page, but that the ID is an integer.</span></span> <span data-ttu-id="928a3-297">`!IsPost` testin kodunu Şu örneğe benzer şekilde değiştirin:</span><span class="sxs-lookup"><span data-stu-id="928a3-297">Change the code for the `!IsPost` test to look like this example:</span></span>

[!code-csharp[Main](updating-data/samples/sample14.cs)]

<span data-ttu-id="928a3-298">`IsEmpty` testine ikinci bir koşul eklediniz, `&&` (mantıksal AND) ile bağlantılandırdınız:</span><span class="sxs-lookup"><span data-stu-id="928a3-298">You've added a second condition to the `IsEmpty` test, linked with `&&` (logical AND):</span></span>

[!code-csharp[Main](updating-data/samples/sample15.cs)]

<span data-ttu-id="928a3-299">[ASP.NET Web sayfaları programlama](../introducing-razor-syntax-c.md) öğreticisine giriş `AsBool`, bir karakter dizesini başka bir veri türüne dönüştürdüğünü `AsInt` gibi yöntemler bulabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="928a3-299">You might remember from the [Introduction to ASP.NET Web Pages Programming](../introducing-razor-syntax-c.md) tutorial that methods like `AsBool` an `AsInt` convert a character string to some other data type.</span></span> <span data-ttu-id="928a3-300">`IsInt` yöntemi (ve `IsBool` ve `IsDateTime`gibi diğerleri) benzerdir.</span><span class="sxs-lookup"><span data-stu-id="928a3-300">The `IsInt` method (and others, like `IsBool` and `IsDateTime`) are similar.</span></span> <span data-ttu-id="928a3-301">Ancak, yalnızca dönüştürme işlemini gerçekleştirmeksizin *dizeyi dönüştürüp* dönüştürmeyeceğinizi test ederler.</span><span class="sxs-lookup"><span data-stu-id="928a3-301">However, they test only whether you *can* convert the string, without actually performing the conversion.</span></span> <span data-ttu-id="928a3-302">Bu nedenle *, aslında sorgu dizesi değerinin bir tamsayıya dönüştürüledildiğini*söylüyorsunuz....</span><span class="sxs-lookup"><span data-stu-id="928a3-302">So here you're essentially saying *If the query string value can be converted to an integer ...*.</span></span>

<span data-ttu-id="928a3-303">Diğer olası sorun, mevcut olmayan bir filmi arıyor.</span><span class="sxs-lookup"><span data-stu-id="928a3-303">The other potential problem is looking for a movie that doesn't exist.</span></span> <span data-ttu-id="928a3-304">Filmi alacağınız kod şu kod gibi görünür:</span><span class="sxs-lookup"><span data-stu-id="928a3-304">The code to get a movie looks like this code:</span></span>

[!code-csharp[Main](updating-data/samples/sample16.cs)]

<span data-ttu-id="928a3-305">Gerçek bir filme karşılık gelen `QuerySingle` yöntemine bir `movieId` değeri geçirirseniz, hiçbir şey döndürülmez ve izleyen deyimler (örneğin, `title=row.Title`) hatalara neden olur.</span><span class="sxs-lookup"><span data-stu-id="928a3-305">If you pass a `movieId` value to the `QuerySingle` method that doesn't correspond to an actual movie, nothing is returned and the statements that follow (for example, `title=row.Title`) result in errors.</span></span>

<span data-ttu-id="928a3-306">Daha kolay bir çözüm vardır.</span><span class="sxs-lookup"><span data-stu-id="928a3-306">Again there's an easy fix.</span></span> <span data-ttu-id="928a3-307">`db.QuerySingle` yöntemi sonuç döndürürse, `row` değişkeni null olur.</span><span class="sxs-lookup"><span data-stu-id="928a3-307">If the `db.QuerySingle` method returns no results, the `row` variable will be null.</span></span> <span data-ttu-id="928a3-308">Bu nedenle, bundan sonra değer almayı denemeden önce `row` değişkeninin null olup olmadığını kontrol edebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="928a3-308">So you can check whether the `row` variable is null before you try to get values from it.</span></span> <span data-ttu-id="928a3-309">Aşağıdaki kod, `row` nesnesinden değerleri alan deyimler etrafında bir `if` bloğu ekler:</span><span class="sxs-lookup"><span data-stu-id="928a3-309">The following code adds an `if` block around the statements that get the values out of the `row` object:</span></span>

[!code-csharp[Main](updating-data/samples/sample17.cs)]

<span data-ttu-id="928a3-310">Bu iki ek doğrulama testi ile, sayfa daha fazla madde işareti-kanıt haline gelir.</span><span class="sxs-lookup"><span data-stu-id="928a3-310">With these two additional validation tests, the page becomes more bullet-proof.</span></span> <span data-ttu-id="928a3-311">`!IsPost` dalı için kodun tamamı şu örneğe benzer şekilde görünür:</span><span class="sxs-lookup"><span data-stu-id="928a3-311">The complete code for the `!IsPost` branch now looks like this example:</span></span>

[!code-csharp[Main](updating-data/samples/sample18.cs)]

<span data-ttu-id="928a3-312">Bu görevin bir `else` bloğu için iyi bir şekilde kullanılması hakkında daha fazla bilgi edineceksiniz.</span><span class="sxs-lookup"><span data-stu-id="928a3-312">We'll note once more that this task is a good use for an `else` block.</span></span> <span data-ttu-id="928a3-313">Testler geçmezse `else` blokları hata iletilerini ayarlar.</span><span class="sxs-lookup"><span data-stu-id="928a3-313">If the tests don't pass, the `else` blocks set error messages.</span></span>

## <a name="adding-a-link-to-return-to-the-movies-page"></a><span data-ttu-id="928a3-314">Filmler sayfasına dönmek için bağlantı ekleme</span><span class="sxs-lookup"><span data-stu-id="928a3-314">Adding a Link to Return to the Movies Page</span></span>

<span data-ttu-id="928a3-315">Son ve yararlı bir ayrıntı, *filmler* sayfasına geri bir bağlantı eklemektir.</span><span class="sxs-lookup"><span data-stu-id="928a3-315">A final and helpful detail is to add a link back to the *Movies* page.</span></span> <span data-ttu-id="928a3-316">Normal olay akışında, kullanıcılar *filmler* sayfasından başlayacaktır ve bir **düzenleme** bağlantısına tıklacaktır.</span><span class="sxs-lookup"><span data-stu-id="928a3-316">In the ordinary flow of events, users will start at the *Movies* page and click an **Edit** link.</span></span> <span data-ttu-id="928a3-317">Bu, bunları, filmi düzenleyebilecekleri ve düğmesine tıklatabilecekleri *Editmovie* sayfasına getirir.</span><span class="sxs-lookup"><span data-stu-id="928a3-317">That brings them to the *EditMovie* page, where they can edit the movie and click the button.</span></span> <span data-ttu-id="928a3-318">Kod değişikliği tamamladıktan sonra, *filmler* sayfasına yeniden yönlendirilir.</span><span class="sxs-lookup"><span data-stu-id="928a3-318">After the code processes the change, it redirects back to the *Movies* page.</span></span>

<span data-ttu-id="928a3-319">Ancak</span><span class="sxs-lookup"><span data-stu-id="928a3-319">However:</span></span>

- <span data-ttu-id="928a3-320">Kullanıcı herhangi bir şeyi değiştirmemeye karar verebilir.</span><span class="sxs-lookup"><span data-stu-id="928a3-320">The user might decide not to change anything.</span></span>
- <span data-ttu-id="928a3-321">Kullanıcı, önce *filmler* sayfasındaki bir **düzenleme** bağlantısına tıklamadan bu sayfayı alabilir.</span><span class="sxs-lookup"><span data-stu-id="928a3-321">The user might have gotten to this page without first clicking an **Edit** link in the *Movies* page.</span></span>

<span data-ttu-id="928a3-322">Her iki durumda da, ana listeye dönmesini kolaylaştırmak istersiniz.</span><span class="sxs-lookup"><span data-stu-id="928a3-322">Either way, you want to make it easy for them to return to the main listing.</span></span> <span data-ttu-id="928a3-323">Bu kolay bir düzeldir &mdash; biçimlendirme içindeki kapanış `</form>` etiketinden hemen sonra aşağıdaki biçimlendirmeyi ekleyin:</span><span class="sxs-lookup"><span data-stu-id="928a3-323">It's an easy fix &mdash; add the following markup just after the closing `</form>` tag in the markup:</span></span>

[!code-html[Main](updating-data/samples/sample19.html)]

<span data-ttu-id="928a3-324">Bu biçimlendirme, başka bir yerde gördüğünüz `<a>` öğesi için aynı sözdizimini kullanır.</span><span class="sxs-lookup"><span data-stu-id="928a3-324">This markup uses the same syntax for an `<a>` element that you've seen elsewhere.</span></span> <span data-ttu-id="928a3-325">URL, "Web sitesinin kökünü `~`" içerir.</span><span class="sxs-lookup"><span data-stu-id="928a3-325">The URL includes `~` to mean "root of the website."</span></span>

## <a name="testing-the-movie-update-process"></a><span data-ttu-id="928a3-326">Film güncelleştirme Işlemini test etme</span><span class="sxs-lookup"><span data-stu-id="928a3-326">Testing the Movie Update Process</span></span>

<span data-ttu-id="928a3-327">Artık test edebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="928a3-327">Now you can test.</span></span> <span data-ttu-id="928a3-328">*Filmler* sayfasını çalıştırın ve filmin yanındaki **Düzenle** ' ye tıklayın.</span><span class="sxs-lookup"><span data-stu-id="928a3-328">Run the *Movies* page, and click **Edit** next to a movie.</span></span> <span data-ttu-id="928a3-329">*Editmovie* sayfası göründüğünde, film üzerinde değişiklikler yapın ve **Değişiklikleri Gönder**' e tıklayın.</span><span class="sxs-lookup"><span data-stu-id="928a3-329">When the *EditMovie* page appears, make changes to the movie and click **Submit Changes**.</span></span> <span data-ttu-id="928a3-330">Film listesi göründüğünde değişikliklerinizin gösterildiğinden emin olun.</span><span class="sxs-lookup"><span data-stu-id="928a3-330">When the movie listing appears, make sure that your changes are shown.</span></span>

<span data-ttu-id="928a3-331">Doğrulamanın çalıştığından emin olmak için, başka bir film için **Düzenle** ' ye tıklayın.</span><span class="sxs-lookup"><span data-stu-id="928a3-331">To make sure that validation is working, click **Edit** for another movie.</span></span> <span data-ttu-id="928a3-332">*Editmovie* sayfasına geldiğinizde, **tarz** alanını (veya **yıl** alanını veya her ikisini birden) temizleyin ve değişikliklerinizi göndermeyi deneyin.</span><span class="sxs-lookup"><span data-stu-id="928a3-332">When you get to the *EditMovie* page, clear the **Genre** field (or **Year** field, or both) and try to submit your changes.</span></span> <span data-ttu-id="928a3-333">Beklenen bir hata görürsünüz:</span><span class="sxs-lookup"><span data-stu-id="928a3-333">You'll see an error, as you'd expect:</span></span>

![Doğrulama hatalarını gösteren film sayfasını Düzenle](updating-data/_static/image4.png)

<span data-ttu-id="928a3-335">Değişikliklerinizi iptal etmek ve *filmler* sayfasına dönmek için **film dökümüne dön** bağlantısına tıklayın.</span><span class="sxs-lookup"><span data-stu-id="928a3-335">Click the **Return to movie listing** link to abandon your changes and return to the *Movies* page.</span></span>

## <a name="coming-up-next"></a><span data-ttu-id="928a3-336">Sonraki adımda</span><span class="sxs-lookup"><span data-stu-id="928a3-336">Coming Up Next</span></span>

<span data-ttu-id="928a3-337">Bir sonraki öğreticide, bir film kaydını silme hakkında bilgi edineceksiniz.</span><span class="sxs-lookup"><span data-stu-id="928a3-337">In the next tutorial, you'll see how to delete a movie record.</span></span>

## <a name="complete-listing-for-movie-page-updated-with-edit-links"></a><span data-ttu-id="928a3-338">Film sayfası için komple liste (düzenleme bağlantılarıyla güncelleştirildi)</span><span class="sxs-lookup"><span data-stu-id="928a3-338">Complete Listing for Movie Page (Updated with Edit Links)</span></span>

[!code-cshtml[Main](updating-data/samples/sample20.cshtml)]

<a id="Complete_Page_Listing_for_EditMovie"></a>
## <a name="complete-page-listing-for-edit-movie-page"></a><span data-ttu-id="928a3-339">Film düzenleme sayfasının sayfa listesini tamamlar</span><span class="sxs-lookup"><span data-stu-id="928a3-339">Complete Page Listing for Edit Movie Page</span></span>

[!code-cshtml[Main](updating-data/samples/sample21.cshtml)]

## <a name="additional-resources"></a><span data-ttu-id="928a3-340">Ek Kaynaklar</span><span class="sxs-lookup"><span data-stu-id="928a3-340">Additional Resources</span></span>

- [<span data-ttu-id="928a3-341">Razor söz dizimini kullanarak ASP.NET Web programlamaya giriş</span><span class="sxs-lookup"><span data-stu-id="928a3-341">Introduction to ASP.NET Web Programming by Using the Razor Syntax</span></span>](../../getting-started/introducing-razor-syntax-c.md)
- <span data-ttu-id="928a3-342">W3Schools sitesinde [SQL Update bildirisi](http://www.w3schools.com/sql/sql_update.asp)</span><span class="sxs-lookup"><span data-stu-id="928a3-342">[SQL UPDATE Statement](http://www.w3schools.com/sql/sql_update.asp) on the W3Schools site</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="928a3-343">[Önceki](entering-data.md)
> [İleri](deleting-data.md)</span><span class="sxs-lookup"><span data-stu-id="928a3-343">[Previous](entering-data.md)
[Next](deleting-data.md)</span></span>
