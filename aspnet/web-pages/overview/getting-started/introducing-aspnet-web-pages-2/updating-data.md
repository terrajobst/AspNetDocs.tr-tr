---
uid: web-pages/overview/getting-started/introducing-aspnet-web-pages-2/updating-data
title: Veritabanı verilerini güncelleştirme - ASP.NET Web sayfaları ile tanışın | Microsoft Docs
author: Rick-Anderson
description: Bu öğreticide, ASP.NET Web sayfaları (Razor) kullandığınızda (değiştirin) var olan bir veritabanını girişini güncelleştirmek gösterilmektedir. Bu seriyi bitirdiğinizi th...
ms.author: riande
ms.date: 01/02/2018
ms.assetid: ac86ec9c-6b69-485b-b9e0-8b9127b13e6b
msc.legacyurl: /web-pages/overview/getting-started/introducing-aspnet-web-pages-2/updating-data
msc.type: authoredcontent
ms.openlocfilehash: 4542ad3ac3e321629bb4de3cd4df12c22ff6cb20
ms.sourcegitcommit: 0f1119340e4464720cfd16d0ff15764746ea1fea
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 04/17/2019
ms.locfileid: "59414627"
---
# <a name="introducing-aspnet-web-pages---updating-database-data"></a><span data-ttu-id="f600f-104">ASP.NET Web sayfalarına giriş - veritabanı verilerini güncelleştirme</span><span class="sxs-lookup"><span data-stu-id="f600f-104">Introducing ASP.NET Web Pages - Updating Database Data</span></span>

<span data-ttu-id="f600f-105">tarafından [Tom FitzMacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="f600f-105">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="f600f-106">Bu öğreticide, ASP.NET Web sayfaları (Razor) kullandığınızda (değiştirin) var olan bir veritabanını girişini güncelleştirmek gösterilmektedir.</span><span class="sxs-lookup"><span data-stu-id="f600f-106">This tutorial shows you how to update (change) an existing database entry when you use ASP.NET Web Pages (Razor).</span></span> <span data-ttu-id="f600f-107">Bu seriyi aracılığıyla bitirdiğinizi [veri girişi tarafından Forms kullanarak ASP.NET Web sayfalarını kullanma](entering-data.md).</span><span class="sxs-lookup"><span data-stu-id="f600f-107">It assumes you have completed the series through [Entering Data by Using Forms Using ASP.NET Web Pages](entering-data.md).</span></span>
> 
> <span data-ttu-id="f600f-108">Öğrenecekleriniz:</span><span class="sxs-lookup"><span data-stu-id="f600f-108">What you'll learn:</span></span>
> 
> - <span data-ttu-id="f600f-109">Tek bir kaydı seçmek nasıl `WebGrid` Yardımcısı.</span><span class="sxs-lookup"><span data-stu-id="f600f-109">How to select an individual record in the `WebGrid` helper.</span></span>
> - <span data-ttu-id="f600f-110">Tek kayıtlı bir veritabanından okumak nasıl.</span><span class="sxs-lookup"><span data-stu-id="f600f-110">How to read a single record from a database.</span></span>
> - <span data-ttu-id="f600f-111">Veritabanı kaydı değerlerle form dağıtılacak yapma.</span><span class="sxs-lookup"><span data-stu-id="f600f-111">How to preload a form with values from the database record.</span></span>
> - <span data-ttu-id="f600f-112">Bir veritabanında varolan bir kaydı güncelleştirme yapma.</span><span class="sxs-lookup"><span data-stu-id="f600f-112">How to update an existing record in a database.</span></span>
> - <span data-ttu-id="f600f-113">Bilgi görüntülemeden sayfasında depolamayı öğrenin.</span><span class="sxs-lookup"><span data-stu-id="f600f-113">How to store information in the page without displaying it.</span></span>
> - <span data-ttu-id="f600f-114">Gizli bir alan bilgilerini depolamak için nasıl kullanılacağını.</span><span class="sxs-lookup"><span data-stu-id="f600f-114">How to use a hidden field to store information.</span></span>
>   
> 
> <span data-ttu-id="f600f-115">Ele alınan özelliklerin/teknolojiler:</span><span class="sxs-lookup"><span data-stu-id="f600f-115">Features/technologies discussed:</span></span>
> 
> - <span data-ttu-id="f600f-116">`WebGrid` Yardımcısı.</span><span class="sxs-lookup"><span data-stu-id="f600f-116">The `WebGrid` helper.</span></span>
> - <span data-ttu-id="f600f-117">SQL `Update` komutu.</span><span class="sxs-lookup"><span data-stu-id="f600f-117">The SQL `Update` command.</span></span>
> - <span data-ttu-id="f600f-118">`Database.Execute` Yöntemi.</span><span class="sxs-lookup"><span data-stu-id="f600f-118">The `Database.Execute` method.</span></span>
> - <span data-ttu-id="f600f-119">Gizli alanları (`<input type="hidden">`).</span><span class="sxs-lookup"><span data-stu-id="f600f-119">Hidden fields (`<input type="hidden">`).</span></span>


## <a name="what-youll-build"></a><span data-ttu-id="f600f-120">Ne oluşturacaksınız</span><span class="sxs-lookup"><span data-stu-id="f600f-120">What You'll Build</span></span>

<span data-ttu-id="f600f-121">Önceki öğreticide, bir veritabanı için bir kayıt eklemek öğrendiniz.</span><span class="sxs-lookup"><span data-stu-id="f600f-121">In the previous tutorial, you learned how to add a record to a database.</span></span> <span data-ttu-id="f600f-122">Burada, bir kaydı düzenlemek için görüntülemek üzere nasıl öğreneceksiniz.</span><span class="sxs-lookup"><span data-stu-id="f600f-122">Here, you'll learn how to display a record for editing.</span></span> <span data-ttu-id="f600f-123">İçinde *filmler* sayfasında, güncelleştirme `WebGrid` yardımcı olan görünmesi bir **Düzenle** her filmin yanındaki bağlantısını:</span><span class="sxs-lookup"><span data-stu-id="f600f-123">In the *Movies* page, you'll update the `WebGrid` helper so that it displays an **Edit** link next to each movie:</span></span>

![WebGrid görüntülemek için her filmin 'Düzenle' bağlantısının dahil olmak üzere](updating-data/_static/image1.png)

<span data-ttu-id="f600f-125">Tıkladığınızda **Düzenle** bağlantı sürer, başka bir sayfaya film bilgileri zaten bir biçimde olduğu:</span><span class="sxs-lookup"><span data-stu-id="f600f-125">When you click the **Edit** link, it takes you to a different page, where the movie information is already in a form:</span></span>

![Düzenlenecek film gösteren film sayfayı Düzenle](updating-data/_static/image2.png)

<span data-ttu-id="f600f-127">Değerlerden herhangi birini değiştirebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="f600f-127">You can change any of the values.</span></span> <span data-ttu-id="f600f-128">Değişiklikleri gönderme, kod sayfasında veritabanını güncelleştirir ve film listeye geri alır.</span><span class="sxs-lookup"><span data-stu-id="f600f-128">When you submit the changes, the code in the page updates the database and takes you back to the movie listing.</span></span>

<span data-ttu-id="f600f-129">İşlemin bu kısmında çalışır gibi neredeyse *AddMovie.cshtml* Bu öğreticinin daha tanıdık gelecektir şekilde önceki öğreticide oluşturduğunuz sayfası.</span><span class="sxs-lookup"><span data-stu-id="f600f-129">This part of the process works almost exactly like the *AddMovie.cshtml* page you created in the previous tutorial, so much of this tutorial will be familiar.</span></span>

<span data-ttu-id="f600f-130">Tek bir film düzenlemek için bir yol uygulayabileceğine birkaç yolu vardır.</span><span class="sxs-lookup"><span data-stu-id="f600f-130">There are several ways you could implement a way to edit an individual movie.</span></span> <span data-ttu-id="f600f-131">Uygulanması kolaydır ve kolay anlaşılır olduğundan gösterilen yaklaşım seçilmiştir.</span><span class="sxs-lookup"><span data-stu-id="f600f-131">The approach shown was chosen because it's easy to implement and easy to understand.</span></span>

## <a name="adding-an-edit-link-to-the-movie-listing"></a><span data-ttu-id="f600f-132">Düzenle bağlantısının film listeye ekleme</span><span class="sxs-lookup"><span data-stu-id="f600f-132">Adding an Edit Link to the Movie Listing</span></span>

<span data-ttu-id="f600f-133">Başlamak için güncelleştireceksiniz *filmler* de listeleme her filmin içeren sayfa bir **Düzenle** bağlantı.</span><span class="sxs-lookup"><span data-stu-id="f600f-133">To begin, you'll update the *Movies* page so that each movie listing also contains an **Edit** link.</span></span>

<span data-ttu-id="f600f-134">Açık *Movies.cshtml* dosya.</span><span class="sxs-lookup"><span data-stu-id="f600f-134">Open the *Movies.cshtml* file.</span></span>

<span data-ttu-id="f600f-135">Sayfanın gövdesi içinde değiştirmek `WebGrid` sütunu ekleyerek biçimlendirme.</span><span class="sxs-lookup"><span data-stu-id="f600f-135">In the body of the page, change the `WebGrid` markup by adding a column.</span></span> <span data-ttu-id="f600f-136">Değiştirilen biçimlendirmesi şöyledir:</span><span class="sxs-lookup"><span data-stu-id="f600f-136">Here's the modified markup:</span></span>

[!code-html[Main](updating-data/samples/sample1.html?highlight=6)]

<span data-ttu-id="f600f-137">Bu yeni bir sütun verilmiştir:</span><span class="sxs-lookup"><span data-stu-id="f600f-137">The new column is this one:</span></span>

[!code-html[Main](updating-data/samples/sample2.html)]

<span data-ttu-id="f600f-138">Bir bağlantı noktası bu sütunun göstermektir (`<a>` öğesi) metni "Düzenle" diyor.</span><span class="sxs-lookup"><span data-stu-id="f600f-138">The point of this column is to show a link (`<a>` element) whose text says "Edit".</span></span> <span data-ttu-id="f600f-139">Sonra duyuyoruz olduğundan sayfa çalıştığında, aşağıdaki gibi görünen bir bağlantı oluşturmak için ile `id` her filmin için farklı bir değer:</span><span class="sxs-lookup"><span data-stu-id="f600f-139">What we're after is to create a link that looks like the following when the page runs, with the `id` value different for each movie:</span></span>

[!code-css[Main](updating-data/samples/sample3.css)]

<span data-ttu-id="f600f-140">Bu bağlantıyı adlı sayfanın çağıracağı *EditMovie*, ve sorgu dizesi geçer `?id=7` o sayfaya.</span><span class="sxs-lookup"><span data-stu-id="f600f-140">This link will invoke a page named *EditMovie*, and it will pass the query string `?id=7` to that page.</span></span>

<span data-ttu-id="f600f-141">Yeni bir sütun için söz dizimi biraz karmaşık görünebilir, ancak yalnızca birkaç öğeleri birlikte koyar olmasıdır.</span><span class="sxs-lookup"><span data-stu-id="f600f-141">The syntax for the new column might look a bit complex, but that's only because it puts together several elements.</span></span> <span data-ttu-id="f600f-142">Her bağımsız öğede oldukça basittir.</span><span class="sxs-lookup"><span data-stu-id="f600f-142">Each individual element is straightforward.</span></span> <span data-ttu-id="f600f-143">Hakkında yoğunlaşabilirsiniz, yalnızca `<a>` öğesi, bu işaretleme görürsünüz:</span><span class="sxs-lookup"><span data-stu-id="f600f-143">If you concentrate on just the `<a>` element, you see this markup:</span></span>

[!code-html[Main](updating-data/samples/sample4.html)]

<span data-ttu-id="f600f-144">Kılavuz nasıl çalıştığı hakkında bazı arka plan: satırları, her veritabanı kaydı için bir kılavuz gösterir ve veritabanı kayıttaki her alan için sütunları gösterir.</span><span class="sxs-lookup"><span data-stu-id="f600f-144">Some background about how the grid works: the grid displays rows, one for each database record, and it displays columns for each field in the database record.</span></span> <span data-ttu-id="f600f-145">Her bir kılavuz satırı oluşturulmuş olsa da `item` nesne ilgili satır için veritabanı kaydını (öğe) içerir.</span><span class="sxs-lookup"><span data-stu-id="f600f-145">While each grid row is being constructed, the `item` object contains the database record (item) for that row.</span></span> <span data-ttu-id="f600f-146">Bu düzenleme, kod ilgili satır için veri almak için bir yol sunar.</span><span class="sxs-lookup"><span data-stu-id="f600f-146">This arrangement gives you a way in code to get at the data for that row.</span></span> <span data-ttu-id="f600f-147">Burada gördüğünüz olmasıdır: ifade `item.ID` geçerli veritabanı öğesi kimliği değerinin alınması.</span><span class="sxs-lookup"><span data-stu-id="f600f-147">That's what you see here: the expression `item.ID` is getting the ID value of the current database item.</span></span> <span data-ttu-id="f600f-148">Kullanarak herhangi bir veritabanı değeri (başlık, türe veya yıl) aynı şekilde alabilir `item.Title`, `item.Genre`, veya `item.Year`.</span><span class="sxs-lookup"><span data-stu-id="f600f-148">You could get any of the database values (title, genre, or year) the same way by using `item.Title`, `item.Genre`, or `item.Year`.</span></span>

<span data-ttu-id="f600f-149">İfade `"~/EditMovie?id=@item.ID` hedef URL kodlanmış bölümü birleştirir (`~/EditMovie?id=`) dinamik olarak türetilmiş Bu kimliğe sahip</span><span class="sxs-lookup"><span data-stu-id="f600f-149">The expression `"~/EditMovie?id=@item.ID` combines the hard-coded part of the target URL (`~/EditMovie?id=`) with this dynamically derived ID.</span></span> <span data-ttu-id="f600f-150">(Gördüğünüz `~` önceki öğreticide; işleci, geçerli Web sitesinin kök temsil eden bir ASP.NET işlecidir.)</span><span class="sxs-lookup"><span data-stu-id="f600f-150">(You saw the `~` operator in the previous tutorial; it's an ASP.NET operator that represents the current website root.)</span></span>

<span data-ttu-id="f600f-151">Bu sütunda biçimlendirme parçası yalnızca aşağıdaki biçimlendirme gibi çalışma zamanında ürettiğini sonucudur:</span><span class="sxs-lookup"><span data-stu-id="f600f-151">The result is that this part of the markup in the column simply produces something like the following markup at run time:</span></span>

[!code-xml[Main](updating-data/samples/sample5.xml)]

<span data-ttu-id="f600f-152">Doğal olarak, gerçek değerini `id` her satır için farklı olacaktır.</span><span class="sxs-lookup"><span data-stu-id="f600f-152">Naturally, the actual value of `id` will be different for each row.</span></span>

## <a name="creating-a-custom-display-for-a-grid-column"></a><span data-ttu-id="f600f-153">Kılavuz sütunu için bir özel görüntü oluşturma</span><span class="sxs-lookup"><span data-stu-id="f600f-153">Creating a Custom Display for a Grid Column</span></span>

<span data-ttu-id="f600f-154">Kılavuz Sütunu şimdi yedekleyin.</span><span class="sxs-lookup"><span data-stu-id="f600f-154">Now back to the grid column.</span></span> <span data-ttu-id="f600f-155">Üç sütunu ilk olarak görüntülenen kılavuz yalnızca veri değerlerini (başlık, türe ve yıl) vardı.</span><span class="sxs-lookup"><span data-stu-id="f600f-155">The three columns you originally had in the grid displayed only data values (title, genre, and year).</span></span> <span data-ttu-id="f600f-156">Veritabanı sütununun adını geçirerek bu görüntü belirtilen &mdash; örneğin `grid.Column("Title")`.</span><span class="sxs-lookup"><span data-stu-id="f600f-156">You specified this display by passing the name of the database column &mdash; for example, `grid.Column("Title")`.</span></span>

<span data-ttu-id="f600f-157">Bu yeni **Düzenle** bağlantı sütununa farklıdır.</span><span class="sxs-lookup"><span data-stu-id="f600f-157">This new **Edit** link column is different.</span></span> <span data-ttu-id="f600f-158">Bir sütun adı belirtmek yerine çağırıyorsanız bir `format` parametresi.</span><span class="sxs-lookup"><span data-stu-id="f600f-158">Instead of specifying a column name, you're passing a `format` parameter.</span></span> <span data-ttu-id="f600f-159">Bu parametre, biçimlendirme tanımlamanıza olanak tanır, `WebGrid` yardımcı işleme ile birlikte `item` kalın veya yeşil olarak sütun verileri görüntüleyebilir veya ne olursa olsun, sizin biçiminde istediğiniz değer.</span><span class="sxs-lookup"><span data-stu-id="f600f-159">This parameter lets you define markup that the `WebGrid` helper will render along with the `item` value to display the column data as bold or green or in whatever format that you want.</span></span> <span data-ttu-id="f600f-160">Örneğin, başlığı kalın görünmesini isterseniz şu örnekteki gibi bir sütun oluşturabilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="f600f-160">For example, if you wanted the title to appear bold, you could create a column like this example:</span></span>

[!code-html[Main](updating-data/samples/sample6.html)]

<span data-ttu-id="f600f-161">(Çeşitli `@` içinde gördüğünüz karakterleri `format` özelliği işaretleme ve kod değeri arasında geçiş işaretlemek.)</span><span class="sxs-lookup"><span data-stu-id="f600f-161">(The various `@` characters you see in the `format` property mark the transition between markup and a code value.)</span></span>

<span data-ttu-id="f600f-162">Hakkında öğrendikten sonra `format` özelliği olduğu anlamak daha kolay bir şekilde nasıl yeni **Düzenle** bağlantı sütununa birlikte yerleştirin:</span><span class="sxs-lookup"><span data-stu-id="f600f-162">Once you know about the `format` property, it's easier to understand how the new **Edit** link column is put together:</span></span>

[!code-html[Main](updating-data/samples/sample7.html)]

<span data-ttu-id="f600f-163">Sütun oluşur *yalnızca* bağlantıyı işleyen biçimlerini, bazı bilgiler (ID) yanı sıra, ayıklanır satır için veritabanı kaydı.</span><span class="sxs-lookup"><span data-stu-id="f600f-163">The column consists *only* of the markup that renders the link, plus some information (the ID) that's extracted from the database record for the row.</span></span>

> [!TIP]
> 
> <span data-ttu-id="f600f-164">**Adlandırılmış parametreler ve bir yöntem için konumsal Parametreler**</span><span class="sxs-lookup"><span data-stu-id="f600f-164">**Named Parameters and Positional Parameters for a Method**</span></span>
> 
> <span data-ttu-id="f600f-165">Birden çok kez bir yöntemi ve parametreleri geçirilen parametre değerleri virgülle ayırarak yalnızca listelediğiniz.</span><span class="sxs-lookup"><span data-stu-id="f600f-165">Many times when you've called a method and passed parameters to it, you've simply listed the parameter values separated by commas.</span></span> <span data-ttu-id="f600f-166">Aşağıda birkaç örnek verilmiştir:</span><span class="sxs-lookup"><span data-stu-id="f600f-166">Here are a couple of examples:</span></span>
> 
> `db.Execute(insertCommand, title, genre, year)`
> 
> `Validation.RequireField("title", "You must enter a title")`
> 
> <span data-ttu-id="f600f-167">Biz bu kod ilk gördüğünüz, ancak her durumda, belirli bir sırayla yöntemleri için parametre geçirme sorun bahsetmek yaramadı &mdash; yani, bu yönteme parametreleri tanımlanmıştır sırası.</span><span class="sxs-lookup"><span data-stu-id="f600f-167">We didn't mention the issue when you first saw this code, but in each case, you're passing parameters to the methods in a specific order &mdash; namely, the order in which the parameters are defined in that method.</span></span> <span data-ttu-id="f600f-168">İçin `db.Execute` ve `Validation.RequireFields`, geçirdiğiniz değerlerin sırasını karıştırıldığında sayfa çalıştığında bir hata iletisi veya en azından bazı ilginç sonuçlar elde edersiniz.</span><span class="sxs-lookup"><span data-stu-id="f600f-168">For `db.Execute` and `Validation.RequireFields`, if you mixed up the order of the values you pass, you'd get an error message when the page runs, or at least some strange results.</span></span> <span data-ttu-id="f600f-169">Açıkça görülebileceği gibi parametreleri geçirmek için sipariş bilmeniz gerekir.</span><span class="sxs-lookup"><span data-stu-id="f600f-169">Clearly, you have to know the order to pass the parameters in.</span></span> <span data-ttu-id="f600f-170">(Webmatrix'te, IntelliSense, adı, türü ve parametre sırası şekil öğrenmenize yardımcı olabilir.)</span><span class="sxs-lookup"><span data-stu-id="f600f-170">(In WebMatrix, IntelliSense can help you learn figure out the name, type, and order of the parameters.)</span></span>
> 
> <span data-ttu-id="f600f-171">Değerlerin sırayla alternatif, kullandığınız *adlandırılmış parametreleri*.</span><span class="sxs-lookup"><span data-stu-id="f600f-171">As an alternative to passing values in order, you can use *named parameters*.</span></span> <span data-ttu-id="f600f-172">(Sırayla parametreleri geçirme bilinen kullanılarak *konumsal parametreler*.) Adlandırılmış parametreler için açıkça parametrenin adı değerini iletirken içerir.</span><span class="sxs-lookup"><span data-stu-id="f600f-172">(Passing parameters in order is known as using *positional parameters*.) For named parameters, you explicitly include the name of the parameter when passing its value.</span></span> <span data-ttu-id="f600f-173">Aşağıdaki öğreticilerde adlandırılmış parametreler birkaç kez zaten kullandınız.</span><span class="sxs-lookup"><span data-stu-id="f600f-173">You've used named parameters already a number of times in these tutorials.</span></span> <span data-ttu-id="f600f-174">Örneğin:</span><span class="sxs-lookup"><span data-stu-id="f600f-174">For example:</span></span>
> 
> [!code-csharp[Main](updating-data/samples/sample8.cs)]
> 
> <span data-ttu-id="f600f-175">and</span><span class="sxs-lookup"><span data-stu-id="f600f-175">and</span></span>
> 
> [!code-css[Main](updating-data/samples/sample9.css)]
> 
> <span data-ttu-id="f600f-176">Özellikle fazla sayıda parametre bir yöntem çektiğinde adlandırılmış parametreler durumlarda birkaç için kullanışlıdır.</span><span class="sxs-lookup"><span data-stu-id="f600f-176">Named parameters are handy for a couple of situations, especially when a method takes many parameters.</span></span> <span data-ttu-id="f600f-177">Yalnızca bir veya iki parametre geçirmek istediğiniz, ancak geçirmek istediğiniz değerleri parametre listesindeki ilk konumlar arasında değildir biridir.</span><span class="sxs-lookup"><span data-stu-id="f600f-177">One is when you want to pass only one or two parameters, but the values you want to pass are not among the first positions in the parameter list.</span></span> <span data-ttu-id="f600f-178">Sizin için anlamlı bir sırada parametreleri ileterek kodunuzu daha okunabilir hale getirmek istediğinizde başka bir durumdur.</span><span class="sxs-lookup"><span data-stu-id="f600f-178">Another situation is when you want to make your code more readable by passing the parameters in the order that makes the most sense to you.</span></span>
> 
> <span data-ttu-id="f600f-179">Kuşkusuz, adlandırılmış parametreleri kullanmak için parametrelerin adlarını bilmeniz gerekir.</span><span class="sxs-lookup"><span data-stu-id="f600f-179">Obviously, to use named parameters, you have to know the names of the parameters.</span></span> <span data-ttu-id="f600f-180">WebMatrix IntelliSense için *Göster* adları, ancak şu anda bunları sizin için doldurduğunuz olamaz.</span><span class="sxs-lookup"><span data-stu-id="f600f-180">WebMatrix IntelliSense can *show* you the names, but it cannot currently fill them in for you.</span></span>


## <a name="creating-the-edit-page"></a><span data-ttu-id="f600f-181">Düzenleme sayfası oluşturma</span><span class="sxs-lookup"><span data-stu-id="f600f-181">Creating the Edit Page</span></span>

<span data-ttu-id="f600f-182">Oluşturabileceğiniz artık *EditMovie* sayfası.</span><span class="sxs-lookup"><span data-stu-id="f600f-182">Now you can create the *EditMovie* page.</span></span> <span data-ttu-id="f600f-183">Kullanıcılar'ı tıklattığınızda **Düzenle** bağlantı, bunlar düştüğünden bu sayfada.</span><span class="sxs-lookup"><span data-stu-id="f600f-183">When users click the **Edit** link, they'll end up on this page.</span></span>

<span data-ttu-id="f600f-184">Adlı bir sayfa oluşturun *EditMovie.cshtml* dosyasında aşağıdaki işaretlemeyle nedir değiştirin:</span><span class="sxs-lookup"><span data-stu-id="f600f-184">Create a page named *EditMovie.cshtml* and replace what's in the file with the following markup:</span></span>

[!code-cshtml[Main](updating-data/samples/sample10.cshtml)]

<span data-ttu-id="f600f-185">Bu işaretleme ve kod, sahip benzer *AddMovie* sayfası.</span><span class="sxs-lookup"><span data-stu-id="f600f-185">This markup and code is similar to what you have in the *AddMovie* page.</span></span> <span data-ttu-id="f600f-186">Gönder düğmesine metninde küçük bir fark yoktur.</span><span class="sxs-lookup"><span data-stu-id="f600f-186">There's a small difference in the text for the submit button.</span></span> <span data-ttu-id="f600f-187">Olduğu gibi *AddMovie* sayfasında, var olan bir `Html.ValidationSummary` çağrı varsa, doğrulama hataları görüntüler.</span><span class="sxs-lookup"><span data-stu-id="f600f-187">As with the *AddMovie* page, there's an `Html.ValidationSummary` call that will display validation errors if there are any.</span></span> <span data-ttu-id="f600f-188">Biz bırakarak çağrıları çıkış şu `Validation.Message`, bu yana hataları doğrulama özeti görüntülenir.</span><span class="sxs-lookup"><span data-stu-id="f600f-188">This time we're leaving out calls to `Validation.Message`, since errors will be displayed in the validation summary.</span></span> <span data-ttu-id="f600f-189">Önceki öğreticide belirtildiği gibi doğrulama özeti ve tek hata iletileri çeşitli birleşimler kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="f600f-189">As noted in the previous tutorial, you can use the validation summary and the individual error messages in various combinations.</span></span>

<span data-ttu-id="f600f-190">Yeniden dikkat `method` özniteliği `<form>` ayarlanır `post`.</span><span class="sxs-lookup"><span data-stu-id="f600f-190">Notice again that the `method` attribute of the `<form>` element is set to `post`.</span></span> <span data-ttu-id="f600f-191">Olduğu gibi *AddMovie.cshtml* sayfası, bu sayfada yapar değişiklikleri veritabanına.</span><span class="sxs-lookup"><span data-stu-id="f600f-191">As with the *AddMovie.cshtml* page, this page makes changes to the database.</span></span> <span data-ttu-id="f600f-192">Bu nedenle, bu formu gerçekleştirmesi gereken bir `POST` işlemi.</span><span class="sxs-lookup"><span data-stu-id="f600f-192">Therefore, this form should perform a `POST` operation.</span></span> <span data-ttu-id="f600f-193">(Arasındaki fark hakkında daha fazla bilgi için `GET` ve `POST` işlemleri görmek [GET, POST ve HTTP fiili güvenliği](form-basics.md#GET,_POST,_and_HTTP_Verb_Safety) HTML formları öğreticide kenar.)</span><span class="sxs-lookup"><span data-stu-id="f600f-193">(For more about the difference between `GET` and `POST` operations, see the [GET, POST, and HTTP Verb Safety](form-basics.md#GET,_POST,_and_HTTP_Verb_Safety) sidebar in the tutorial on HTML forms.)</span></span>

<span data-ttu-id="f600f-194">Önceki bir öğreticide gördüğünüz gibi `value` metin kutularını özniteliklerini ayarlanır Razor koduyla bunları dağıtılacak için.</span><span class="sxs-lookup"><span data-stu-id="f600f-194">As you saw in an earlier tutorial, the `value` attributes of the text boxes are being set with Razor code in order to preload them.</span></span> <span data-ttu-id="f600f-195">Bu kez, yine de değişkenlere benzer kullanmakta olduğunuz `title` ve `genre` yerine bu görev `Request.Form["title"]`:</span><span class="sxs-lookup"><span data-stu-id="f600f-195">This time, though, you're using variables like `title` and `genre` for that task instead of `Request.Form["title"]`:</span></span>

`<input type="text" name="title" value="@title" />`

<span data-ttu-id="f600f-196">Olarak daha önce bu işaretleme film değerlerin metin kutusu değerlerle dağıtılacak.</span><span class="sxs-lookup"><span data-stu-id="f600f-196">As before, this markup will preload the text box values with the movie values.</span></span> <span data-ttu-id="f600f-197">Neden olduğu kullanmak yerine bu kez değişkenleri kullanmak için kullanışlı bir dakika içinde görürsünüz `Request` nesne.</span><span class="sxs-lookup"><span data-stu-id="f600f-197">You'll see in a moment why it's handy to use variables this time instead of using the `Request` object.</span></span>

<span data-ttu-id="f600f-198">Ayrıca bir `<input type="hidden">` bu sayfadaki öğesi.</span><span class="sxs-lookup"><span data-stu-id="f600f-198">There's also a `<input type="hidden">` element on this page.</span></span> <span data-ttu-id="f600f-199">Bu öğe görünür sayfada yapmadan film Kimliğini depolar.</span><span class="sxs-lookup"><span data-stu-id="f600f-199">This element stores the movie ID without making it visible on the page.</span></span> <span data-ttu-id="f600f-200">Kimliği başlangıçta sayfa tarafından bir sorgu dizesi değerini kullanarak geçirilir (`?id=7` veya URL'ye benzer).</span><span class="sxs-lookup"><span data-stu-id="f600f-200">The ID is initially passed to the page by using a query string value (`?id=7` or similar in the URL).</span></span> <span data-ttu-id="f600f-201">Kimlik değerini gizli bir alana koyarak kullanılabilir olduğundan emin olursunuz zaman formun gönderildi, artık sayfayı çağrıldı özgün URL'ye erişiminiz olsa bile.</span><span class="sxs-lookup"><span data-stu-id="f600f-201">By putting the ID value into a hidden field, you can make sure that it's available when the form is submitted, even if you no longer have access to the original URL that the page was invoked with.</span></span>

<span data-ttu-id="f600f-202">Farklı *AddMovie* sayfası, kodunu *EditMovie* sayfası, iki ayrı işlevler içerir.</span><span class="sxs-lookup"><span data-stu-id="f600f-202">Unlike the *AddMovie* page, the code for the *EditMovie* page has two distinct functions.</span></span> <span data-ttu-id="f600f-203">Sayfanın ilk kez görüntülendiğinde olan ilk işlev (ve *yalnızca* sonra), kod Sorgu dizesinden film kimliği alır.</span><span class="sxs-lookup"><span data-stu-id="f600f-203">The first function is that when the page is displayed for the first time (and *only* then), the code gets the movie ID from the query string.</span></span> <span data-ttu-id="f600f-204">Kod ardından kullandığı kimliği karşılık gelen bir film veritabanı dışına okuyun ve görüntülemek için (Bu metin kutularına dağıtılacak).</span><span class="sxs-lookup"><span data-stu-id="f600f-204">The code then uses the ID to read the corresponding movie out of the database and display (preload) it in the text boxes.</span></span>

<span data-ttu-id="f600f-205">Kullanıcı tıkladığında olan ikinci işlevi **değişiklikleri gönderme** düğmesini koduna sahip metin kutularını değerlerini okumak ve onları doğrulamak.</span><span class="sxs-lookup"><span data-stu-id="f600f-205">The second function is that when the user clicks the **Submit Changes** button, the code has to read the values of the text boxes and validate them.</span></span> <span data-ttu-id="f600f-206">Kod, yeni değerlerle veritabanı öğeyi güncelleştirmek de vardır.</span><span class="sxs-lookup"><span data-stu-id="f600f-206">The code also has to update the database item with the new values.</span></span> <span data-ttu-id="f600f-207">Gördüğünüz gibi bu teknik bir kayıt eklemeye benzer *AddMovie*.</span><span class="sxs-lookup"><span data-stu-id="f600f-207">This technique is similar to adding a record, as you saw in *AddMovie*.</span></span>

## <a name="adding-code-to-read-a-single-movie"></a><span data-ttu-id="f600f-208">Tek bir filmi okumayı kod ekleme</span><span class="sxs-lookup"><span data-stu-id="f600f-208">Adding Code to Read a Single Movie</span></span>

<span data-ttu-id="f600f-209">İlk işlev gerçekleştirmek için bu kodu sayfanın üst kısmına ekleyin:</span><span class="sxs-lookup"><span data-stu-id="f600f-209">To perform the first function, add this code to the top of the page:</span></span>

[!code-cshtml[Main](updating-data/samples/sample11.cshtml)]

<span data-ttu-id="f600f-210">Bu kod çoğunu başlatan bir blok içinde olduğu `if(!IsPost)`.</span><span class="sxs-lookup"><span data-stu-id="f600f-210">Most of this code is inside a block that starts `if(!IsPost)`.</span></span> <span data-ttu-id="f600f-211">`!` İşleci anlamına gelir "not", ifade anlamına gelir. Bu nedenle *bu isteği bir posta gönderimi değilse*, bildiren dolaylı bir yolu olan *çalıştıran bu sayfa ilk kez bu isteği olup olmadığını*.</span><span class="sxs-lookup"><span data-stu-id="f600f-211">The `!` operator means "not," so the expression means *if this request is not a post submission*, which is an indirect way of saying *if this request is the first time that this page has been run*.</span></span> <span data-ttu-id="f600f-212">Daha önce belirtildiği gibi bu kodun çalışması gereken *yalnızca* ilk kez çalıştırdığında sayfası.</span><span class="sxs-lookup"><span data-stu-id="f600f-212">As noted earlier, this code should run *only* the first time the page runs.</span></span> <span data-ttu-id="f600f-213">Kod içine yaramadı alırsanız `if(!IsPost)`, sayfa, olup olmadığını ilk kez her çağrıldığında çalıştırmak veya yanıt düğmesine tıklayın.</span><span class="sxs-lookup"><span data-stu-id="f600f-213">If you didn't enclose the code in `if(!IsPost)`, it would run every time the page is invoked, whether the first time or in response to a button click.</span></span>

<span data-ttu-id="f600f-214">Kodu içeren bildirimi bir `else` şu engelleyin.</span><span class="sxs-lookup"><span data-stu-id="f600f-214">Notice that the code includes an `else` block this time.</span></span> <span data-ttu-id="f600f-215">Ne zaman kullanıma sunduk dediğin gibi `if` blokları, bazen test koşul true değilse alternatif kod çalıştırmak istediğiniz.</span><span class="sxs-lookup"><span data-stu-id="f600f-215">As we said when we introduced `if` blocks, sometimes you want to run alternative code if the condition you're testing isn't true.</span></span> <span data-ttu-id="f600f-216">Durum burada olmasıdır.</span><span class="sxs-lookup"><span data-stu-id="f600f-216">That's the case here.</span></span> <span data-ttu-id="f600f-217">(Diğer bir deyişle, sayfaya geçirilen kimliği Tamam ise), koşul başarılı olursa, veritabanından bir satırı okuyun.</span><span class="sxs-lookup"><span data-stu-id="f600f-217">If the condition passes (that is, if the ID passed to the page is ok), you read a row from the database.</span></span> <span data-ttu-id="f600f-218">Ancak, koşul başarısız olursa `else` bloğu çalışır ve bir hata iletisi kodunu ayarlar.</span><span class="sxs-lookup"><span data-stu-id="f600f-218">However, if the condition doesn't pass, the `else` block runs and the code sets an error message.</span></span>

## <a name="validating-a-value-passed-to-the-page"></a><span data-ttu-id="f600f-219">Sayfaya geçirilen bir değeri doğrulanıyor</span><span class="sxs-lookup"><span data-stu-id="f600f-219">Validating a Value Passed to the Page</span></span>

<span data-ttu-id="f600f-220">Kod `Request.QueryString["id"]` sayfasına geçirilir kimliği alınamıyor.</span><span class="sxs-lookup"><span data-stu-id="f600f-220">The code uses `Request.QueryString["id"]` to get the ID that's passed to the page.</span></span> <span data-ttu-id="f600f-221">Kod bir değeri kimliği için gerçekten iletildi emin olur</span><span class="sxs-lookup"><span data-stu-id="f600f-221">The code makes sure that a value was actually passed for the ID.</span></span> <span data-ttu-id="f600f-222">Hiçbir değer geçirildi, kod bir doğrulama hatası ayarlar.</span><span class="sxs-lookup"><span data-stu-id="f600f-222">If no value was passed, the code sets a validation error.</span></span>

<span data-ttu-id="f600f-223">Bu kod, bilgileri doğrulamak için farklı bir yol gösterir.</span><span class="sxs-lookup"><span data-stu-id="f600f-223">This code shows a different way to validate information.</span></span> <span data-ttu-id="f600f-224">Önceki öğreticide çalıştığınız `Validation` Yardımcısı.</span><span class="sxs-lookup"><span data-stu-id="f600f-224">In the previous tutorial, you worked with the `Validation` helper.</span></span> <span data-ttu-id="f600f-225">Doğrulamak için alanları kayıtlı ve ASP.NET otomatik olarak doğrulama tamamladıysanız ve hataları kullanarak görüntülenen `Html.ValidationMessage` ve `Html.ValidationSummary`.</span><span class="sxs-lookup"><span data-stu-id="f600f-225">You registered fields to validate, and ASP.NET automatically did the validation and displayed errors by using `Html.ValidationMessage` and `Html.ValidationSummary`.</span></span> <span data-ttu-id="f600f-226">Bu durumda, ancak siz değil gerçekten kullanıcı girişini doğrulama.</span><span class="sxs-lookup"><span data-stu-id="f600f-226">In this case, however, you're not really validating user input.</span></span> <span data-ttu-id="f600f-227">Bunun yerine, sayfaya başka yerlerden geçirilen değeri doğrulama.</span><span class="sxs-lookup"><span data-stu-id="f600f-227">Instead, you're validating a value that was passed to the page from elsewhere.</span></span> <span data-ttu-id="f600f-228">`Validation` Yardımcı olmayan yapın, sizin için.</span><span class="sxs-lookup"><span data-stu-id="f600f-228">The `Validation` helper doesn't do that for you.</span></span>

<span data-ttu-id="f600f-229">Bu nedenle, değeri kendiniz ile test ederek kontrol `if(!Request.QueryString["ID"].IsEmpty()`).</span><span class="sxs-lookup"><span data-stu-id="f600f-229">Therefore, you check the value yourself, by testing it with `if(!Request.QueryString["ID"].IsEmpty()`).</span></span> <span data-ttu-id="f600f-230">Bir sorun varsa, hata kullanarak görüntüleyebilirsiniz `Html.ValidationSummary`ile yaptığınız gibi `Validation` Yardımcısı.</span><span class="sxs-lookup"><span data-stu-id="f600f-230">If there's a problem, you can display the error by using `Html.ValidationSummary`, as you did with the `Validation` helper.</span></span> <span data-ttu-id="f600f-231">Bunu yapmak için çağrı `Validation.AddFormError` ve görüntülenecek bir ileti geçirin.</span><span class="sxs-lookup"><span data-stu-id="f600f-231">To do that, you call `Validation.AddFormError` and pass it a message to display.</span></span> <span data-ttu-id="f600f-232">`Validation.AddFormError` oturum zaten alışık olduğunuz doğrulama sistemi tie özel iletiler tanımlamanızı sağlar yerleşik bir yöntemdir.</span><span class="sxs-lookup"><span data-stu-id="f600f-232">`Validation.AddFormError` is a built-in method that lets you define custom messages that tie in with the validation system you're already familiar with.</span></span> <span data-ttu-id="f600f-233">(Bu öğreticinin ilerleyen bölümlerinde bu doğrulama işlemi biraz daha sağlam hale hakkında konuşacağız.)</span><span class="sxs-lookup"><span data-stu-id="f600f-233">(Later in this tutorial we'll talk about how to make this validation process a little more robust.)</span></span>

<span data-ttu-id="f600f-234">Film kimliği olduğundan emin olduktan sonra kodu yalnızca bir tek veritabanı öğesi için arayan veritabanı okur.</span><span class="sxs-lookup"><span data-stu-id="f600f-234">After making sure that there's an ID for the movie, the code reads the database, looking for only a single database item.</span></span> <span data-ttu-id="f600f-235">(Genel düzen veritabanı işlemlerinin büyük bir olasılıkla fark etmiş: veritabanı, SQL deyimini tanımlayın ve deyimini çalıştırın.) Bu kez, SQL `Select` deyimi `WHERE ID = @0`.</span><span class="sxs-lookup"><span data-stu-id="f600f-235">(You probably have noticed the general pattern for database operations: open the database, define a SQL statement, and run the statement.) This time, the SQL `Select` statement includes `WHERE ID = @0`.</span></span> <span data-ttu-id="f600f-236">Kimliği benzersiz olduğundan, yalnızca bir kayıtla döndürülebilir.</span><span class="sxs-lookup"><span data-stu-id="f600f-236">Because the ID is unique, only one record can be returned.</span></span>

<span data-ttu-id="f600f-237">Sorgu kullanarak gerçekleştirilir `db.QuerySingle` (değil `db.Query`film listeleme için kullandığınız gibi), ve kod sonucu koyar `row` değişkeni.</span><span class="sxs-lookup"><span data-stu-id="f600f-237">The query is performed by using `db.QuerySingle` (not `db.Query`, as you used for the movie listing), and the code puts the result into the `row` variable.</span></span> <span data-ttu-id="f600f-238">Adı `row` rastgeledir; değişkenleri, istediğiniz şekilde adlandırabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="f600f-238">The name `row` is arbitrary; you can name the variables anything you like.</span></span> <span data-ttu-id="f600f-239">Böylece bu değerleri metin kutularına görüntülenebilen en üstünde başlatılmamış değişkenler ardından film ayrıntıları ile doldurulur.</span><span class="sxs-lookup"><span data-stu-id="f600f-239">The variables initialized at the top are then filled with the movie details so that these values can be displayed in the text boxes.</span></span>

## <a name="testing-the-edit-page-so-far"></a><span data-ttu-id="f600f-240">Düzen Sayfası (şimdiye) test etme</span><span class="sxs-lookup"><span data-stu-id="f600f-240">Testing the Edit Page (So Far)</span></span>

<span data-ttu-id="f600f-241">Sayfanızın test etmek istiyorsanız, çalıştırma *filmler* artık sayfasında ve tıklayın bir **Düzenle** tüm film yanındaki bağlantı.</span><span class="sxs-lookup"><span data-stu-id="f600f-241">If you'd like to test your page, run the *Movies* page now and click an **Edit** link next to any movie.</span></span> <span data-ttu-id="f600f-242">Göreceğiniz *EditMovie* Ayrıntıları sayfası, seçilen film doldurulur:</span><span class="sxs-lookup"><span data-stu-id="f600f-242">You'll see the *EditMovie* page with the details filled in for the movie you selected:</span></span>

![Düzenlenecek film gösteren film sayfayı Düzenle](updating-data/_static/image3.png)

<span data-ttu-id="f600f-244">Sayfanın URL'sini gibi içerdiğine dikkat edin `?id=10` (veya başka bir sayı).</span><span class="sxs-lookup"><span data-stu-id="f600f-244">Notice that the URL of the page includes something like `?id=10` (or some other number).</span></span> <span data-ttu-id="f600f-245">Şu ana kadar test ettiğiniz **Düzenle** Bağlantılar *film* çalışma sayfanızı kimliği Sorgu dizesinden okuyor ve veritabanı tek film kaydını almak için sorgu çalışma sayfasında.</span><span class="sxs-lookup"><span data-stu-id="f600f-245">So far you've tested that **Edit** links in the *Movie* page work, that your page is reading the ID from the query string, and that the database query to get a single movie record is working.</span></span>

<span data-ttu-id="f600f-246">Film bilgileri değiştirebilirsiniz, ancak tıkladığınızda hiçbir şey olmuyor **değişiklikleri gönderme**.</span><span class="sxs-lookup"><span data-stu-id="f600f-246">You can change the movie information, but nothing happens when you click **Submit Changes**.</span></span>

## <a name="adding-code-to-update-the-movie-with-the-users-changes"></a><span data-ttu-id="f600f-247">Film kullanıcının değişikliklerle güncelleştirmek için kod ekleme</span><span class="sxs-lookup"><span data-stu-id="f600f-247">Adding Code to Update the Movie with the User's Changes</span></span>

<span data-ttu-id="f600f-248">İçinde *EditMovie.cshtml* (değişiklikleri kaydetme) ikinci işlevi uygulamak için aşağıdaki kodu yalnızca kapanış ayracı ekleyin `@` blok.</span><span class="sxs-lookup"><span data-stu-id="f600f-248">In the *EditMovie.cshtml* file, to implement the second function (saving changes), add the following code just inside the closing brace of the `@` block.</span></span> <span data-ttu-id="f600f-249">(Kod tam olarak nerede emin değilseniz bakabilirsiniz [film düzenleme sayfası için tam kod](#Complete_Page_Listing_for_EditMovie) Bu öğreticinin sonunda görünür.)</span><span class="sxs-lookup"><span data-stu-id="f600f-249">(If you're not sure exactly where to put the code, you can look at the [complete code listing for the Edit Movie page](#Complete_Page_Listing_for_EditMovie) that appears at the end of this tutorial.)</span></span>

[!code-csharp[Main](updating-data/samples/sample12.cs)]

<span data-ttu-id="f600f-250">Yeniden, bu işaretleme ve kod kodda benzer *AddMovie*.</span><span class="sxs-lookup"><span data-stu-id="f600f-250">Again, this markup and code is similar to the code in *AddMovie*.</span></span> <span data-ttu-id="f600f-251">Kodu bir `if(IsPost)` bloğunda yalnızca kullanıcı tıkladığında bu kodu çalıştığından **değişiklikleri gönderme** düğmesi &mdash; diğer bir deyişle, ne zaman (ve yalnızca) form forumumuza gönderildi.</span><span class="sxs-lookup"><span data-stu-id="f600f-251">The code is in an `if(IsPost)` block, because this code runs only when the user clicks the **Submit Changes** button &mdash; that is, when (and only when) the form has been posted.</span></span> <span data-ttu-id="f600f-252">Bu durumda, bir test gibi kullanmadığınız `if(IsPost && Validation.IsValid())`— diğer bir deyişle, her iki testleri kullanarak birleştirdiğiniz değil ve.</span><span class="sxs-lookup"><span data-stu-id="f600f-252">In this case, you're not using a test like `if(IsPost && Validation.IsValid())`— that is, you're not combining both tests by using AND.</span></span> <span data-ttu-id="f600f-253">Bu sayfada, öncelikle bir form gönderimi olup olmadığını belirlemeniz (`if(IsPost)`) ve ardından yalnızca doğrulama için alanları kaydedin.</span><span class="sxs-lookup"><span data-stu-id="f600f-253">In this page, you first determine whether there's a form submission (`if(IsPost)`), and only then register the fields for validation.</span></span> <span data-ttu-id="f600f-254">Doğrulama sonuçları test edebilirsiniz sonra (`if(Validation.IsValid()`).</span><span class="sxs-lookup"><span data-stu-id="f600f-254">Then you can test the validation results (`if(Validation.IsValid()`).</span></span> <span data-ttu-id="f600f-255">Akışı biraz daha farklıdır *AddMovie.cshtml* sayfa ancak etkisi aynıdır.</span><span class="sxs-lookup"><span data-stu-id="f600f-255">The flow is slightly different than in the *AddMovie.cshtml* page, but the effect is the same.</span></span>

<span data-ttu-id="f600f-256">Kullanarak metin kutularını değerlerini alın `Request.Form["title"]` ve diğer benzer bir kod `<input>` öğeleri.</span><span class="sxs-lookup"><span data-stu-id="f600f-256">You get the values of the text boxes by using `Request.Form["title"]` and similar code for the other `<input>` elements.</span></span> <span data-ttu-id="f600f-257">Bu kez, film Kimliğini kod dışında gizli alan alır dikkat edin (`<input type="hidden">`).</span><span class="sxs-lookup"><span data-stu-id="f600f-257">Notice that this time, the code gets the movie ID out of the hidden field (`<input type="hidden">`).</span></span> <span data-ttu-id="f600f-258">Sayfanın ilk kez çalıştırdığınızda, kodu sorgu dizesi dışında kimliği alındı.</span><span class="sxs-lookup"><span data-stu-id="f600f-258">When the page ran the first time, the code got the ID out of the query string.</span></span> <span data-ttu-id="f600f-259">Sorgu dizesi şekilde o zamandan bu yana değiştirildiği durumda başlangıçta zobrazilo film kimliği aldığınızdan emin olmak için gizli alanındaki değer elde edin.</span><span class="sxs-lookup"><span data-stu-id="f600f-259">You get the value from the hidden field to make sure that you're getting the ID of the movie that was originally displayed, in case the query string was somehow altered since then.</span></span>

<span data-ttu-id="f600f-260">Arasındaki en önemli fark *AddMovie* kodu ve bu kodu olduğundan bu kodda, SQL kullanmanızı `Update` deyimi yerine `Insert Into` deyimi.</span><span class="sxs-lookup"><span data-stu-id="f600f-260">The really important difference between the *AddMovie* code and this code is that in this code you use the SQL `Update` statement instead of the `Insert Into` statement.</span></span> <span data-ttu-id="f600f-261">Aşağıdaki örnek SQL söz dizimi görülmektedir `Update` deyimi:</span><span class="sxs-lookup"><span data-stu-id="f600f-261">The following example shows the syntax of the SQL `Update` statement:</span></span>

`UPDATE table SET col1="value", col2="value", col3="value" ... WHERE ID = value`

<span data-ttu-id="f600f-262">Herhangi bir sırada hiçbir sütun belirtebilirsiniz ve mutlaka sırasında her bir sütunun yoksa bir `Update` işlemi.</span><span class="sxs-lookup"><span data-stu-id="f600f-262">You can specify any columns in any order, and you don't necessarily have to update every column during an `Update` operation.</span></span> <span data-ttu-id="f600f-263">(Kimliği kendisini yürürlükte kaydettiğiniz kaydı yeni bir kayıt ve verilmeyen için olduğundan güncelleştirilemiyor bir `Update` işlem.)</span><span class="sxs-lookup"><span data-stu-id="f600f-263">(You cannot update the ID itself, because that would in effect save the record as a new record, and that's not allowed for an `Update` operation.)</span></span>

> [!NOTE] 
> 
> <span data-ttu-id="f600f-264">**Önemli** `Where` veritabanı hangi veritabanı nasıl bilir olduğu için kimliği yan tümcesinde çok önemli güncelleştirmek istediğiniz kayıt.</span><span class="sxs-lookup"><span data-stu-id="f600f-264">**Important** The `Where` clause with the ID is very important, because that's how the database knows which database record you want to update.</span></span> <span data-ttu-id="f600f-265">Devre dışı bırakılırsa `Where` yan tümcesi, veritabanı güncelleştirme *her* veritabanında kayıt.</span><span class="sxs-lookup"><span data-stu-id="f600f-265">If you left off the `Where` clause, the database would update *every* record in the database.</span></span> <span data-ttu-id="f600f-266">Çoğu durumda, olağanüstü bir durumda olacaktır.</span><span class="sxs-lookup"><span data-stu-id="f600f-266">In most cases, that would be a disaster.</span></span>


<span data-ttu-id="f600f-267">Kodda yer tutucuları kullanarak SQL deyiminde güncelleştirilecek hiçbir değer geçirilir.</span><span class="sxs-lookup"><span data-stu-id="f600f-267">In the code, the values to update are passed to the SQL statement by using placeholders.</span></span> <span data-ttu-id="f600f-268">Ne önce söz ettiğimiz yinelenecek: güvenlik nedeniyle, *yalnızca* bir SQL deyimi için değerleri geçirmek için kullanın.</span><span class="sxs-lookup"><span data-stu-id="f600f-268">To repeat what we've said before: for security reasons, *only* use placeholders to pass values to a SQL statement.</span></span>

<span data-ttu-id="f600f-269">Kod sonra `db.Execute` çalıştırılacak `Update` deyimi, değişiklikleri görebileceğiniz geri listesi sayfasına yönlendirir.</span><span class="sxs-lookup"><span data-stu-id="f600f-269">After the code uses `db.Execute` to run the `Update` statement, it redirects back to the listing page, where you can see the changes.</span></span>

> [!TIP] 
> 
> <span data-ttu-id="f600f-270">**Farklı SQL deyimleri, farklı yöntemler**</span><span class="sxs-lookup"><span data-stu-id="f600f-270">**Different SQL Statements, Different Methods**</span></span>
> 
> <span data-ttu-id="f600f-271">Farklı SQL deyimleri çalıştırma için biraz farklı yöntemler kullanın fark etmiş olabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="f600f-271">You might have noticed that you use slightly different methods to run different SQL statements.</span></span> <span data-ttu-id="f600f-272">Çalıştırılacak bir `Select` sorgu, büyük olasılıkla döndürür birden çok kayıt, kullandığınız `Query` yöntemi.</span><span class="sxs-lookup"><span data-stu-id="f600f-272">To run a `Select` query that potentially returns multiple records, you use the `Query` method.</span></span> <span data-ttu-id="f600f-273">Çalıştırılacak bir `Select` bildiğiniz bir sorgu tek bir veritabanı öğesi döndürür, kullandığınız `QuerySingle` yöntemi.</span><span class="sxs-lookup"><span data-stu-id="f600f-273">To run a `Select` query that you know will return only one database item, you use the `QuerySingle` method.</span></span> <span data-ttu-id="f600f-274">Değişiklikleri yapın, ancak veritabanı öğeleri iade olmayan komutları çalıştırmak için kullandığınız `Execute` yöntemi.</span><span class="sxs-lookup"><span data-stu-id="f600f-274">To run commands that make changes but that don't return database items, you use the `Execute` method.</span></span>
> 
> <span data-ttu-id="f600f-275">Çünkü bunların her biri farklı sonuçlar döndürür zaten arasındaki farkı içinde anlatıldığı gibi farklı yöntemlere sahip olmanız `Query` ve `QuerySingle`.</span><span class="sxs-lookup"><span data-stu-id="f600f-275">You have to have different methods because each of them returns different results, as you saw already in the difference between `Query` and `QuerySingle`.</span></span> <span data-ttu-id="f600f-276">( `Execute` Aslında bir değer döndürür ayrıca &mdash; yani, komut tarafından etkilenen veritabanı satır sayısını &mdash; ancak sizin, şimdiye yoksayılıyor.)</span><span class="sxs-lookup"><span data-stu-id="f600f-276">(The `Execute` method actually returns a value also &mdash; namely, the number of database rows that were affected by the command &mdash; but you've been ignoring that so far.)</span></span>
> 
> <span data-ttu-id="f600f-277">Elbette, `Query` yöntemi yalnızca bir veritabanı satır döndürebilir.</span><span class="sxs-lookup"><span data-stu-id="f600f-277">Of course, the `Query` method might return only one database row.</span></span> <span data-ttu-id="f600f-278">Ancak, ASP.NET sonuçları her zaman davranır `Query` yöntem bir koleksiyon.</span><span class="sxs-lookup"><span data-stu-id="f600f-278">However, ASP.NET always treats the results of the `Query` method as a collection.</span></span> <span data-ttu-id="f600f-279">Yöntemi yalnızca bir satır döndürür olsa bile, tek bir satır koleksiyondan ayıklamak sahip.</span><span class="sxs-lookup"><span data-stu-id="f600f-279">Even if the method returns just one row, you have to extract that single row from the collection.</span></span> <span data-ttu-id="f600f-280">Bu nedenle, durumlarda Burada, *bilmeniz* kullanmak daha kullanışlı bir bit olduğundan, yalnızca bir satır döneceğiz `QuerySingle`.</span><span class="sxs-lookup"><span data-stu-id="f600f-280">Therefore, in situations where you *know* you'll get back only one row, it's a bit more convenient to use `QuerySingle`.</span></span>
> 
> <span data-ttu-id="f600f-281">Belirli türde bir veritabanı işlemleri gerçekleştirmek birkaç yöntem vardır.</span><span class="sxs-lookup"><span data-stu-id="f600f-281">There are a few other methods that perform specific types of database operations.</span></span> <span data-ttu-id="f600f-282">Veritabanı yöntemleri listesini bulabilirsiniz [ASP.NET Web sayfaları API hızlı başvurusu](../../api-reference/asp-net-web-pages-api-reference.md#Data).</span><span class="sxs-lookup"><span data-stu-id="f600f-282">You can find a listing of database methods in the [ASP.NET Web Pages API Quick Reference](../../api-reference/asp-net-web-pages-api-reference.md#Data).</span></span>


## <a name="making-validation-for-the-id-more-robust"></a><span data-ttu-id="f600f-283">Doğrulama için kimliği daha sağlam hale</span><span class="sxs-lookup"><span data-stu-id="f600f-283">Making Validation for the ID More Robust</span></span>

<span data-ttu-id="f600f-284">Bu film veritabanından Al çalışabilmeniz için sayfa çalıştığında, ilk kez, film Kimliğini Sorgu dizesinden alın.</span><span class="sxs-lookup"><span data-stu-id="f600f-284">The first time that the page runs, you get the movie ID from the query string so that you can go get that movie from the database.</span></span> <span data-ttu-id="f600f-285">Aslında olduğunu bu kodu kullanarak yaptığınız arayın, gitmek için bir değer emin yaptınız:</span><span class="sxs-lookup"><span data-stu-id="f600f-285">You made sure that there actually was a value to go look for, which you did by using this code:</span></span>

[!code-csharp[Main](updating-data/samples/sample13.cs)]

<span data-ttu-id="f600f-286">Bu kod bir kullanıcı için alırsa emin olmak için kullanılan *EditMovies* filmi ilk seçmeden sayfası *filmler* sayfası, sayfayı kolay bir hata iletisi görüntüler.</span><span class="sxs-lookup"><span data-stu-id="f600f-286">You used this code to make sure that if a user gets to the *EditMovies* page without first selecting a movie in the *Movies* page, the page would display a user-friendly error message.</span></span> <span data-ttu-id="f600f-287">(Aksi takdirde, kullanıcılar büyük olasılıkla yalnızca bunları karıştırır bir hata görür.)</span><span class="sxs-lookup"><span data-stu-id="f600f-287">(Otherwise, users would see an error that would probably just confuse them.)</span></span>

<span data-ttu-id="f600f-288">Ancak, bu doğrulama çok sağlam değil.</span><span class="sxs-lookup"><span data-stu-id="f600f-288">However, this validation isn't very robust.</span></span> <span data-ttu-id="f600f-289">Sayfa Ayrıca bu hatalarla çağrılan:</span><span class="sxs-lookup"><span data-stu-id="f600f-289">The page might also be invoked with these errors:</span></span>

- <span data-ttu-id="f600f-290">Kimliği bir sayı değil.</span><span class="sxs-lookup"><span data-stu-id="f600f-290">The ID isn't a number.</span></span> <span data-ttu-id="f600f-291">Örneğin, sayfa gibi bir URL ile çağırılabilir `http://localhost:nnnnn/EditMovie?id=abc`.</span><span class="sxs-lookup"><span data-stu-id="f600f-291">For example, the page could be invoked with a URL like `http://localhost:nnnnn/EditMovie?id=abc`.</span></span>
- <span data-ttu-id="f600f-292">Bir sayı Kimliğin şöyle olduğunu, ancak mevcut olmayan bir filmi başvurduğu (örneğin, `http://localhost:nnnnn/EditMovie?id=100934`).</span><span class="sxs-lookup"><span data-stu-id="f600f-292">The ID is a number, but it references a movie that doesn't exist (for example, `http://localhost:nnnnn/EditMovie?id=100934`).</span></span>

<span data-ttu-id="f600f-293">Bu URL'lerden çalıştırma sonucu görüyorsunuz merak ediyorsanız *filmler* sayfası.</span><span class="sxs-lookup"><span data-stu-id="f600f-293">If you're curious to see the errors that result from these URLs, run the *Movies* page.</span></span> <span data-ttu-id="f600f-294">Bir filmi düzenlemek için seçin ve sonra URL'sini değiştirme *EditMovie* kimliği veya var olmayan film Kimliğini içeren bir alfabetik bir URL'ye sayfa.</span><span class="sxs-lookup"><span data-stu-id="f600f-294">Select a movie to edit, and then change the URL of the *EditMovie* page to a URL that contains an alphabetic ID or the ID of a non-existent movie.</span></span>

<span data-ttu-id="f600f-295">Bu nedenle ne yapmalısınız?</span><span class="sxs-lookup"><span data-stu-id="f600f-295">So what should you do?</span></span> <span data-ttu-id="f600f-296">İlk düzeltmeyi yalnızca bir kimlik sayfasına geçirilir, ancak kimliği bir tamsayı olduğu emin olmaktır.</span><span class="sxs-lookup"><span data-stu-id="f600f-296">The first fix is to make sure that not only is an ID passed to the page, but that the ID is an integer.</span></span> <span data-ttu-id="f600f-297">Kodu değiştirmek `!IsPost` test şu örnekteki gibi aramak için:</span><span class="sxs-lookup"><span data-stu-id="f600f-297">Change the code for the `!IsPost` test to look like this example:</span></span>

[!code-csharp[Main](updating-data/samples/sample14.cs)]

<span data-ttu-id="f600f-298">İkinci bir koşul eklediğiniz `IsEmpty` ile bağlantılı test `&&` (mantıksal ve):</span><span class="sxs-lookup"><span data-stu-id="f600f-298">You've added a second condition to the `IsEmpty` test, linked with `&&` (logical AND):</span></span>

[!code-csharp[Main](updating-data/samples/sample15.cs)]

<span data-ttu-id="f600f-299">Hatırlıyor [ASP.NET Web sayfaları programlama giriş](../introducing-razor-syntax-c.md) yöntemleri gibi öğretici `AsBool` bir `AsInt` bir karakter dizesi bazı diğer veri türüne dönüştürün.</span><span class="sxs-lookup"><span data-stu-id="f600f-299">You might remember from the [Introduction to ASP.NET Web Pages Programming](../introducing-razor-syntax-c.md) tutorial that methods like `AsBool` an `AsInt` convert a character string to some other data type.</span></span> <span data-ttu-id="f600f-300">`IsInt` Yöntemi (ve diğerleri gibi `IsBool` ve `IsDateTime`) benzer.</span><span class="sxs-lookup"><span data-stu-id="f600f-300">The `IsInt` method (and others, like `IsBool` and `IsDateTime`) are similar.</span></span> <span data-ttu-id="f600f-301">Ancak, bunlar yalnızca sınama olup olmadığını, *olabilir* dize dönüştürme işlemini gerçekleştirmeden Dönüştür.</span><span class="sxs-lookup"><span data-stu-id="f600f-301">However, they test only whether you *can* convert the string, without actually performing the conversion.</span></span> <span data-ttu-id="f600f-302">Burada, temelde belirten *tamsayıya sorgu dize değeri dönüştürülebilir ise...* .</span><span class="sxs-lookup"><span data-stu-id="f600f-302">So here you're essentially saying *If the query string value can be converted to an integer ...*.</span></span>

<span data-ttu-id="f600f-303">Başka bir olası sorun, mevcut olmayan bir film arıyor.</span><span class="sxs-lookup"><span data-stu-id="f600f-303">The other potential problem is looking for a movie that doesn't exist.</span></span> <span data-ttu-id="f600f-304">Bir filmi alma kodu şu kod gibi görünür:</span><span class="sxs-lookup"><span data-stu-id="f600f-304">The code to get a movie looks like this code:</span></span>

[!code-csharp[Main](updating-data/samples/sample16.cs)]

<span data-ttu-id="f600f-305">Geçirirseniz bir `movieId` değerini `QuerySingle` deyimleri, izleyin ve gerçek bir filmi gelmiyor yöntemi, hiçbir şey döndürülür (örneğin, `title=row.Title`) hatalara neden.</span><span class="sxs-lookup"><span data-stu-id="f600f-305">If you pass a `movieId` value to the `QuerySingle` method that doesn't correspond to an actual movie, nothing is returned and the statements that follow (for example, `title=row.Title`) result in errors.</span></span>

<span data-ttu-id="f600f-306">Tekrar bir kolayca düzeltme yoktur.</span><span class="sxs-lookup"><span data-stu-id="f600f-306">Again there's an easy fix.</span></span> <span data-ttu-id="f600f-307">Varsa `db.QuerySingle` yöntemi döndürür, sonuç `row` değişkeni null olacaktır.</span><span class="sxs-lookup"><span data-stu-id="f600f-307">If the `db.QuerySingle` method returns no results, the `row` variable will be null.</span></span> <span data-ttu-id="f600f-308">Denetleyebilirsiniz şekilde olmadığını `row` değerleri almak denemeden önce değişken NULL'dur.</span><span class="sxs-lookup"><span data-stu-id="f600f-308">So you can check whether the `row` variable is null before you try to get values from it.</span></span> <span data-ttu-id="f600f-309">Aşağıdaki kodu ekler bir `if` bloğu içine tanesi değerlerini alma deyimleri `row` nesnesi:</span><span class="sxs-lookup"><span data-stu-id="f600f-309">The following code adds an `if` block around the statements that get the values out of the `row` object:</span></span>

[!code-csharp[Main](updating-data/samples/sample17.cs)]

<span data-ttu-id="f600f-310">Bu iki ek doğrulama testleri ile daha madde işareti kavram sayfa olur.</span><span class="sxs-lookup"><span data-stu-id="f600f-310">With these two additional validation tests, the page becomes more bullet-proof.</span></span> <span data-ttu-id="f600f-311">İçin tam kod `!IsPost` dalı artık şu örnekteki gibi görünür:</span><span class="sxs-lookup"><span data-stu-id="f600f-311">The complete code for the `!IsPost` branch now looks like this example:</span></span>

[!code-csharp[Main](updating-data/samples/sample18.cs)]

<span data-ttu-id="f600f-312">Biz bir kez daha bu görev için iyi bir kullanımı olduğuna dikkat edin bir `else` blok.</span><span class="sxs-lookup"><span data-stu-id="f600f-312">We'll note once more that this task is a good use for an `else` block.</span></span> <span data-ttu-id="f600f-313">Testleri geçirmezseniz `else` blokları hata iletileri ayarlayın.</span><span class="sxs-lookup"><span data-stu-id="f600f-313">If the tests don't pass, the `else` blocks set error messages.</span></span>

## <a name="adding-a-link-to-return-to-the-movies-page"></a><span data-ttu-id="f600f-314">Filmler sayfasına geri dönmek için bağlantı ekleme</span><span class="sxs-lookup"><span data-stu-id="f600f-314">Adding a Link to Return to the Movies Page</span></span>

<span data-ttu-id="f600f-315">Son ve yararlı bir ayrıntı bağlantısını eklemektir geri *filmler* sayfası.</span><span class="sxs-lookup"><span data-stu-id="f600f-315">A final and helpful detail is to add a link back to the *Movies* page.</span></span> <span data-ttu-id="f600f-316">Kullanıcıların olayları sıradan akışında başlangıç *filmler* sayfasında ve tıklayın bir **Düzenle** bağlantı.</span><span class="sxs-lookup"><span data-stu-id="f600f-316">In the ordinary flow of events, users will start at the *Movies* page and click an **Edit** link.</span></span> <span data-ttu-id="f600f-317">Getiren bunları *EditMovie* sayfasında, burada film düzenleme yapıp düğmesine tıklayın.</span><span class="sxs-lookup"><span data-stu-id="f600f-317">That brings them to the *EditMovie* page, where they can edit the movie and click the button.</span></span> <span data-ttu-id="f600f-318">Kod değişikliği işledikten sonra geri yönlendirir *filmler* sayfası.</span><span class="sxs-lookup"><span data-stu-id="f600f-318">After the code processes the change, it redirects back to the *Movies* page.</span></span>

<span data-ttu-id="f600f-319">Ancak:</span><span class="sxs-lookup"><span data-stu-id="f600f-319">However:</span></span>

- <span data-ttu-id="f600f-320">Kullanıcı herhangi bir şey değiştirmemeye karar verebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="f600f-320">The user might decide not to change anything.</span></span>
- <span data-ttu-id="f600f-321">Kullanıcının bu sayfaya ilk tıklamadan edindiğiniz bir **Düzenle** bağlantısını *filmler* sayfası.</span><span class="sxs-lookup"><span data-stu-id="f600f-321">The user might have gotten to this page without first clicking an **Edit** link in the *Movies* page.</span></span>

<span data-ttu-id="f600f-322">Her iki durumda da, bunları ana listesi için döndürülecek kolaylaştırmak istediğiniz.</span><span class="sxs-lookup"><span data-stu-id="f600f-322">Either way, you want to make it easy for them to return to the main listing.</span></span> <span data-ttu-id="f600f-323">Çözümü kolay olan &mdash; kapattıktan sonra aşağıdaki işaretlemeyi ekleyin `</form>` biçimlendirmede etiketi:</span><span class="sxs-lookup"><span data-stu-id="f600f-323">It's an easy fix &mdash; add the following markup just after the closing `</form>` tag in the markup:</span></span>

[!code-html[Main](updating-data/samples/sample19.html)]

<span data-ttu-id="f600f-324">Bu işaretleme aynı sözdizimini kullanan bir `<a>` başka bir yerde gördüğünüz öğesi.</span><span class="sxs-lookup"><span data-stu-id="f600f-324">This markup uses the same syntax for an `<a>` element that you've seen elsewhere.</span></span> <span data-ttu-id="f600f-325">URL içerir `~` auto'yu "Web sitesinin kök."</span><span class="sxs-lookup"><span data-stu-id="f600f-325">The URL includes `~` to mean "root of the website."</span></span>

## <a name="testing-the-movie-update-process"></a><span data-ttu-id="f600f-326">Film güncelleştirme işlemi test etme</span><span class="sxs-lookup"><span data-stu-id="f600f-326">Testing the Movie Update Process</span></span>

<span data-ttu-id="f600f-327">Artık test edebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="f600f-327">Now you can test.</span></span> <span data-ttu-id="f600f-328">Çalıştırma *filmler* sayfasında ve tıklayın **Düzenle** film yanında.</span><span class="sxs-lookup"><span data-stu-id="f600f-328">Run the *Movies* page, and click **Edit** next to a movie.</span></span> <span data-ttu-id="f600f-329">Zaman *EditMovie* sayfası görüntülenirse, film ve seçeneğini değişiklik **değişiklikleri gönderme**.</span><span class="sxs-lookup"><span data-stu-id="f600f-329">When the *EditMovie* page appears, make changes to the movie and click **Submit Changes**.</span></span> <span data-ttu-id="f600f-330">Film listesi göründüğünde, değişikliklerinizi gösterildiğinden emin olun.</span><span class="sxs-lookup"><span data-stu-id="f600f-330">When the movie listing appears, make sure that your changes are shown.</span></span>

<span data-ttu-id="f600f-331">Doğrulama çalıştığından emin olmak için tıklayın **Düzenle** başka bir filmi için.</span><span class="sxs-lookup"><span data-stu-id="f600f-331">To make sure that validation is working, click **Edit** for another movie.</span></span> <span data-ttu-id="f600f-332">Ulaştığınızda *EditMovie* sayfasında, NET **Tarz** alan (veya **yıl** alan veya her ikisi de) ve yaptığınız değişiklikleri göndermek deneyin.</span><span class="sxs-lookup"><span data-stu-id="f600f-332">When you get to the *EditMovie* page, clear the **Genre** field (or **Year** field, or both) and try to submit your changes.</span></span> <span data-ttu-id="f600f-333">Beklediğiniz gibi bir hata görürsünüz:</span><span class="sxs-lookup"><span data-stu-id="f600f-333">You'll see an error, as you'd expect:</span></span>

![Doğrulama hatalarını gösteren film sayfayı Düzenle](updating-data/_static/image4.png)

<span data-ttu-id="f600f-335">Tıklayın **dönmek için film liste** değişikliklerinizden vazgeçmek ve dönmek için bağlantı *filmler* sayfası.</span><span class="sxs-lookup"><span data-stu-id="f600f-335">Click the **Return to movie listing** link to abandon your changes and return to the *Movies* page.</span></span>

## <a name="coming-up-next"></a><span data-ttu-id="f600f-336">Sıradaki gelen</span><span class="sxs-lookup"><span data-stu-id="f600f-336">Coming Up Next</span></span>

<span data-ttu-id="f600f-337">Sonraki öğreticide, bir film kayıt silme görürsünüz.</span><span class="sxs-lookup"><span data-stu-id="f600f-337">In the next tutorial, you'll see how to delete a movie record.</span></span>

## <a name="complete-listing-for-movie-page-updated-with-edit-links"></a><span data-ttu-id="f600f-338">Tam listesi için (düzenleme bağlantıları ile güncelleştirilmiş) film sayfası</span><span class="sxs-lookup"><span data-stu-id="f600f-338">Complete Listing for Movie Page (Updated with Edit Links)</span></span>

[!code-cshtml[Main](updating-data/samples/sample20.cshtml)]

<a id="Complete_Page_Listing_for_EditMovie"></a>
## <a name="complete-page-listing-for-edit-movie-page"></a><span data-ttu-id="f600f-339">Tam düzen film sayfa için sayfa listesi</span><span class="sxs-lookup"><span data-stu-id="f600f-339">Complete Page Listing for Edit Movie Page</span></span>

[!code-cshtml[Main](updating-data/samples/sample21.cshtml)]

## <a name="additional-resources"></a><span data-ttu-id="f600f-340">Ek Kaynaklar</span><span class="sxs-lookup"><span data-stu-id="f600f-340">Additional Resources</span></span>

- [<span data-ttu-id="f600f-341">ASP.NET Web programlama Razor söz dizimini kullanarak giriş</span><span class="sxs-lookup"><span data-stu-id="f600f-341">Introduction to ASP.NET Web Programming by Using the Razor Syntax</span></span>](../../getting-started/introducing-razor-syntax-c.md)
- <span data-ttu-id="f600f-342">[SQL güncelleştirme bildirimi](http://www.w3schools.com/sql/sql_update.asp) W3Schools sitesinde</span><span class="sxs-lookup"><span data-stu-id="f600f-342">[SQL UPDATE Statement](http://www.w3schools.com/sql/sql_update.asp) on the W3Schools site</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="f600f-343">[Önceki](entering-data.md)
> [İleri](deleting-data.md)</span><span class="sxs-lookup"><span data-stu-id="f600f-343">[Previous](entering-data.md)
[Next](deleting-data.md)</span></span>
