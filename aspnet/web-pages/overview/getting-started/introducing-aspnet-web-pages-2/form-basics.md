---
uid: web-pages/overview/getting-started/introducing-aspnet-web-pages-2/form-basics
title: ASP.NET Web sayfaları - HTML formuyla ilgili temel kavramlar ile tanışın | Microsoft Docs
author: Rick-Anderson
description: Bu öğreticide, Giriş bir formun nasıl oluşturulacağını ve ASP.NET Web sayfaları (Razor) kullandığınızda, kullanıcının giriş işlemek nasıl temelleri gösterilir. Ve şimdi...
ms.author: riande
ms.date: 05/28/2015
ms.assetid: 81ed82bf-b940-44f1-b94a-555d0cb7cc98
msc.legacyurl: /web-pages/overview/getting-started/introducing-aspnet-web-pages-2/form-basics
msc.type: authoredcontent
ms.openlocfilehash: f88f7a31551abda029bee0ec16aa35ce2ef5d2f0
ms.sourcegitcommit: 0f1119340e4464720cfd16d0ff15764746ea1fea
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 04/09/2019
ms.locfileid: "59385962"
---
# <a name="introducing-aspnet-web-pages---html-form-basics"></a><span data-ttu-id="16a2c-104">ASP.NET Web sayfaları - HTML formuyla ilgili temel kavramlar ile tanışın</span><span class="sxs-lookup"><span data-stu-id="16a2c-104">Introducing ASP.NET Web Pages - HTML Form Basics</span></span>

<span data-ttu-id="16a2c-105">tarafından [Tom FitzMacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="16a2c-105">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="16a2c-106">Bu öğreticide, Giriş bir formun nasıl oluşturulacağını ve ASP.NET Web sayfaları (Razor) kullandığınızda, kullanıcının giriş işlemek nasıl temelleri gösterilir.</span><span class="sxs-lookup"><span data-stu-id="16a2c-106">This tutorial shows you the basics of how to create an input form and how to handle the user's input when you use ASP.NET Web Pages (Razor).</span></span> <span data-ttu-id="16a2c-107">Ve bir veritabanı kendinizi, veritabanında belirli filmler bulmalarına olanak form becerilerinizi kullanacaksınız.</span><span class="sxs-lookup"><span data-stu-id="16a2c-107">And now that you've got a database, you'll use your form skills to let users find specific movies in the database.</span></span> <span data-ttu-id="16a2c-108">Bu seriyi aracılığıyla bitirdiğinizi [görüntüleme veri kullanarak ASP.NET Web sayfalarına giriş](/aspnet/web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data).</span><span class="sxs-lookup"><span data-stu-id="16a2c-108">It assumes you have completed the series through [Introduction to Displaying Data Using ASP.NET Web Pages](/aspnet/web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data).</span></span>
> 
> <span data-ttu-id="16a2c-109">Öğrenecekleriniz:</span><span class="sxs-lookup"><span data-stu-id="16a2c-109">What you'll learn:</span></span>
> 
> - <span data-ttu-id="16a2c-110">Standart HTML öğeleri kullanılarak, bir form oluşturma</span><span class="sxs-lookup"><span data-stu-id="16a2c-110">How to create a form by using standard HTML elements.</span></span>
> - <span data-ttu-id="16a2c-111">Ndaki bir forma kullanıcı okuma giriş.</span><span class="sxs-lookup"><span data-stu-id="16a2c-111">How to read the user's input in a form.</span></span>
> - <span data-ttu-id="16a2c-112">Seçerek bir arama'yı kullanarak verileri alır, kullanıcı terimi, bir SQL sorgusu oluşturma sağlar.</span><span class="sxs-lookup"><span data-stu-id="16a2c-112">How to create a SQL query that selectively gets data by using a search term that the user supplies.</span></span>
> - <span data-ttu-id="16a2c-113">Sayfasında "ne bir kullanıcı girilen unutmayın" alanların nasıl.</span><span class="sxs-lookup"><span data-stu-id="16a2c-113">How to have fields in the page "remember" what the user entered.</span></span>
>   
> 
> <span data-ttu-id="16a2c-114">Ele alınan özelliklerin/teknolojiler:</span><span class="sxs-lookup"><span data-stu-id="16a2c-114">Features/technologies discussed:</span></span>
> 
> - <span data-ttu-id="16a2c-115">`Request` Nesne.</span><span class="sxs-lookup"><span data-stu-id="16a2c-115">The `Request` object.</span></span>
> - <span data-ttu-id="16a2c-116">SQL `Where` yan tümcesi.</span><span class="sxs-lookup"><span data-stu-id="16a2c-116">The SQL `Where` clause.</span></span>


## <a name="what-youll-build"></a><span data-ttu-id="16a2c-117">Ne oluşturacaksınız</span><span class="sxs-lookup"><span data-stu-id="16a2c-117">What You'll Build</span></span>

<span data-ttu-id="16a2c-118">Önceki öğreticide, bir veritabanı oluşturdunuz, veri eklendiğinde ve ardından kullanılan `WebGrid` verileri görüntülemek için yardımcı.</span><span class="sxs-lookup"><span data-stu-id="16a2c-118">In the previous tutorial, you created a database, added data to it, and then used the `WebGrid` helper to display the data.</span></span> <span data-ttu-id="16a2c-119">Bu öğreticide, ayarlanmış başlık girdiğiniz herhangi bir sözcük içeriyor ya da belirli bir türe, filmler bulmanıza olanak tanıyan bir arama kutusu ekleyeceksiniz.</span><span class="sxs-lookup"><span data-stu-id="16a2c-119">In this tutorial, you'll add a search box that lets you find movies of a specific genre or whose title contains whatever word you enter.</span></span> <span data-ttu-id="16a2c-120">(Örneğin, sizin tarzı "Action" veya "Harry" veya "Adventure" ayarlanmış başlık içerdiği tüm filmlere bulmak mümkün olacaktır)</span><span class="sxs-lookup"><span data-stu-id="16a2c-120">(For example, you'll be able to find all movies whose genre is "Action" or whose title contains "Harry" or "Adventure.")</span></span>

<span data-ttu-id="16a2c-121">Bu öğretici ile işiniz bittiğinde bunun gibi bir sayfa gerekir:</span><span class="sxs-lookup"><span data-stu-id="16a2c-121">When you're done with this tutorial, you'll have a page like this one:</span></span>

![Türe ve başlık araması filmler sayfası](form-basics/_static/image1.png)

<span data-ttu-id="16a2c-123">Sayfa listesi parçası son öğretici ile aynı olan &mdash; bir kılavuz.</span><span class="sxs-lookup"><span data-stu-id="16a2c-123">The listing part of the page is the same as in the last tutorial &mdash; a grid.</span></span> <span data-ttu-id="16a2c-124">Fark, kılavuz için Aranan yalnızca filmler göstereceğini olacaktır.</span><span class="sxs-lookup"><span data-stu-id="16a2c-124">The difference will be that the grid will show only the movies that you searched for.</span></span>

## <a name="about-html-forms"></a><span data-ttu-id="16a2c-125">HTML formları hakkında</span><span class="sxs-lookup"><span data-stu-id="16a2c-125">About HTML Forms</span></span>

<span data-ttu-id="16a2c-126">(Deneyim arasındaki farkı ve HTML form oluşturma ile kendinizi varsa `GET` ve `POST`, bu bölümü atlayabilirsiniz.)</span><span class="sxs-lookup"><span data-stu-id="16a2c-126">(If you've got experience with creating HTML forms and with the difference between `GET` and `POST`, you can skip this section.)</span></span>

<span data-ttu-id="16a2c-127">Kullanıcı girişi öğelerinin bir biçimde &mdash; metin kutularını, düğmeleri, radyo düğmeleri, onay kutuları, açılan listeler ve benzeri.</span><span class="sxs-lookup"><span data-stu-id="16a2c-127">A form has user input elements &mdash; text boxes, buttons, radio buttons, check boxes, drop-down lists, and so on.</span></span> <span data-ttu-id="16a2c-128">Kullanıcılar bu denetimleri doldurun veya seçimleri yapın ve ardından bir düğmeye tıklayarak form gönderilemiyor.</span><span class="sxs-lookup"><span data-stu-id="16a2c-128">Users fill in these controls or make selections and then submit the form by clicking a button.</span></span>

<span data-ttu-id="16a2c-129">Bir form temel HTML sözdizimini Bu örnekle gösterilmiştir:</span><span class="sxs-lookup"><span data-stu-id="16a2c-129">The basic HTML syntax of a form is illustrated by this example:</span></span>

[!code-html[Main](form-basics/samples/sample1.html)]

<span data-ttu-id="16a2c-130">Bu işaretleme, bir sayfa çalıştığında, bu çizim gibi görünüyor, basit bir form oluşturur:</span><span class="sxs-lookup"><span data-stu-id="16a2c-130">When this markup runs in a page, it creates a simple form that looks like this illustration:</span></span>

![Tarayıcıda işlenmiş olarak temel HTML formu](form-basics/_static/image2.png)

<span data-ttu-id="16a2c-132">`<form>` Öğesi gönderilecek HTML öğeleri alır.</span><span class="sxs-lookup"><span data-stu-id="16a2c-132">The `<form>` element encloses HTML elements to be submitted.</span></span> <span data-ttu-id="16a2c-133">(Öğeleri sayfaya ekleyin, ancak bunları içine yerleştirebilirsiniz unuttunuz hale getirmek için kolay bir hata olduğu bir `<form>` öğesi.</span><span class="sxs-lookup"><span data-stu-id="16a2c-133">(An easy mistake to make is to add elements to the page but then forget to put them inside a `<form>` element.</span></span> <span data-ttu-id="16a2c-134">Bu durumda hiçbir şey gönderilir.) `method` Özniteliği, kullanıcı girişini göndermek nasıl tarayıcı söyler.</span><span class="sxs-lookup"><span data-stu-id="16a2c-134">In that case, nothing is submitted.) The `method` attribute tells the browser how to submit the user input.</span></span> <span data-ttu-id="16a2c-135">Bu ayar `post` bir güncelleştirme sunucusunda veya çok gerçekleştiriyorsanız `get` yalnızca sunucudan verilerin getirilmesi durumunda.</span><span class="sxs-lookup"><span data-stu-id="16a2c-135">You set this to `post` if you're performing an update on the server or to `get` if you're just fetching data from the server.</span></span>

<a id="GET,_POST,_and_HTTP_Verb_Safety"></a>

> [!TIP] 
> 
> **<span data-ttu-id="16a2c-136">GET, POST ve HTTP fiili güvenliği</span><span class="sxs-lookup"><span data-stu-id="16a2c-136">GET, POST, and HTTP Verb Safety</span></span>**
> 
> <span data-ttu-id="16a2c-137">HTTP, tarayıcılar ve sunucuları hakkında bilgi alışverişi yapmak için kullandığı protokole temel işlemleri çok kolaydır.</span><span class="sxs-lookup"><span data-stu-id="16a2c-137">HTTP, the protocol that browsers and servers use to exchange information, is remarkably simple in its basic operations.</span></span> <span data-ttu-id="16a2c-138">Tarayıcılar, sunucuya isteklerinde bulunmak için yalnızca birkaç fiilleri kullanın.</span><span class="sxs-lookup"><span data-stu-id="16a2c-138">Browsers use only a few verbs to make requests to servers.</span></span> <span data-ttu-id="16a2c-139">Web için kod yazdığınızda, bu fiilleri ve bunları nasıl tarayıcı ve sunucu kullanılacağını anlamak yararlıdır.</span><span class="sxs-lookup"><span data-stu-id="16a2c-139">When you write code for the web, it's helpful to understand these verbs and how the browser and server use them.</span></span> <span data-ttu-id="16a2c-140">En sık kullanılan fiilleri bunlar ve uzakta:</span><span class="sxs-lookup"><span data-stu-id="16a2c-140">Far and away the most commonly used verbs are these:</span></span>
> 
> - `GET`<span data-ttu-id="16a2c-141">biçimindeki telefon numarasıdır.</span><span class="sxs-lookup"><span data-stu-id="16a2c-141">.</span></span> <span data-ttu-id="16a2c-142">Tarayıcı, bir sunucudan almak için bu fiil kullanır.</span><span class="sxs-lookup"><span data-stu-id="16a2c-142">The browser uses this verb to fetch something from the server.</span></span> <span data-ttu-id="16a2c-143">Örneğin, bir URL'sini tarayıcınıza yazın, tarayıcı gerçekleştirir bir `GET` işlemi istediğiniz sayfa istemek için.</span><span class="sxs-lookup"><span data-stu-id="16a2c-143">For example, when you type a URL into your browser, the browser performs a `GET` operation to request the page you want.</span></span> <span data-ttu-id="16a2c-144">Grafik sayfası içeriyorsa, tarayıcı ek gerçekleştirir `GET` görüntüleri almak için operations.</span><span class="sxs-lookup"><span data-stu-id="16a2c-144">If the page includes graphics, the browser performs additional `GET` operations to get the images.</span></span> <span data-ttu-id="16a2c-145">Varsa `GET` işlemine sahip bilgilerini sunucuya geçirmek, bilgileri sorgu dizesini URL'ye bir parçası olarak geçirilir.</span><span class="sxs-lookup"><span data-stu-id="16a2c-145">If the `GET` operation has to pass information to the server, the information is passed as part of the URL in the query string.</span></span>
> - `POST`<span data-ttu-id="16a2c-146">biçimindeki telefon numarasıdır.</span><span class="sxs-lookup"><span data-stu-id="16a2c-146">.</span></span> <span data-ttu-id="16a2c-147">Tarayıcı gönderen bir `POST` eklenebilir veya sunucu üzerinde değiştirilmiş verileri göndermek için isteği.</span><span class="sxs-lookup"><span data-stu-id="16a2c-147">The browser sends a `POST` request in order to submit data to be added or changed on the server.</span></span> <span data-ttu-id="16a2c-148">Örneğin, `POST` fiil, veritabanı kaydı oluşturma veya var olanları değiştirmek için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="16a2c-148">For example, the `POST` verb is used to create records in a database or change existing ones.</span></span> <span data-ttu-id="16a2c-149">Çoğu zaman, ne zaman bir formu doldurun ve Gönder düğmesini tıklatın, tarayıcı gerçekleştiren bir `POST` işlemi.</span><span class="sxs-lookup"><span data-stu-id="16a2c-149">Most of the time, when you fill in a form and click the submit button, the browser performs a `POST` operation.</span></span> <span data-ttu-id="16a2c-150">İçinde bir `POST` işlemi, sunucuya geçirilen veriler, sayfanın gövdesi içinde.</span><span class="sxs-lookup"><span data-stu-id="16a2c-150">In a `POST` operation, the data being passed to the server is in the body of the page.</span></span>
> 
> <span data-ttu-id="16a2c-151">Olan bu eylemler arasında önemli bir ayrımdır bir `GET` işlemi sunucu üzerindeki herhangi bir ayarı değiştirmek için beklenen değildir — veya biraz daha soyut bir şekilde koymak için bir `GET` işlemi sunucunun durumunda bir değişiklik neden olmaz.</span><span class="sxs-lookup"><span data-stu-id="16a2c-151">An important distinction between these verbs is that a `GET` operation is not supposed to change anything on the server — or to put it in a slightly more abstract way, a `GET` operation does not result in a change in state on the server.</span></span> <span data-ttu-id="16a2c-152">Gerçekleştirebileceğiniz bir `GET` istediğiniz ve bu kaynakları değiştirme gibi birden çok kez aynı kaynaklara işlemi.</span><span class="sxs-lookup"><span data-stu-id="16a2c-152">You can perform a `GET` operation on the same resources as many times as you like, and those resources don't change.</span></span> <span data-ttu-id="16a2c-153">(A `GET` işlemi genellikle söylediğiniz "güvenli" ya da teknik bir terim kullanılacak olan *ıdempotent*.) Buna karşılık, doğal olarak, bir `POST` istek sunucusundaki bir şey işlemi gerçekleştirdiğiniz her zaman değiştirir.</span><span class="sxs-lookup"><span data-stu-id="16a2c-153">(A `GET` operation is often said to be "safe," or to use a technical term, is *idempotent*.) In contrast, of course, a `POST` request changes something on the server each time you perform the operation.</span></span>
> 
> <span data-ttu-id="16a2c-154">İki örnek Bu ayrım göstermeye yardımcı olur.</span><span class="sxs-lookup"><span data-stu-id="16a2c-154">Two examples will help illustrate this distinction.</span></span> <span data-ttu-id="16a2c-155">Bing veya Google gibi bir altyapı kullanarak bir arama yaparken oluşan bir metin kutusundan bir formu doldurun ve ardından, arama düğmesine tıklayın.</span><span class="sxs-lookup"><span data-stu-id="16a2c-155">When you perform a search using an engine like Bing or Google, you fill in a form that consists of one text box, and then you click the search button.</span></span> <span data-ttu-id="16a2c-156">Tarayıcı gerçekleştiren bir `GET` işlemle URL'nin bir bölümü olarak geçirilen kutusuna girdiğiniz değer.</span><span class="sxs-lookup"><span data-stu-id="16a2c-156">The browser performs a `GET` operation, with the value you entered into the box passed as part of the URL.</span></span> <span data-ttu-id="16a2c-157">Kullanarak bir `GET` işlemi sunucudaki tüm kaynakların bir arama işlemi değişmediğinden bu tür bir form uygundur için bu yalnızca bilgi getirir.</span><span class="sxs-lookup"><span data-stu-id="16a2c-157">Using a `GET` operation for this type of form is fine, because a search operation doesn't change any resources on the server, it just fetches information.</span></span>
> 
> <span data-ttu-id="16a2c-158">Şimdi, çevrimiçi bir şey sıralama işlemini göz önünde bulundurun.</span><span class="sxs-lookup"><span data-stu-id="16a2c-158">Now consider the process of ordering something online.</span></span> <span data-ttu-id="16a2c-159">Sipariş ayrıntıları girin ve ardından Gönder düğmesine tıklayın.</span><span class="sxs-lookup"><span data-stu-id="16a2c-159">You fill in the order details and then click the submit button.</span></span> <span data-ttu-id="16a2c-160">Bu işlem bir `POST` istek işlemi sunucusunda yeni bir sipariş kaydı, bir değişiklik hesap bilgilerinizi ve belki de diğer birçok değişikliği gibi değişiklikleri neden.</span><span class="sxs-lookup"><span data-stu-id="16a2c-160">This operation will be a `POST` request, because the operation will result in changes on the server, such as a new order record, a change in your account information, and perhaps many other changes.</span></span> <span data-ttu-id="16a2c-161">Farklı `GET` işlemi yinelenemez, `POST` isteği — oluşturmadıysanız, isteği yeniden gönderildi her zaman yeni bir sipariş sunucuda karşılaşırsınız.</span><span class="sxs-lookup"><span data-stu-id="16a2c-161">Unlike the `GET` operation, you cannot repeat your `POST` request — if you did, each time you resubmitted the request, you'd generate a new order on the server.</span></span> <span data-ttu-id="16a2c-162">(Bu gibi durumlarda, Web siteleri genellikle bir gönderme düğmesi birden çok kez'i sizi uyarır ve böylece form yanlışlıkla yeniden yoksa Gönder düğmesini devre dışı bırakır.)</span><span class="sxs-lookup"><span data-stu-id="16a2c-162">(In cases like this, websites will often warn you not to click a submit button more than once, or will disable the submit button so that you don't resubmit the form accidentally.)</span></span>
> 
> <span data-ttu-id="16a2c-163">Bu öğretici sırasında her ikisi de kullanacağınız bir `GET` işlemi ve `POST` HTML formlarla çalışmak için işlemi.</span><span class="sxs-lookup"><span data-stu-id="16a2c-163">In the course of this tutorial, you'll use both a `GET` operation and a `POST` operation to work with HTML forms.</span></span> <span data-ttu-id="16a2c-164">Her durum kullandığınız fiili uygun olanı İşte açıklayacağız.</span><span class="sxs-lookup"><span data-stu-id="16a2c-164">We'll explain in each case why the verb you use is the appropriate one.</span></span>
> 
> <span data-ttu-id="16a2c-165">(HTTP fiilleri hakkında daha fazla bilgi için bkz: [yöntemi tanımları](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) W3C sitesinde makale.)</span><span class="sxs-lookup"><span data-stu-id="16a2c-165">(To learn more about HTTP verbs, see the [Method Definitions](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) article on the W3C site.)</span></span>


<span data-ttu-id="16a2c-166">Çoğu kullanıcı girişi öğelerinin HTML'dir `<input>` öğeleri.</span><span class="sxs-lookup"><span data-stu-id="16a2c-166">Most user input elements are HTML `<input>` elements.</span></span> <span data-ttu-id="16a2c-167">Gibi görünürler `<input type="type" name="name">,` burada *türü* istediğiniz kullanıcı giriş denetim türünü belirtir.</span><span class="sxs-lookup"><span data-stu-id="16a2c-167">They look like `<input type="type" name="name">,` where *type* indicates the kind of user input control you want.</span></span> <span data-ttu-id="16a2c-168">Sık Kullanılanlardan bu öğeler şunlardır:</span><span class="sxs-lookup"><span data-stu-id="16a2c-168">These elements are the common ones:</span></span>

- <span data-ttu-id="16a2c-169">Metin kutusu:</span><span class="sxs-lookup"><span data-stu-id="16a2c-169">Text box:</span></span> `<input type="text">`
- <span data-ttu-id="16a2c-170">Onay kutusu:</span><span class="sxs-lookup"><span data-stu-id="16a2c-170">Check box:</span></span> `<input type="check">`
- <span data-ttu-id="16a2c-171">Radyo düğmesi:</span><span class="sxs-lookup"><span data-stu-id="16a2c-171">Radio button:</span></span> `<input type="radio">`
- <span data-ttu-id="16a2c-172">Düğme:</span><span class="sxs-lookup"><span data-stu-id="16a2c-172">Button:</span></span> `<input type="button">`
- <span data-ttu-id="16a2c-173">Gönder düğmesi:</span><span class="sxs-lookup"><span data-stu-id="16a2c-173">Submit button:</span></span> `<input type="submit">`

<span data-ttu-id="16a2c-174">Ayrıca `<textarea>` öğesi bir çok satırlı metin kutusu oluşturma ve `<select>` öğesi bir açılan liste veya kaydırılabilir bir liste oluşturmak için.</span><span class="sxs-lookup"><span data-stu-id="16a2c-174">You can also use the `<textarea>` element to create a multiline text box and the `<select>` element to create a drop-down list or scrollable list.</span></span> <span data-ttu-id="16a2c-175">(HTML hakkında daha fazla öğeleri oluşturmak için bkz: [HTML formları ve giriş](http://www.w3schools.com/html/html_forms.asp) W3Schools sitesinde.)</span><span class="sxs-lookup"><span data-stu-id="16a2c-175">(For more about HTML form elements, see [HTML Forms and Input](http://www.w3schools.com/html/html_forms.asp) on the W3Schools site.)</span></span>

<span data-ttu-id="16a2c-176">`name` Kısa süre içinde anlatıldığı gibi daha sonra öğenin değeri nasıl elde edecekleriniz adı olduğu için öznitelik çok önemlidir.</span><span class="sxs-lookup"><span data-stu-id="16a2c-176">The `name` attribute is very important, because the name is how you'll get the value of the element later, as you'll see shortly.</span></span>

<span data-ttu-id="16a2c-177">Kullanıcının girişi ile neler sayfasında geliştirici olarak size ilginç parçasıdır.</span><span class="sxs-lookup"><span data-stu-id="16a2c-177">The interesting part is what you, the page developer, do with the user's input.</span></span> <span data-ttu-id="16a2c-178">Bu öğelerle ilişkili hiçbir yerleşik davranış yoktur.</span><span class="sxs-lookup"><span data-stu-id="16a2c-178">There's no built-in behavior associated with these elements.</span></span> <span data-ttu-id="16a2c-179">Bunun yerine, kullanıcı girilebilen veya değerlerini alın ve bunlarla bir şey gerekir.</span><span class="sxs-lookup"><span data-stu-id="16a2c-179">Instead, you have to get the values that the user has entered or selected and do something with them.</span></span> <span data-ttu-id="16a2c-180">Bu öğreticide öğrenecekleriniz olmasıdır.</span><span class="sxs-lookup"><span data-stu-id="16a2c-180">That's what you'll learn in this tutorial.</span></span>

> [!TIP] 
> 
> **<span data-ttu-id="16a2c-181">HTML5 ve girdi biçimleri</span><span class="sxs-lookup"><span data-stu-id="16a2c-181">HTML5 and Input Forms</span></span>**
> 
> <span data-ttu-id="16a2c-182">Biliyor olabilirsiniz gibi HTML geçiş durumunda ve en son sürümünü (HTML5) bilgi girmek kullanıcılar için daha sezgisel yolu için destek içerir.</span><span class="sxs-lookup"><span data-stu-id="16a2c-182">As you might know, HTML is in transition and the latest version (HTML5) includes support for more intuitive ways for users to enter information.</span></span> <span data-ttu-id="16a2c-183">Örneğin, HTML5'te istediğiniz kullanıcının bir tarih girin (sayfa Geliştirici), sayfa söyleyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="16a2c-183">For example, in HTML5, you (the page developer) can tell the page that you want the user to enter a date.</span></span> <span data-ttu-id="16a2c-184">Tarayıcı daha sonra otomatik olarak bir tarih el ile girmek kullanıcının gerektirmek yerine bir takvim görüntüleyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="16a2c-184">The browser can then automatically display a calendar rather than requiring the user to enter a date manually.</span></span> <span data-ttu-id="16a2c-185">Ancak, HTML5, yenilikler ve tüm tarayıcılarda henüz desteklenmiyor.</span><span class="sxs-lookup"><span data-stu-id="16a2c-185">However, HTML5 is new and is not supported in all browsers yet.</span></span>
> 
> <span data-ttu-id="16a2c-186">ASP.NET Web sayfaları, kullanıcının tarayıcı mu için toplasa bile, giriş HTML5 destekler.</span><span class="sxs-lookup"><span data-stu-id="16a2c-186">ASP.NET Web Pages supports HTML5 input to the extent that the user's browser does.</span></span> <span data-ttu-id="16a2c-187">Yeni öznitelikler için hakkında bir fikir için `<input>` HTML5, öğesinin [HTML &lt;giriş&gt; öznitelik türü](http://www.w3schools.com/html/html_form_input_types.asp) W3Schools sitesinde.</span><span class="sxs-lookup"><span data-stu-id="16a2c-187">For an idea of the new attributes for the `<input>` element in HTML5, see [HTML &lt;input&gt; type Attribute](http://www.w3schools.com/html/html_form_input_types.asp) on the W3Schools site.</span></span>


## <a name="creating-the-form"></a><span data-ttu-id="16a2c-188">Form Oluşturma</span><span class="sxs-lookup"><span data-stu-id="16a2c-188">Creating the Form</span></span>

<span data-ttu-id="16a2c-189">Webmatrix'te, içinde **dosyaları** çalışma alanında, açık *Movies.cshtml* sayfası.</span><span class="sxs-lookup"><span data-stu-id="16a2c-189">In WebMatrix, in the **Files** workspace, open the *Movies.cshtml* page.</span></span>

<span data-ttu-id="16a2c-190">Kapatma sonrasında `</h1>` etiketi ve açmadan önce `<div>` etiketi `grid.GetHtml` çağrı, aşağıdaki işaretlemeyi ekleyin:</span><span class="sxs-lookup"><span data-stu-id="16a2c-190">After the closing `</h1>` tag and before the opening `<div>` tag of the `grid.GetHtml` call, add the following markup:</span></span>

[!code-html[Main](form-basics/samples/sample2.html)]

<span data-ttu-id="16a2c-191">Bu işaretleme adlı bir metin kutusu olan bir form oluşturur `searchGenre` ve bir Gönder düğmesi.</span><span class="sxs-lookup"><span data-stu-id="16a2c-191">This markup creates a form that has a text box named `searchGenre` and a submit button.</span></span> <span data-ttu-id="16a2c-192">Metin kutusu ve düğme içine alınmıştır göndermek bir `<form>` öğesi olan `method` özniteliği `get`.</span><span class="sxs-lookup"><span data-stu-id="16a2c-192">The text box and submit button are enclosed in a `<form>` element whose `method` attribute is set to `get`.</span></span> <span data-ttu-id="16a2c-193">(Metin kutusu yoksa ve Gönder düğmesi içinde unutmayın bir `<form>` öğesi, düğmeye tıkladığınızda bir şey gönderilir.) Kullandığınız `GET` fiili form oluşturduğumuzdan Burada, sunucu üzerinde değişiklik değil; yalnızca bir arama sonuçları.</span><span class="sxs-lookup"><span data-stu-id="16a2c-193">(Remember that if you don't put the text box and submit button inside a `<form>` element, nothing will be submitted when you click the button.) You use the `GET` verb here because you're creating a form that does not make any changes on the server — it just results in a search.</span></span> <span data-ttu-id="16a2c-194">(Önceki öğreticide kullandığınız bir `post` değişiklikleri sunucuya nasıl gönderdiğiniz olan yöntem.</span><span class="sxs-lookup"><span data-stu-id="16a2c-194">(In the previous tutorial, you used a `post` method, which is how you submit changes to the server.</span></span> <span data-ttu-id="16a2c-195">Sonraki öğreticide yeniden görürsünüz.)</span><span class="sxs-lookup"><span data-stu-id="16a2c-195">You'll see that in the next tutorial again.)</span></span>

<span data-ttu-id="16a2c-196">Sayfayı çalıştırın.</span><span class="sxs-lookup"><span data-stu-id="16a2c-196">Run the page.</span></span> <span data-ttu-id="16a2c-197">Form için herhangi bir davranış tanımladıysanız olsa da, nasıl göründüğünü görebilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="16a2c-197">Although you haven't defined any behavior for the form, you can see what it looks like:</span></span>

![Arama kutusunu Tarz filmler sayfası](form-basics/_static/image3.png)

<span data-ttu-id="16a2c-199">"Komedi." gibi bir metin kutusuna bir değer girin</span><span class="sxs-lookup"><span data-stu-id="16a2c-199">Enter a value into the text box, like "Comedy."</span></span> <span data-ttu-id="16a2c-200">Ardından **arama Tarz**.</span><span class="sxs-lookup"><span data-stu-id="16a2c-200">Then click **Search Genre**.</span></span>

<span data-ttu-id="16a2c-201">Sayfanın URL'sini not alın.</span><span class="sxs-lookup"><span data-stu-id="16a2c-201">Take note of the URL of the page.</span></span> <span data-ttu-id="16a2c-202">Ayarladığınız çünkü `<form>` öğenin `method` özniteliğini `get`, girdiğiniz değer artık böyle bir URL'ye sorgu dizesi parçasıdır:</span><span class="sxs-lookup"><span data-stu-id="16a2c-202">Because you set the `<form>` element's `method` attribute to `get`, the value you entered is now part of the query string in the URL, like this:</span></span>

`http://localhost:45661/Movies.cshtml?searchGenre=Comedy`

## <a name="reading-form-values"></a><span data-ttu-id="16a2c-203">Okuma Form değerleri</span><span class="sxs-lookup"><span data-stu-id="16a2c-203">Reading Form Values</span></span>

<span data-ttu-id="16a2c-204">Veritabanı verilerini alır ve sonuçları bir kılavuzda görüntüleyen bazı kod sayfası zaten var.</span><span class="sxs-lookup"><span data-stu-id="16a2c-204">The page already contains some code that gets database data and displays the results in a grid.</span></span> <span data-ttu-id="16a2c-205">Artık arama terimini içeren bir SQL sorgusu çalıştırabilmeniz için metin kutusunun değerini okur bazı kod eklemeniz gerekir.</span><span class="sxs-lookup"><span data-stu-id="16a2c-205">Now you have to add some code that reads the value of the text box so you can run a SQL query that includes the search term.</span></span>

<span data-ttu-id="16a2c-206">Formun yöntemi kümesine çünkü `get`, aşağıdaki gibi kod kullanarak metin kutusuna girilen değerin edinebilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="16a2c-206">Because you set the form's method to `get`, you can read the value that was entered into the text box by using code like the following:</span></span>

`var searchTerm = Request.QueryString["searchGenre"];`

<span data-ttu-id="16a2c-207">`Request.QueryString` Nesne ( `QueryString` özelliği `Request` nesnesi) bir parçası olarak gönderilen öğe değerlerini içeren `GET` işlemi.</span><span class="sxs-lookup"><span data-stu-id="16a2c-207">The `Request.QueryString` object (the `QueryString` property of the `Request` object) includes the values of elements that were submitted as part of the `GET` operation.</span></span> <span data-ttu-id="16a2c-208">`Request.QueryString` Özelliği içeren bir *koleksiyon* (liste) biçiminde gönderilen değerler.</span><span class="sxs-lookup"><span data-stu-id="16a2c-208">The `Request.QueryString` property contains a *collection* (a list) of the values that are submitted in the form.</span></span> <span data-ttu-id="16a2c-209">Herhangi bir değer almak için istediğiniz öğe adını belirtin.</span><span class="sxs-lookup"><span data-stu-id="16a2c-209">To get any individual value, you specify the name of the element that you want.</span></span> <span data-ttu-id="16a2c-210">İşte bu nedenle sahip olmanız bir `name` özniteliği `<input>` öğesi (`searchTerm`), metin kutusu oluşturur.</span><span class="sxs-lookup"><span data-stu-id="16a2c-210">That's why you have to have a `name` attribute on the `<input>` element (`searchTerm`) that creates the text box.</span></span> <span data-ttu-id="16a2c-211">(Hakkında daha fazla bilgi için `Request` nesne, bkz: [kenar](#BKMK_TheRequestObject) daha sonra.)</span><span class="sxs-lookup"><span data-stu-id="16a2c-211">(For more about the `Request` object, see the [sidebar](#BKMK_TheRequestObject) later.)</span></span>

<span data-ttu-id="16a2c-212">Metin kutusunun değerini okumak basit bir işlemdir.</span><span class="sxs-lookup"><span data-stu-id="16a2c-212">It's simple enough to read the value of the text box.</span></span> <span data-ttu-id="16a2c-213">Ancak kullanıcı hiçbir şey hiç metin kutusuna girmediniz ancak tıklanan **arama** aramak için bir şey olmadığı yine de bu öğesini yoksayabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="16a2c-213">But if the user didn't enter anything at all in the text box but clicked **Search** anyway, you can ignore that click, since there's nothing to search.</span></span>

<span data-ttu-id="16a2c-214">Aşağıdaki kod bu koşullar uygulamak nasıl oluşturulduğunu gösteren bir örnektir.</span><span class="sxs-lookup"><span data-stu-id="16a2c-214">The following code is an example that shows how to implement these conditions.</span></span> <span data-ttu-id="16a2c-215">(Bu birazdan gerçekleştirirsiniz; Bu kod henüz eklemeniz gerekmez.)</span><span class="sxs-lookup"><span data-stu-id="16a2c-215">(You don't have to add this code yet; you'll do that in a moment.)</span></span>

[!code-csharp[Main](form-basics/samples/sample3.cs)]

<span data-ttu-id="16a2c-216">Bu yolla test ayırır:</span><span class="sxs-lookup"><span data-stu-id="16a2c-216">The test breaks down in this way:</span></span>

- <span data-ttu-id="16a2c-217">Değerini almak `Request.QueryString["searchGenre"]`, yani içine girilen değerin `<input>` adlı bir öğe `searchGenre`.</span><span class="sxs-lookup"><span data-stu-id="16a2c-217">Get the value of `Request.QueryString["searchGenre"]`, namely the value that was entered into the `<input>` element named `searchGenre`.</span></span>
- <span data-ttu-id="16a2c-218">Bunu kullanarak boş olup olmadığını öğrenmek `IsEmpty` yöntemi.</span><span class="sxs-lookup"><span data-stu-id="16a2c-218">Find out if it's empty by using the `IsEmpty` method.</span></span> <span data-ttu-id="16a2c-219">Bu yöntem, bir şey (örneğin, bir form öğesi) bir değeri içerip içermediğini belirlemek için standart bir yoludur.</span><span class="sxs-lookup"><span data-stu-id="16a2c-219">This method is the standard way to determine whether something (for example, a form element) contains a value.</span></span> <span data-ttu-id="16a2c-220">Ancak yalnızca varsa gerçekten, ilgilendiğiniz *değil* boş, bu nedenle...</span><span class="sxs-lookup"><span data-stu-id="16a2c-220">But really, you care only if it's *not* empty, therefore ...</span></span>
- <span data-ttu-id="16a2c-221">Ekleme `!` işleci önüne `IsEmpty` test edin.</span><span class="sxs-lookup"><span data-stu-id="16a2c-221">Add the `!` operator in front of the `IsEmpty` test.</span></span> <span data-ttu-id="16a2c-222">( `!` İşleci mantıksal değil anlamına gelir).</span><span class="sxs-lookup"><span data-stu-id="16a2c-222">(The `!` operator means logical NOT).</span></span>

<span data-ttu-id="16a2c-223">Düz İngilizce olarak tüm `if` aşağıdaki koşul çevirir: *Formun searchGenre öğe sonra boş değilse...*</span><span class="sxs-lookup"><span data-stu-id="16a2c-223">In plain English, the entire `if` condition translates into the following: *If the form's searchGenre element is not empty, then ...*</span></span>

<span data-ttu-id="16a2c-224">Bu bloğu aşama arama terimi kullanan bir sorgu oluşturmak için ayarlar.</span><span class="sxs-lookup"><span data-stu-id="16a2c-224">This block sets the stage for creating a query that uses the search term.</span></span> <span data-ttu-id="16a2c-225">Bu sonraki bölümde gerçekleştirirsiniz.</span><span class="sxs-lookup"><span data-stu-id="16a2c-225">You'll do that in the next section.</span></span>

<a id="BKMK_TheRequestObject"></a>

> [!TIP] 
> 
> **<span data-ttu-id="16a2c-226">İstek nesnesi</span><span class="sxs-lookup"><span data-stu-id="16a2c-226">The Request Object</span></span>**
> 
> <span data-ttu-id="16a2c-227">`Request` Nesnesi bir sayfa istenen veya gönderildiğinde, uygulamanız için tarayıcı gönderdiği tüm bilgileri içerir.</span><span class="sxs-lookup"><span data-stu-id="16a2c-227">The `Request` object contains all the information that the browser sends to your application when a page is requested or submitted.</span></span> <span data-ttu-id="16a2c-228">Bu nesne kullanıcının sağladığı gibi metin kutusu değerleri veya karşıya yüklenecek dosyayı herhangi bir bilgi içerir.</span><span class="sxs-lookup"><span data-stu-id="16a2c-228">This object includes any information that the user provides, like text box values or a file to upload.</span></span> <span data-ttu-id="16a2c-229">Tanımlama bilgileri, URL sorgu dizesi (varsa), değerleri gibi ek bilgi için her türlü ayrıca kullanarak kullanıcı, tarayıcıda ayarlanmış olan dillerin listesini tarayıcı türü, çalışan sayfanın dosya yolu ve dahası.</span><span class="sxs-lookup"><span data-stu-id="16a2c-229">It also includes all sorts of additional information, like cookies, values in the URL query string (if any), the file path of the page that is running, the type of browser that the user is using, the list of languages that are set in the browser, and much more.</span></span>
> 
> <span data-ttu-id="16a2c-230">`Request` Nesnesi bir *koleksiyon* (listesi) değerleri.</span><span class="sxs-lookup"><span data-stu-id="16a2c-230">The `Request` object is a *collection* (list) of values.</span></span> <span data-ttu-id="16a2c-231">Tek bir değer koleksiyonu dışında adını belirterek alın:</span><span class="sxs-lookup"><span data-stu-id="16a2c-231">You get an individual value out of the collection by specifying its name:</span></span>
> 
> `var someValue = Request["name"];`
> 
> <span data-ttu-id="16a2c-232">`Request` Nesne aslında birkaç alt kümeleri sunar.</span><span class="sxs-lookup"><span data-stu-id="16a2c-232">The `Request` object actually exposes several subsets.</span></span> <span data-ttu-id="16a2c-233">Örneğin:</span><span class="sxs-lookup"><span data-stu-id="16a2c-233">For example:</span></span>
> 
> - `Request.Form` <span data-ttu-id="16a2c-234">gönderilen içindeki öğelerden değerleri size `<form>` istek öğesi bir `POST` istek.</span><span class="sxs-lookup"><span data-stu-id="16a2c-234">gives you values from elements inside the submitted `<form>` element if the request is a `POST` request.</span></span>
> - `Request.QueryString` <span data-ttu-id="16a2c-235">size, URL'nin sorgu dizesinde yalnızca değerleri sağlar.</span><span class="sxs-lookup"><span data-stu-id="16a2c-235">gives you just the values in the URL's query string.</span></span> <span data-ttu-id="16a2c-236">(Gibi bir URL içinde `http://mysite/myapp/page?searchGenre=action&page=2`, `?searchGenre=action&page=2` bölümdür URL sorgu dizesi.)</span><span class="sxs-lookup"><span data-stu-id="16a2c-236">(In a URL like `http://mysite/myapp/page?searchGenre=action&page=2`, the `?searchGenre=action&page=2` section of the URL is the query string.)</span></span>
> - `Request.Cookies` <span data-ttu-id="16a2c-237">Koleksiyon tarayıcı gönderildiğini tanımlama bilgilerine erişim sağlar.</span><span class="sxs-lookup"><span data-stu-id="16a2c-237">collection gives you access to cookies that the browser has sent.</span></span>
> 
> <span data-ttu-id="16a2c-238">Bildiğiniz bir değer almak için gönderilen biçimindedir, kullanabileceğiniz `Request["name"]`.</span><span class="sxs-lookup"><span data-stu-id="16a2c-238">To get a value that you know is in the submitted form, you can use `Request["name"]`.</span></span> <span data-ttu-id="16a2c-239">Alternatif olarak, daha belirli sürümlerini kullanabilir `Request.Form["name"]` (için `POST` istekleri) veya `Request.QueryString["name"]` (için `GET` istekleri).</span><span class="sxs-lookup"><span data-stu-id="16a2c-239">Alternatively, you can use the more specific versions `Request.Form["name"]` (for `POST` requests) or `Request.QueryString["name"]` (for `GET` requests).</span></span> <span data-ttu-id="16a2c-240">Elbette, *adı* almak için öğe adıdır.</span><span class="sxs-lookup"><span data-stu-id="16a2c-240">Of course, *name* is the name of the item to get.</span></span>
> 
> <span data-ttu-id="16a2c-241">Almak istediğiniz öğe adı koleksiyon içinde benzersiz olması gerekir.</span><span class="sxs-lookup"><span data-stu-id="16a2c-241">The name of the item you want to get has to be unique within the collection you're using.</span></span> <span data-ttu-id="16a2c-242">İşte bu `Request` nesnesi sağlar kümeleri ister `Request.Form` ve `Request.QueryString`.</span><span class="sxs-lookup"><span data-stu-id="16a2c-242">That's why the `Request` object provides the subsets like `Request.Form` and `Request.QueryString`.</span></span> <span data-ttu-id="16a2c-243">Sayfanız adlı bir form öğesi içerdiğini varsayın `userName` ve *ayrıca* adlı bir tanımlama bilgisi içeren `userName`.</span><span class="sxs-lookup"><span data-stu-id="16a2c-243">Suppose that your page contains a form element named `userName` and *also* contains a cookie named `userName`.</span></span> <span data-ttu-id="16a2c-244">Alırsanız `Request["userName"]`, belirsiz olduğu form değeri veya tanımlama bilgisinin isteyip istemediğiniz.</span><span class="sxs-lookup"><span data-stu-id="16a2c-244">If you get `Request["userName"]`, it's ambiguous whether you want the form value or the cookie.</span></span> <span data-ttu-id="16a2c-245">Ancak, alırsanız `Request.Form["userName"]` veya `Request.Cookie["userName"]`, hangi değerin alma hakkında açık.</span><span class="sxs-lookup"><span data-stu-id="16a2c-245">However, if you get `Request.Form["userName"]` or `Request.Cookie["userName"]`, you're being explicit about which value to get.</span></span>
> 
> <span data-ttu-id="16a2c-246">Belirli ve alt kümesini kullanmak için iyi bir uygulamadır `Request` , gibi ilginizi çeken `Request.Form` veya `Request.QueryString`.</span><span class="sxs-lookup"><span data-stu-id="16a2c-246">It's a good practice to be specific and use the subset of `Request` that you're interested in, like `Request.Form` or `Request.QueryString`.</span></span> <span data-ttu-id="16a2c-247">Bu öğreticide oluşturmakta olduğunuz basit sayfaları için bu büyük olasılıkla gerçekten fark yapmaz.</span><span class="sxs-lookup"><span data-stu-id="16a2c-247">For the simple pages that you're creating in this tutorial, it probably doesn't really make any difference.</span></span> <span data-ttu-id="16a2c-248">Ancak, daha karmaşık sayfaları oluşturmak gibi Sürüm'ni kullanarak `Request.Form` veya `Request.QueryString` sayfanın bir form (veya birden çok form) içerdiğinde ortaya çıkabilecek sorunları önlemenize yardımcı olabilir tanımlama bilgileri, sorgu dizesi değerlerini ve benzeri.</span><span class="sxs-lookup"><span data-stu-id="16a2c-248">However, as you create more complex pages, using the explicit version `Request.Form` or `Request.QueryString` can help you avoid problems that can arise when the page contains a form (or multiple forms), cookies, query string values, and so on.</span></span>


## <a name="creating-a-query-by-using-a-search-term"></a><span data-ttu-id="16a2c-249">Bir arama terimi kullanarak bir sorgu oluşturma</span><span class="sxs-lookup"><span data-stu-id="16a2c-249">Creating a Query by Using a Search Term</span></span>

<span data-ttu-id="16a2c-250">Kullanıcı girilen arama terimini alma artık bildiğinize göre bunu kullanan bir sorgu oluşturabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="16a2c-250">Now that you know how to get the search term that the user entered, you can create a query that uses it.</span></span> <span data-ttu-id="16a2c-251">Veritabanı dışında tüm film öğeleri almak için bu bildirimi gibi görünen bir SQL sorgusu kullanmakta olduğunuz unutmayın:</span><span class="sxs-lookup"><span data-stu-id="16a2c-251">Remember that to get all the movie items out of the database, you're using a SQL query that looks like this statement:</span></span>

`SELECT * FROM Movies`

<span data-ttu-id="16a2c-252">İçeren bir sorgu kullanmak zorunda yalnızca belirli filmler almak için bir `Where` yan tümcesi.</span><span class="sxs-lookup"><span data-stu-id="16a2c-252">To get only certain movies, you have to use a query that includes a `Where` clause.</span></span> <span data-ttu-id="16a2c-253">Bu yan tümce üzerinde satırları sorgu tarafından döndürülen bir koşul ayarlamaya olanak tanır.</span><span class="sxs-lookup"><span data-stu-id="16a2c-253">This clause lets you set a condition on which rows are returned by the query.</span></span> <span data-ttu-id="16a2c-254">Örnek buradadır:</span><span class="sxs-lookup"><span data-stu-id="16a2c-254">Here's an example:</span></span>

`SELECT * FROM Movies WHERE Genre = 'Action'`

<span data-ttu-id="16a2c-255">Temel biçimi `WHERE column = value`.</span><span class="sxs-lookup"><span data-stu-id="16a2c-255">The basic format is `WHERE column = value`.</span></span> <span data-ttu-id="16a2c-256">Bunun yanında, yalnızca farklı işleçlerini kullanabilirsiniz `=`gibi `>` (büyüktür), `<` (küçüktür), `<>` (eşit değildir), `<=` (küçüktür veya eşittir), vb. ne aradığınız bağlı olarak.</span><span class="sxs-lookup"><span data-stu-id="16a2c-256">You can use different operators besides just `=`, like `>` (greater than), `<` (less than), `<>` (not equal to), `<=` (less than or equal to), etc., depending on what you're looking for.</span></span>

<span data-ttu-id="16a2c-257">SQL deyimleri, merak durumunda, büyük küçük harfe duyarlı değildir &mdash; `SELECT` aynı `Select` (ve hatta `select`).</span><span class="sxs-lookup"><span data-stu-id="16a2c-257">In case you're wondering, SQL statements are not case sensitive &mdash; `SELECT` is the same as `Select` (or even `select`).</span></span> <span data-ttu-id="16a2c-258">Ancak, kişilerin genellikle bir SQL deyimi anahtar sözcükler gibi büyük harfle `SELECT` ve `WHERE`okunmasını kolaylaştırmak için.</span><span class="sxs-lookup"><span data-stu-id="16a2c-258">However, people often capitalize keywords in a SQL statement, like `SELECT` and `WHERE`, to make it easier to read.</span></span>

### <a name="passing-the-search-term-as-a-parameter"></a><span data-ttu-id="16a2c-259">Arama terimi, bir parametre olarak geçirerek</span><span class="sxs-lookup"><span data-stu-id="16a2c-259">Passing the search term as a parameter</span></span>

<span data-ttu-id="16a2c-260">Belirli bir türe oldukça kolaydır için arama (`WHERE Genre = 'Action'`), ancak kullanıcının girdiği herhangi Tarz için arama yapmak istiyorsanız.</span><span class="sxs-lookup"><span data-stu-id="16a2c-260">Searching for a specific genre is easy enough (`WHERE Genre = 'Action'`), but you want to be able to search for any genre that the user enters.</span></span> <span data-ttu-id="16a2c-261">Bunu yapmak için arama yapmak için bir yer tutucu değeri içeren SQL sorgusu oluşturun.</span><span class="sxs-lookup"><span data-stu-id="16a2c-261">To do that, you create as SQL query that includes a placeholder for the value to search.</span></span> <span data-ttu-id="16a2c-262">Bunun, bu komutu gibi görünür:</span><span class="sxs-lookup"><span data-stu-id="16a2c-262">It will look like this command:</span></span>

`SELECT * FROM Movies WHERE Genre = @0`

<span data-ttu-id="16a2c-263">Yer tutucusu `@` karakterini izleyen sıfır.</span><span class="sxs-lookup"><span data-stu-id="16a2c-263">The placeholder is the `@` character followed by zero.</span></span> <span data-ttu-id="16a2c-264">Tahmin gibi bir sorgu birden fazla yer tutucuları içerebilir ve bunlar adlı `@0`, `@1`, `@2`vb.</span><span class="sxs-lookup"><span data-stu-id="16a2c-264">As you might guess, a query can contain multiple placeholders, and they'd be named `@0`, `@1`, `@2`, etc.</span></span>

<span data-ttu-id="16a2c-265">Sorgu ve gerçekten değer geçirin aşağıdaki gibi bir kod kullanın:</span><span class="sxs-lookup"><span data-stu-id="16a2c-265">To set up the query and actually pass it the value, you use the code like the following:</span></span>

[!code-sql[Main](form-basics/samples/sample4.sql)]

<span data-ttu-id="16a2c-266">Bu kod, ne daha önce veri kılavuzunda görüntülemek için yaptığınız için benzer.</span><span class="sxs-lookup"><span data-stu-id="16a2c-266">This code is similar to what you've already done to display data in the grid.</span></span> <span data-ttu-id="16a2c-267">Tek fark vardır:</span><span class="sxs-lookup"><span data-stu-id="16a2c-267">The only differences are:</span></span>

- <span data-ttu-id="16a2c-268">Sorgu bir yer tutucu içeriyor (`WHERE Genre = @0"`).</span><span class="sxs-lookup"><span data-stu-id="16a2c-268">The query contains a placeholder (`WHERE Genre = @0"`).</span></span>
- <span data-ttu-id="16a2c-269">Sorgu bir değişken içine konur (`selectCommand`); sorgu doğrudan geçirilen önce `db.Query` yöntemi.</span><span class="sxs-lookup"><span data-stu-id="16a2c-269">The query is put into a variable (`selectCommand`); before, you passed the query directly to the `db.Query` method.</span></span>
- <span data-ttu-id="16a2c-270">Çağırdığınızda `db.Query` yöntemi, geçirdiğiniz sorgu hem yer tutucusu için kullanılacak değer.</span><span class="sxs-lookup"><span data-stu-id="16a2c-270">When you call the `db.Query` method, you pass both the query and the value to use for the placeholder.</span></span> <span data-ttu-id="16a2c-271">(Sorgu birden fazla yer tutucuları varsa bunları geçecekse yöntemi için ayrı değerler olarak tüm.)</span><span class="sxs-lookup"><span data-stu-id="16a2c-271">(If the query had multiple placeholders, you'd pass them all as separate values to the method.)</span></span>

<span data-ttu-id="16a2c-272">Bu öğelerin hepsini araya, aşağıdaki kodu alın:</span><span class="sxs-lookup"><span data-stu-id="16a2c-272">If you put all these elements together, you get the following code:</span></span>

[!code-csharp[Main](form-basics/samples/sample5.cs)]

> [!NOTE] 
> 
> **<span data-ttu-id="16a2c-273">Önemli!</span><span class="sxs-lookup"><span data-stu-id="16a2c-273">Important!</span></span>** <span data-ttu-id="16a2c-274">Yer tutucuları kullanma (gibi `@0`) bir SQL komutu için değerleri geçirmek için *son derece önemli* güvenlik.</span><span class="sxs-lookup"><span data-stu-id="16a2c-274">Using placeholders (like `@0`) to pass values to a SQL command is *extremely important* for security.</span></span> <span data-ttu-id="16a2c-275">Bu, değişken veri yer tutucuları olan gördüğünüz şekilde SQL komutlarını oluşturmak tek yoludur.</span><span class="sxs-lookup"><span data-stu-id="16a2c-275">The way you see it here, with placeholders for variable data, is the only way you should construct SQL commands.</span></span>
> 
> <span data-ttu-id="16a2c-276">Hiçbir zaman bir SQL deyimi (birleştirme) hazır metin ve kullanıcıdan alma değerleri koyarak birlikte oluşturun.</span><span class="sxs-lookup"><span data-stu-id="16a2c-276">Never construct a SQL statement by putting together (concatenating) literal text and values you get from the user.</span></span> <span data-ttu-id="16a2c-277">Bir SQL deyimi içinde kullanıcı girişi birleştirme açılır sitenize bir *SQL ekleme saldırısına* burada kötü niyetli bir kullanıcı veritabanınıza hack değerleri sayfanıza gönderir.</span><span class="sxs-lookup"><span data-stu-id="16a2c-277">Concatenating user input into a SQL statement opens your site to a *SQL injection attack* where a malicious user submits values to your page that hack your database.</span></span> <span data-ttu-id="16a2c-278">(Daha fazla makalede [SQL ekleme](https://msdn.microsoft.com/library/ms161953.aspx) MSDN Web sitesinde.)</span><span class="sxs-lookup"><span data-stu-id="16a2c-278">(You can read more in the article [SQL Injection](https://msdn.microsoft.com/library/ms161953.aspx) the MSDN website.)</span></span>


## <a name="updating-the-movies-page-with-search-code"></a><span data-ttu-id="16a2c-279">Arama koduyla filmler sayfanın güncelleştiriliyor</span><span class="sxs-lookup"><span data-stu-id="16a2c-279">Updating the Movies Page with Search Code</span></span>

<span data-ttu-id="16a2c-280">Kodda güncelleştirebilirsiniz artık *Movies.cshtml* dosya.</span><span class="sxs-lookup"><span data-stu-id="16a2c-280">Now you can update the code in the *Movies.cshtml* file.</span></span> <span data-ttu-id="16a2c-281">Başlamak için sayfanın üstündeki kod bloğu içindeki kod şu kodla değiştirin:</span><span class="sxs-lookup"><span data-stu-id="16a2c-281">To begin, replace the code in the code block at the top of the page with this code:</span></span>

[!code-csharp[Main](form-basics/samples/sample6.cs)]

<span data-ttu-id="16a2c-282">Burada sorgu içine yerleştirdiğiniz fark `selectCommand` için ileteceksiniz değişkeni `db.Query` daha sonra.</span><span class="sxs-lookup"><span data-stu-id="16a2c-282">The difference here is that you've put the query into the `selectCommand` variable, which you'll pass to `db.Query` later.</span></span> <span data-ttu-id="16a2c-283">SQL deyimine bir değişkene koyarak arama yapmak için yaparsınız deyimi değiştirmenize olanak tanır.</span><span class="sxs-lookup"><span data-stu-id="16a2c-283">Putting the SQL statement into a variable lets you change the statement, which is what you'll do to perform the search.</span></span>

<span data-ttu-id="16a2c-284">Geri daha sonra gireceğiniz, şu iki satırı da kaldırdıktan:</span><span class="sxs-lookup"><span data-stu-id="16a2c-284">You've also removed these two lines, which you'll put back in later:</span></span>

[!code-csharp[Main](form-basics/samples/sample7.cs)]

<span data-ttu-id="16a2c-285">Sorgu henüz çalıştırmak istemediğiniz (diğer bir deyişle, çağırma `db.Query`) ve başlatmak istemiyorsanız `WebGrid` Yardımcısı henüz ya da.</span><span class="sxs-lookup"><span data-stu-id="16a2c-285">You don't want to run the query yet (that is, call `db.Query`) and you don't want to initialize the `WebGrid` helper yet either.</span></span> <span data-ttu-id="16a2c-286">Hangi SQL deyimini çalıştırmak sahip verdi sonra bunları yaparsınız.</span><span class="sxs-lookup"><span data-stu-id="16a2c-286">You'll do those things after you've figured out which SQL statement has to run.</span></span>

<span data-ttu-id="16a2c-287">Bu yeniden bloğundan sonra arama'yı işlemek için yeni mantığı ekleyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="16a2c-287">After this rewritten block, you can add the new logic for handling the search.</span></span> <span data-ttu-id="16a2c-288">Tamamlanan kodu aşağıdakine benzer olacaktır.</span><span class="sxs-lookup"><span data-stu-id="16a2c-288">The completed code will look like the following.</span></span> <span data-ttu-id="16a2c-289">Sayfanızın kod bu örnek eşleşecek şekilde güncelleştirin:</span><span class="sxs-lookup"><span data-stu-id="16a2c-289">Update the code in your page so it matches this example:</span></span>

[!code-cshtml[Main](form-basics/samples/sample8.cshtml)]

<span data-ttu-id="16a2c-290">Sayfa artık şu şekilde çalışır.</span><span class="sxs-lookup"><span data-stu-id="16a2c-290">The page now works like this.</span></span> <span data-ttu-id="16a2c-291">Sayfa her çalıştırıldığında kod veritabanı açılır ve `selectCommand` değişkenini ayarlamak için tüm kayıtları alır SQL deyimini `Movies` tablo.</span><span class="sxs-lookup"><span data-stu-id="16a2c-291">Every time the page runs, the code opens the database and the `selectCommand` variable is set to the SQL statement that gets all the records from the `Movies` table.</span></span> <span data-ttu-id="16a2c-292">Kod ayrıca başlatır `searchTerm` değişkeni.</span><span class="sxs-lookup"><span data-stu-id="16a2c-292">The code also initializes the `searchTerm` variable.</span></span>

<span data-ttu-id="16a2c-293">Ancak, geçerli istek için bir değer içeriyorsa `searchGenre` öğesi, kod kümeleri `selectCommand` farklı bir sorgu için — yani birine içeren `Where` bir türe için aranacak yan tümcesi.</span><span class="sxs-lookup"><span data-stu-id="16a2c-293">However, if the current request includes a value for the `searchGenre` element, the code sets `selectCommand` to a different query — namely, to one that includes the `Where` clause to search for a genre.</span></span> <span data-ttu-id="16a2c-294">Ayrıca ayarlar `searchTerm` için her şeyi (hiçbir şey olabilir) arama kutusu için geçirildi.</span><span class="sxs-lookup"><span data-stu-id="16a2c-294">It also sets `searchTerm` to whatever was passed for the search box (which might be nothing).</span></span>

<span data-ttu-id="16a2c-295">Bağımsız olarak SQL deyimi yer `selectCommand`, ardından kod çağırır `db.Query` sorguyu çalıştırmak için hangi iletmeden bulunduğu `searchTerm`.</span><span class="sxs-lookup"><span data-stu-id="16a2c-295">Regardless of which SQL statement is in `selectCommand`, the code then calls `db.Query` to run the query, passing it whatever is in `searchTerm`.</span></span> <span data-ttu-id="16a2c-296">Yoksa hiçbir şey `searchTerm`, bu durumda olmadığı için parametre değeri geçirmek için bir önemi `selectCommand` yine de.</span><span class="sxs-lookup"><span data-stu-id="16a2c-296">If there's nothing in `searchTerm`, it doesn't matter, because in that case there's no parameter to pass the value to `selectCommand` anyway.</span></span>

<span data-ttu-id="16a2c-297">Son olarak, kod başlatır `WebGrid` önceden olduğu gibi sorgu sonuçlarını kullanarak Yardımcısı.</span><span class="sxs-lookup"><span data-stu-id="16a2c-297">Finally, the code initializes the `WebGrid` helper by using the query results, just like before.</span></span>

<span data-ttu-id="16a2c-298">SQL deyimi koyarak görebilirsiniz ve değişkenler, arama terimini esneklik için kod ekledik.</span><span class="sxs-lookup"><span data-stu-id="16a2c-298">You can see that by putting the SQL statement and the search term into variables, you've added flexibility to the code.</span></span> <span data-ttu-id="16a2c-299">Bu öğreticinin ilerleyen bölümlerinde anlatıldığı gibi temel bu çerçeveyi kullanabilirsiniz ve aramalar farklı türleri için mantık ekleme tutun.</span><span class="sxs-lookup"><span data-stu-id="16a2c-299">As you'll see later in this tutorial, you can use this basic framework and keep adding logic for different types of searches.</span></span>

## <a name="testing-the-search-by-genre-feature"></a><span data-ttu-id="16a2c-300">Türe göre arama özelliğini test etme</span><span class="sxs-lookup"><span data-stu-id="16a2c-300">Testing the Search-by-Genre Feature</span></span>

<span data-ttu-id="16a2c-301">Webmatrix'te, çalıştırma *Movies.cshtml* sayfası.</span><span class="sxs-lookup"><span data-stu-id="16a2c-301">In WebMatrix, run the *Movies.cshtml* page.</span></span> <span data-ttu-id="16a2c-302">Metin kutusu Tarz için sayfayı görürsünüz.</span><span class="sxs-lookup"><span data-stu-id="16a2c-302">You see the page with the text box for genre.</span></span>

<span data-ttu-id="16a2c-303">Test kayıtlarınızı birine girdikten sonra tıklayın Tarz girin **arama**.</span><span class="sxs-lookup"><span data-stu-id="16a2c-303">Enter a genre that you've entered for one of your test records, then click **Search**.</span></span> <span data-ttu-id="16a2c-304">Bu süre, o Tarz eşleşen filmler listesi görürsünüz:</span><span class="sxs-lookup"><span data-stu-id="16a2c-304">This time you see a listing of just the movies that match that genre:</span></span>

![Tarzı 'Comedies' aradıktan sonra listeleyen filmler sayfası](form-basics/_static/image4.png)

<span data-ttu-id="16a2c-306">Farklı bir türe girin ve tekrar arama yapın.</span><span class="sxs-lookup"><span data-stu-id="16a2c-306">Enter a different genre and search again.</span></span> <span data-ttu-id="16a2c-307">Arama büyük/küçük harfe duyarlı değildir görebilmeniz için tüm tümü büyük harf veya harf kullanarak Tarz girmeyi deneyin.</span><span class="sxs-lookup"><span data-stu-id="16a2c-307">Try entering the genre by using all lowercase or all uppercase letters so that you can see that the search is not case sensitive.</span></span>

## <a name="remembering-what-the-user-entered"></a><span data-ttu-id="16a2c-308">"Hangi kullanıcının girdiği hatırlamak"</span><span class="sxs-lookup"><span data-stu-id="16a2c-308">"Remembering" What the User Entered</span></span>

<span data-ttu-id="16a2c-309">Girilen bir türe ve tıklanan sonra fark etmiş **arama Tarz**, bir listesi için bu tarz gördünüz.</span><span class="sxs-lookup"><span data-stu-id="16a2c-309">You might have noticed that after you entered a genre and clicked **Search Genre**, you saw a listing for that genre.</span></span> <span data-ttu-id="16a2c-310">Ancak, arama metin kutusu boş &mdash; girilen diğer bir deyişle, unutmayın kaydetmedi.</span><span class="sxs-lookup"><span data-stu-id="16a2c-310">However, the search text box was empty &mdash; in other words, it didn't remember what you'd entered.</span></span>

<span data-ttu-id="16a2c-311">Bu davranış neden meydana geldiğini anlamak önemlidir.</span><span class="sxs-lookup"><span data-stu-id="16a2c-311">It's important to understand why this behavior occurs.</span></span> <span data-ttu-id="16a2c-312">Bir sayfa gönderdiğinizde, tarayıcı web sunucusuna bir istek gönderir.</span><span class="sxs-lookup"><span data-stu-id="16a2c-312">When you submit a page, the browser sends a request to the web server.</span></span> <span data-ttu-id="16a2c-313">ASP.NET isteği aldığında, sayfa yeni bir örneğini oluşturur, kod içinde çalışır ve sayfa tarayıcıya'ı yeniden işler.</span><span class="sxs-lookup"><span data-stu-id="16a2c-313">When ASP.NET gets the request, it creates a brand-new instance of the page, runs the code in it, and then renders the page to the browser again.</span></span> <span data-ttu-id="16a2c-314">Aslında, ancak sayfa kendisinin önceki bir sürümüyle yalnızca çalışmakta olduğunuz bilmez.</span><span class="sxs-lookup"><span data-stu-id="16a2c-314">In effect, though, the page doesn't know that you were just working with a previous version of itself.</span></span> <span data-ttu-id="16a2c-315">BT'nin, bazı olan isteği alındı bilir tüm veri içinde oluşturur.</span><span class="sxs-lookup"><span data-stu-id="16a2c-315">All it knows is that it got a request that had some form data in it.</span></span>

<span data-ttu-id="16a2c-316">Her zaman bir sayfa istemek &mdash; ilk kez mi, göndererek &mdash; yeni bir sayfa karşılaşacağınız.</span><span class="sxs-lookup"><span data-stu-id="16a2c-316">Every time you request a page &mdash; whether for the first time or by submitting it &mdash; you're getting a new page.</span></span> <span data-ttu-id="16a2c-317">Web sunucusu son isteğinizin belleğe sahip değil.</span><span class="sxs-lookup"><span data-stu-id="16a2c-317">The web server has no memory of your last request.</span></span> <span data-ttu-id="16a2c-318">ASP.NET diğerinden yapar ve tarayıcı diğerinden yapar.</span><span class="sxs-lookup"><span data-stu-id="16a2c-318">Neither does ASP.NET, and neither does the browser.</span></span> <span data-ttu-id="16a2c-319">Yalnızca sayfanın ayrı Bu örnekler arasında aralarında aktaran herhangi bir veri bağlantısıdır.</span><span class="sxs-lookup"><span data-stu-id="16a2c-319">The only connection between these separate instances of the page is any data that you transmit between them.</span></span> <span data-ttu-id="16a2c-320">Örneğin, bir sayfa gönderirseniz, yeni sayfa örnek önceki örneği tarafından gönderildiği form verileri alabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="16a2c-320">If you submit a page, for example, the new page instance can get the form data that was sent by the earlier instance.</span></span> <span data-ttu-id="16a2c-321">(Sayfalar arasında veri aktarmak için başka bir yolu, tanımlama bilgileri kullanmaktır.)</span><span class="sxs-lookup"><span data-stu-id="16a2c-321">(Another way to pass data between pages is to use cookies.)</span></span>

<span data-ttu-id="16a2c-322">Bu durumu tanımlamak için bir biçimsel web sayfaları olduğunuzu yöntemdir *durum bilgisiz*.</span><span class="sxs-lookup"><span data-stu-id="16a2c-322">A formal way to describe this situation is to say that web pages are *stateless*.</span></span> <span data-ttu-id="16a2c-323">Web sunucuları ve sayfaların ve sayfa öğeleri sayfasının önceki durumu hakkındaki tüm bilgileri bulundurmaz.</span><span class="sxs-lookup"><span data-stu-id="16a2c-323">Web servers and the pages themselves and the elements in the page do not maintain any information about the previous state of a page.</span></span> <span data-ttu-id="16a2c-324">Web, tek tek istekler için durum koruma hızla genellikle belki de binlerce işleyen web sunucularının kaynakları bile yüz binlerce, saniye başına istek harcadıklarını çünkü bu şekilde tasarlanmıştır.</span><span class="sxs-lookup"><span data-stu-id="16a2c-324">The web was designed this way because maintaining state for individual requests would quickly exhaust the resources of web servers, which often handle thousands, maybe even hundreds of thousands, of requests per second.</span></span>

<span data-ttu-id="16a2c-325">İşte bu nedenle metin kutusu boş.</span><span class="sxs-lookup"><span data-stu-id="16a2c-325">So that's why the text box was empty.</span></span> <span data-ttu-id="16a2c-326">Sayfa gönderdikten sonra ASP.NET sayfası yeni bir örneğini oluşturulur ve işaretleme ve kod çalıştı.</span><span class="sxs-lookup"><span data-stu-id="16a2c-326">After you submitted the page, ASP.NET created a new instance of the page and ran through the code and markup.</span></span> <span data-ttu-id="16a2c-327">Olan herhangi bir öğe, metin kutusuna bir değer koymak için ASP.NET söyledi koddaki.</span><span class="sxs-lookup"><span data-stu-id="16a2c-327">There was nothing in that code that told ASP.NET to put a value into the text box.</span></span> <span data-ttu-id="16a2c-328">Bu nedenle ASP.NET herhangi bir şey olmadı ve metin kutusunda bir değer olmadan işlenir.</span><span class="sxs-lookup"><span data-stu-id="16a2c-328">So ASP.NET didn't do anything, and the text box was rendered without a value in it.</span></span>

<span data-ttu-id="16a2c-329">Bu sorunu aşmak gerçekten kolay bir yolu yoktur.</span><span class="sxs-lookup"><span data-stu-id="16a2c-329">There's actually an easy way to get around this issue.</span></span> <span data-ttu-id="16a2c-330">Metin kutusuna girdiğiniz Tarz *olduğu* kodda kullanabileceğiniz &mdash; içinde `Request.QueryString["searchGenre"]`.</span><span class="sxs-lookup"><span data-stu-id="16a2c-330">The genre that you entered into the text box *is* available to you in code &mdash; it's in `Request.QueryString["searchGenre"]`.</span></span>

<span data-ttu-id="16a2c-331">Metin kutusu için biçimlendirme güncelleştirme böylece `value` öznitelik değerini alır `searchTerm`, bu örnekte ister:</span><span class="sxs-lookup"><span data-stu-id="16a2c-331">Update the markup for the text box so that the `value` attribute gets its value from `searchTerm`, like this example:</span></span>

[!code-html[Main](form-basics/samples/sample9.html?highlight=1)]

<span data-ttu-id="16a2c-332">Bu sayfada, ayrıca ayarladığınız `value` özniteliğini `searchTerm` girdiğiniz değişken türü içerdiğinden değişken.</span><span class="sxs-lookup"><span data-stu-id="16a2c-332">In this page, you could have also set the `value` attribute to the `searchTerm` variable, since that variable also contains the genre you entered.</span></span> <span data-ttu-id="16a2c-333">Ancak kullanarak `Request` nesne ayarlayın `value` özniteliği bu görevi gerçekleştirmek için standart bir yol gösterildiği gibi.</span><span class="sxs-lookup"><span data-stu-id="16a2c-333">But using the `Request` object to set the `value` attribute as shown here is the standard way to accomplish this task.</span></span> <span data-ttu-id="16a2c-334">(Hatta istediğiniz Bunu yapmak varsayılarak &mdash; bazı durumlarda, sayfayı oluşturmak isteyebilirsiniz *olmadan* alanlarındaki değerleri.</span><span class="sxs-lookup"><span data-stu-id="16a2c-334">(Assuming you even want to do this &mdash; in some situations, you might want to render the page *without* values in the fields.</span></span> <span data-ttu-id="16a2c-335">Tüm uygulamanızla neler olduğunu bağımlı.)</span><span class="sxs-lookup"><span data-stu-id="16a2c-335">It all depends on what's going on with your app.)</span></span>

> [!NOTE]
> <span data-ttu-id="16a2c-336">"Parola için kullanılan bir metin kutusunun değerini hatırlayamıyorsunuz".</span><span class="sxs-lookup"><span data-stu-id="16a2c-336">You can't "remember" the value of a text box that's used for passwords.</span></span> <span data-ttu-id="16a2c-337">Bu kod kullanarak bir parola alanını doldurmak ulaşmasını sağlamak için bir güvenlik açığı olacaktır.</span><span class="sxs-lookup"><span data-stu-id="16a2c-337">It would be a security hole to allow people to fill in a password field by using code.</span></span>


<span data-ttu-id="16a2c-338">Sayfayı yeniden çalıştırın, bir türe girin ve tıklayın **arama Tarz**.</span><span class="sxs-lookup"><span data-stu-id="16a2c-338">Run the page again, enter a genre, and click **Search Genre**.</span></span> <span data-ttu-id="16a2c-339">Bu süre yalnızca, arama sonuçlarını görmek, ancak son girilen metin kutusunu hatırlar:</span><span class="sxs-lookup"><span data-stu-id="16a2c-339">This time not only do you see the results of the search, but the text box remembers what you entered last time:</span></span>

![Sayfa gösteren metin kutusuna 'önceki giriş anımsanacak '](form-basics/_static/image5.png)

## <a name="searching-for-any-word-in-the-title"></a><span data-ttu-id="16a2c-341">Başlık herhangi bir sözcük arama</span><span class="sxs-lookup"><span data-stu-id="16a2c-341">Searching for Any Word in the Title</span></span>

<span data-ttu-id="16a2c-342">Artık tüm Tarz için arama yapabilirsiniz ancak arama için bir başlık isteyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="16a2c-342">You can now search for any genre, but you might also want to search for a title.</span></span> <span data-ttu-id="16a2c-343">Bunun yerine, herhangi bir yeri içinde bir başlık görünür bir sözcük arayabilirsiniz arama yaparken bir başlık tam doğru elde etmek zordur.</span><span class="sxs-lookup"><span data-stu-id="16a2c-343">It's hard to get a title exactly right when you search, so instead you can search for a word that appears anywhere inside a title.</span></span> <span data-ttu-id="16a2c-344">SQL'de Bunu yapmak için kullandığınız `LIKE` işleci ve söz dizimi aşağıdaki gibidir:</span><span class="sxs-lookup"><span data-stu-id="16a2c-344">To do that in SQL, you use the `LIKE` operator and syntax like the following:</span></span>

`SELECT * FROM Movies WHERE Title LIKE '%adventure%'`

<span data-ttu-id="16a2c-345">Bu komut, "adventure" başlıklarını içeren tüm filmlere alır.</span><span class="sxs-lookup"><span data-stu-id="16a2c-345">This command gets all the movies whose titles contain "adventure".</span></span> <span data-ttu-id="16a2c-346">Kullanırken `LIKE` işleci, joker karakter içeren `%` arama teriminin bir parçası olarak.</span><span class="sxs-lookup"><span data-stu-id="16a2c-346">When you use the `LIKE` operator, you include the wildcard character `%` as part of the search term.</span></span> <span data-ttu-id="16a2c-347">Arama `LIKE 'adventure%'` "'adventure' ile başlayan" anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="16a2c-347">The search `LIKE 'adventure%'` means "starting with 'adventure'".</span></span> <span data-ttu-id="16a2c-348">(Teknik olarak, "Dize 'herhangi bir şey tarafından izlenen adventure'." anlamına gelir) Benzer şekilde, arama terimi `LIKE '%adventure'` "hiçbir şey 'adventure' dize tarafından izlenen", "'adventure' görmektesiniz" söylemek için başka bir yol olduğu anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="16a2c-348">(Technically, it means "The string 'adventure' followed by anything.") Similarly, the search term `LIKE '%adventure'` means "anything followed by the string 'adventure'", which is another way to say "ending with 'adventure'".</span></span>

<span data-ttu-id="16a2c-349">Arama terimi `LIKE '%adventure%'` bu nedenle "ile"adventure' başlığı herhangi bir yerindeki."anlamına gelir</span><span class="sxs-lookup"><span data-stu-id="16a2c-349">The search term `LIKE '%adventure%'` therefore means "with 'adventure' anywhere in the title."</span></span> <span data-ttu-id="16a2c-350">(Teknik olarak, "her şey 'adventure' herhangi bir şey tarafından izlenen, ardından başlık,.")</span><span class="sxs-lookup"><span data-stu-id="16a2c-350">(Technically, "anything in the title, followed by 'adventure', followed by anything.")</span></span>

<span data-ttu-id="16a2c-351">İçinde `<form>` öğesi kapanış hemen altında aşağıdaki işaretlemeyi ekleyin `</div>` etiketi Tarz arama (yalnızca kapatmadan önce `</form>` öğesi):</span><span class="sxs-lookup"><span data-stu-id="16a2c-351">Inside the `<form>` element, add the following markup right under the closing `</div>` tag for the genre search (just before the closing `</form>` element):</span></span>

[!code-html[Main](form-basics/samples/sample10.html)]

<span data-ttu-id="16a2c-352">Birleştirmeniz gerekir. Bu arama işlemek üzere kod Tarz arama koda benzer `LIKE` arama.</span><span class="sxs-lookup"><span data-stu-id="16a2c-352">The code to handle this search is similar to the code for the genre search, except that you have to assemble the `LIKE` search.</span></span> <span data-ttu-id="16a2c-353">Bu sayfanın üst kısmındaki kod bloğunun içine ekleyin `if` hemen sonrasına block `if` bloğu Tarz arama için:</span><span class="sxs-lookup"><span data-stu-id="16a2c-353">Inside the code block at the top of the page, add this `if` block just after the `if` block for the genre search:</span></span>

[!code-csharp[Main](form-basics/samples/sample11.cs)]

<span data-ttu-id="16a2c-354">Arama kullanması hariç, bu kod, daha önce gördüğünüz aynı mantığı kullanır. bir `LIKE` işleci ve kod koyar "`%`" önce ve sonra arama terimi.</span><span class="sxs-lookup"><span data-stu-id="16a2c-354">This code uses the same logic you saw earlier, except that the search uses a `LIKE` operator and the code puts "`%`" before and after the search term.</span></span>

<span data-ttu-id="16a2c-355">Başka bir arama sayfasına eklemek kolay nasıl dikkat edin.</span><span class="sxs-lookup"><span data-stu-id="16a2c-355">Notice how it was easy to add another search to the page.</span></span> <span data-ttu-id="16a2c-356">Tüm yapmak zorunda şöyleydi:</span><span class="sxs-lookup"><span data-stu-id="16a2c-356">All you had to do was:</span></span>

- <span data-ttu-id="16a2c-357">Oluşturma bir `if` ilgili arama kutusuna bir değere sahip olup olmadığını görmek için test edilen blok.</span><span class="sxs-lookup"><span data-stu-id="16a2c-357">Create an `if` block that tested to see whether the relevant search box had a value.</span></span>
- <span data-ttu-id="16a2c-358">Ayarlama `selectCommand` değişken için yeni bir SQL deyimi.</span><span class="sxs-lookup"><span data-stu-id="16a2c-358">Set the `selectCommand` variable to a new SQL statement.</span></span>
- <span data-ttu-id="16a2c-359">Ayarlama `searchTerm` değişken için değeri sorguya iletin.</span><span class="sxs-lookup"><span data-stu-id="16a2c-359">Set the `searchTerm` variable to the value to pass to the query.</span></span>

<span data-ttu-id="16a2c-360">Başlık araması için yeni mantığı içeren tam kod bloğunu şu şekildedir:</span><span class="sxs-lookup"><span data-stu-id="16a2c-360">Here's the complete code block, which contains the new logic for a title search:</span></span>

[!code-cshtml[Main](form-basics/samples/sample12.cshtml)]

<span data-ttu-id="16a2c-361">Bu kodun yaptığı bir özeti aşağıda verilmiştir:</span><span class="sxs-lookup"><span data-stu-id="16a2c-361">Here's a summary of what this code does:</span></span>

- <span data-ttu-id="16a2c-362">Değişkenleri `searchTerm` ve `selectCommand` üstünde başlatılır.</span><span class="sxs-lookup"><span data-stu-id="16a2c-362">The variables `searchTerm` and `selectCommand` are initialized at the top.</span></span> <span data-ttu-id="16a2c-363">Uygun SQL komut kullanıcı sayfasında yaptığı şirket tabanlı ve (varsa) uygun bir arama terimi bu değişkenleri ayarlamak için oluşturacağız.</span><span class="sxs-lookup"><span data-stu-id="16a2c-363">You're going to set these variables to the appropriate search term (if any) and appropriate SQL command based on what the user does in the page.</span></span> <span data-ttu-id="16a2c-364">Varsayılan arama, tüm filmlere veritabanından almaya ilişkin basit bir durumdur.</span><span class="sxs-lookup"><span data-stu-id="16a2c-364">The default search is the simple case of getting all the movies from the database.</span></span>
- <span data-ttu-id="16a2c-365">Testler için `searchGenre` ve `searchTitle`, kod kümeleri `searchTerm` aramak istediğiniz değeri.</span><span class="sxs-lookup"><span data-stu-id="16a2c-365">In the tests for `searchGenre` and `searchTitle`, the code sets `searchTerm` to the value you want to search for.</span></span> <span data-ttu-id="16a2c-366">Bu kod blokları de ayarlanmış `selectCommand` , arama için uygun bir SQL komutu.</span><span class="sxs-lookup"><span data-stu-id="16a2c-366">Those code blocks also set `selectCommand` to an appropriate SQL command for that search.</span></span>
- <span data-ttu-id="16a2c-367">`db.Query` Yöntemi çağrıldığında yalnızca bir kez, içinde hangi komutu çalıştırırsam SQL kullanarak `selectedCommand` ve değer `searchTerm`.</span><span class="sxs-lookup"><span data-stu-id="16a2c-367">The `db.Query` method is invoked only once, using whatever SQL command is in `selectedCommand` and whatever value is in `searchTerm`.</span></span> <span data-ttu-id="16a2c-368">(Hiçbir tarz ve başlık sözcük yok), arama terimi yok ise değerini `searchTerm` boş bir dizedir.</span><span class="sxs-lookup"><span data-stu-id="16a2c-368">If there is no search term (no genre and no title word), the value of `searchTerm` is an empty string.</span></span> <span data-ttu-id="16a2c-369">Bu durumda, sorgu parametresi gerektirmediği ancak, fark etmez.</span><span class="sxs-lookup"><span data-stu-id="16a2c-369">However, that doesn't matter, because in that case the query doesn't require a parameter.</span></span>

## <a name="testing-the-title-search-feature"></a><span data-ttu-id="16a2c-370">Başlık arama özelliğini test etme</span><span class="sxs-lookup"><span data-stu-id="16a2c-370">Testing the Title Search Feature</span></span>

<span data-ttu-id="16a2c-371">Artık tamamlanmış arama sayfası test edebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="16a2c-371">Now you can test your completed search page.</span></span> <span data-ttu-id="16a2c-372">Çalıştırma *Movies.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="16a2c-372">Run *Movies.cshtml*.</span></span>

<span data-ttu-id="16a2c-373">Bir türe girip __iade **arama Tarz**.</span><span class="sxs-lookup"><span data-stu-id="16a2c-373">Enter a genre and click **Search Genre**.</span></span> <span data-ttu-id="16a2c-374">Kılavuz önce gibi bu tarz filmler görüntüler.</span><span class="sxs-lookup"><span data-stu-id="16a2c-374">The grid displays movies of that genre, like before.</span></span>

<span data-ttu-id="16a2c-375">Bir başlık sözcük girin ve tıklayın **arama başlık**.</span><span class="sxs-lookup"><span data-stu-id="16a2c-375">Enter a title word and click **Search Title**.</span></span> <span data-ttu-id="16a2c-376">Kılavuzun bu sözcüğü başlığında filmler görüntüler.</span><span class="sxs-lookup"><span data-stu-id="16a2c-376">The grid displays movies that have that word in the title.</span></span>

![Başlığında '' için arama yaptıktan sonra listeleyen filmler sayfası](form-basics/_static/image6.png)

<span data-ttu-id="16a2c-378">Her iki metin kutusunu boş bırakın ve her iki düğmesine tıklayın.</span><span class="sxs-lookup"><span data-stu-id="16a2c-378">Leave both text boxes blank and click either button.</span></span> <span data-ttu-id="16a2c-379">Tüm filmlere kılavuz görüntüler.</span><span class="sxs-lookup"><span data-stu-id="16a2c-379">The grid displays all the movies.</span></span>

## <a name="combining-the-queries"></a><span data-ttu-id="16a2c-380">Sorguları birleştirme</span><span class="sxs-lookup"><span data-stu-id="16a2c-380">Combining the Queries</span></span>

<span data-ttu-id="16a2c-381">Gerçekleştirebileceğiniz arama Dışlayıcı olduğunu fark edebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="16a2c-381">You might notice that the searches you can perform are exclusive.</span></span> <span data-ttu-id="16a2c-382">Her iki arama kutusu değerleri içeren bile aynı zamanda, başlık ve Tarz arama yapamazsınız.</span><span class="sxs-lookup"><span data-stu-id="16a2c-382">You can't search the title and the genre at the same time, even if both search boxes have values in them.</span></span> <span data-ttu-id="16a2c-383">Örneğin, "Adventure" ayarlanmış başlık içeren tüm eylem filmler için arama yapamazsınız.</span><span class="sxs-lookup"><span data-stu-id="16a2c-383">For example, you can't search for all action movies whose title contains "Adventure".</span></span> <span data-ttu-id="16a2c-384">(Tarz ve başlık için bir değer girerseniz sayfası artık, kodlanmış gibi başlığı aramasını öncelik alır.) Koşullar birleştiren bir arama oluşturmak için aşağıdaki gibi bir söz dizimi olan bir SQL sorgusu oluşturma gerekir:</span><span class="sxs-lookup"><span data-stu-id="16a2c-384">(As the page is coded now, if you enter values for both genre and title, the title search gets precedence.) To create a search that combines the conditions, you would have to create a SQL query that has syntax like the following:</span></span>

`SELECT * FROM Movies WHERE Genre = @0 AND Title LIKE @1`

<span data-ttu-id="16a2c-385">Ve aşağıdaki gibi bir deyim kullanarak sorguyu çalıştırmak gerekir (yaklaşık konuşulur):</span><span class="sxs-lookup"><span data-stu-id="16a2c-385">And you'd have to run the query by using a statement like the following (roughly speaking):</span></span>

`var selectedData = db.Query(selectCommand, searchGenre, searchTitle);`

<span data-ttu-id="16a2c-386">Gördüğünüz gibi çok sayıda permütasyon arama ölçütü izin vermek için mantığı oluşturma biraz karmaşık alabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="16a2c-386">Creating logic to allow many permutations of search criteria can get a bit involved, as you can see.</span></span> <span data-ttu-id="16a2c-387">Bu nedenle, biz burada durdurulacak.</span><span class="sxs-lookup"><span data-stu-id="16a2c-387">Therefore, we'll stop here.</span></span>

## <a name="coming-up-next"></a><span data-ttu-id="16a2c-388">Sıradaki gelen</span><span class="sxs-lookup"><span data-stu-id="16a2c-388">Coming Up Next</span></span>

<span data-ttu-id="16a2c-389">Sonraki öğreticide, bir form kullanıcıların filmler veritabanına ekleme kullanan bir sayfa oluşturacaksınız.</span><span class="sxs-lookup"><span data-stu-id="16a2c-389">In the next tutorial, you'll create a page that uses a form to let users add movies to the database.</span></span>

## <a name="complete-listing-for-movie-page-updated-with-search"></a><span data-ttu-id="16a2c-390">Tam listesi için (arama ile güncelleştirilmiş) film sayfası</span><span class="sxs-lookup"><span data-stu-id="16a2c-390">Complete Listing for Movie Page (Updated with Search)</span></span>

[!code-cshtml[Main](form-basics/samples/sample13.cshtml)]

## <a name="additional-resources"></a><span data-ttu-id="16a2c-391">Ek Kaynaklar</span><span class="sxs-lookup"><span data-stu-id="16a2c-391">Additional Resources</span></span>

- [<span data-ttu-id="16a2c-392">ASP.NET Web programlama Razor söz dizimini kullanarak giriş</span><span class="sxs-lookup"><span data-stu-id="16a2c-392">Introduction to ASP.NET Web Programming Using the Razor Syntax</span></span>](https://go.microsoft.com/fwlink/?LinkID=202890)
- <span data-ttu-id="16a2c-393">[SQL WHERE yan tümcesi](http://www.w3schools.com/sql/sql_where.asp) W3Schools sitesinde</span><span class="sxs-lookup"><span data-stu-id="16a2c-393">[SQL WHERE Clause](http://www.w3schools.com/sql/sql_where.asp) on the W3Schools site</span></span>
- <span data-ttu-id="16a2c-394">[Yöntem tanımlarını](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) W3C sitesinde makale</span><span class="sxs-lookup"><span data-stu-id="16a2c-394">[Method Definitions](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) article on the W3C site</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="16a2c-395">[Önceki](displaying-data.md)
> [İleri](entering-data.md)</span><span class="sxs-lookup"><span data-stu-id="16a2c-395">[Previous](displaying-data.md)
[Next](entering-data.md)</span></span>
