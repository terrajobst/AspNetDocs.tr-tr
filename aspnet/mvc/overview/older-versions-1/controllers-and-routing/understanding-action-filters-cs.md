---
uid: mvc/overview/older-versions-1/controllers-and-routing/understanding-action-filters-cs
title: Eylem filtrelerini anlama (C#) | Microsoft Docs
author: microsoft
description: Bu öğreticinin amacı, eylem filtrelerini açıklamaktır. Eylem filtresi, bir denetleyici eylemine veya bir denetleyicinin tamamına uygulayabileceğiniz bir özniteliktir...
ms.author: riande
ms.date: 10/16/2008
ms.assetid: a94e4e81-40c1-47b7-8613-126a1a6cc93d
msc.legacyurl: /mvc/overview/older-versions-1/controllers-and-routing/understanding-action-filters-cs
msc.type: authoredcontent
ms.openlocfilehash: d1c72c2355c6122f851351a8c1e8f04fa63ae04e
ms.sourcegitcommit: 22fbd8863672c4ad6693b8388ad5c8e753fb41a2
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 11/28/2019
ms.locfileid: "74590083"
---
# <a name="understanding-action-filters-c"></a><span data-ttu-id="ec849-104">Eylem Filtrelerini Anlama (C#)</span><span class="sxs-lookup"><span data-stu-id="ec849-104">Understanding Action Filters (C#)</span></span>

<span data-ttu-id="ec849-105">[Microsoft](https://github.com/microsoft) tarafından</span><span class="sxs-lookup"><span data-stu-id="ec849-105">by [Microsoft](https://github.com/microsoft)</span></span>

[<span data-ttu-id="ec849-106">PDF 'YI indir</span><span class="sxs-lookup"><span data-stu-id="ec849-106">Download PDF</span></span>](https://download.microsoft.com/download/e/f/3/ef3f2ff6-7424-48f7-bdaa-180ef64c3490/ASPNET_MVC_Tutorial_14_CS.pdf)

> <span data-ttu-id="ec849-107">Bu öğreticinin amacı, eylem filtrelerini açıklamaktır.</span><span class="sxs-lookup"><span data-stu-id="ec849-107">The goal of this tutorial is to explain action filters.</span></span> <span data-ttu-id="ec849-108">Eylem filtresi, bir denetleyici eylemi için uygulayabileceğiniz bir özniteliktir veya bir denetleyicinin tamamına, eylemin yürütülme biçimini değiştirir.</span><span class="sxs-lookup"><span data-stu-id="ec849-108">An action filter is an attribute that you can apply to a controller action -- or an entire controller -- that modifies the way in which the action is executed.</span></span>

## <a name="understanding-action-filters"></a><span data-ttu-id="ec849-109">Eylem filtrelerini anlama</span><span class="sxs-lookup"><span data-stu-id="ec849-109">Understanding Action Filters</span></span>

<span data-ttu-id="ec849-110">Bu öğreticinin amacı, eylem filtrelerini açıklamaktır.</span><span class="sxs-lookup"><span data-stu-id="ec849-110">The goal of this tutorial is to explain action filters.</span></span> <span data-ttu-id="ec849-111">Eylem filtresi, bir denetleyici eylemi için uygulayabileceğiniz bir özniteliktir veya bir denetleyicinin tamamına, eylemin yürütülme biçimini değiştirir.</span><span class="sxs-lookup"><span data-stu-id="ec849-111">An action filter is an attribute that you can apply to a controller action -- or an entire controller -- that modifies the way in which the action is executed.</span></span> <span data-ttu-id="ec849-112">ASP.NET MVC çerçevesi birkaç eylem filtresi içerir:</span><span class="sxs-lookup"><span data-stu-id="ec849-112">The ASP.NET MVC framework includes several action filters:</span></span>

- <span data-ttu-id="ec849-113">OutputCache – bu eylem filtresi, belirli bir süre boyunca bir denetleyici eyleminin çıkışını önbelleğe alır.</span><span class="sxs-lookup"><span data-stu-id="ec849-113">OutputCache – This action filter caches the output of a controller action for a specified amount of time.</span></span>
- <span data-ttu-id="ec849-114">HandleError: Bu eylem filtresi, bir denetleyici eylemi yürütüldüğünde oluşan hataları işler.</span><span class="sxs-lookup"><span data-stu-id="ec849-114">HandleError – This action filter handles errors raised when a controller action executes.</span></span>
- <span data-ttu-id="ec849-115">Yetkilendir – bu eylem filtresi, belirli bir kullanıcı veya role erişimi kısıtlamanıza olanak sağlar.</span><span class="sxs-lookup"><span data-stu-id="ec849-115">Authorize – This action filter enables you to restrict access to a particular user or role.</span></span>

<span data-ttu-id="ec849-116">Kendi özel eylem filtrelerinizi de oluşturabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="ec849-116">You also can create your own custom action filters.</span></span> <span data-ttu-id="ec849-117">Örneğin, özel bir kimlik doğrulama sistemi uygulamak için özel bir eylem filtresi oluşturmak isteyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="ec849-117">For example, you might want to create a custom action filter in order to implement a custom authentication system.</span></span> <span data-ttu-id="ec849-118">Ya da, bir denetleyici eylemi tarafından döndürülen Görünüm verilerini değiştiren bir eylem filtresi oluşturmak isteyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="ec849-118">Or, you might want to create an action filter that modifies the view data returned by a controller action.</span></span>

<span data-ttu-id="ec849-119">Bu öğreticide, baştan sona bir eylem filtresi oluşturmayı öğreneceksiniz.</span><span class="sxs-lookup"><span data-stu-id="ec849-119">In this tutorial, you learn how to build an action filter from the ground up.</span></span> <span data-ttu-id="ec849-120">Bir eylemin, Visual Studio çıktı penceresine işlenmesinin farklı aşamalarını günlüğe kaydeden bir günlük eylemi filtresi oluşturacağız.</span><span class="sxs-lookup"><span data-stu-id="ec849-120">We create a Log action filter that logs different stages of the processing of an action to the Visual Studio Output window.</span></span>

### <a name="using-an-action-filter"></a><span data-ttu-id="ec849-121">Eylem filtresi kullanma</span><span class="sxs-lookup"><span data-stu-id="ec849-121">Using an Action Filter</span></span>

<span data-ttu-id="ec849-122">Eylem filtresi bir özniteliktir.</span><span class="sxs-lookup"><span data-stu-id="ec849-122">An action filter is an attribute.</span></span> <span data-ttu-id="ec849-123">Birçok eylem filtresini tek bir denetleyici eylemine veya bir denetleyicinin tamamına uygulayabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="ec849-123">You can apply most action filters to either an individual controller action or an entire controller.</span></span>

<span data-ttu-id="ec849-124">Örneğin, liste 1 ' deki veri denetleyicisi, geçerli saati döndüren `Index()` adlı bir eylem gösterir.</span><span class="sxs-lookup"><span data-stu-id="ec849-124">For example, the Data controller in Listing 1 exposes an action named `Index()` that returns the current time.</span></span> <span data-ttu-id="ec849-125">Bu eylem `OutputCache` eylem filtresiyle birlikte tasarlanmıştır.</span><span class="sxs-lookup"><span data-stu-id="ec849-125">This action is decorated with the `OutputCache` action filter.</span></span> <span data-ttu-id="ec849-126">Bu filtre, eylem tarafından döndürülen değerin 10 saniye önbellekte önbelleğe alınmasına neden olur.</span><span class="sxs-lookup"><span data-stu-id="ec849-126">This filter causes the value returned by the action to be cached for 10 seconds.</span></span>

<span data-ttu-id="ec849-127">**Listeleme 1 – `Controllers\DataController.cs`**</span><span class="sxs-lookup"><span data-stu-id="ec849-127">**Listing 1 – `Controllers\DataController.cs`**</span></span>

[!code-csharp[Main](understanding-action-filters-cs/samples/sample1.cs)]

<span data-ttu-id="ec849-128">Tarayıcınızın adres çubuğuna/Data/Index URL 'sini girerek `Index()` eylemini tekrar çağırdığınızda ve Yenile düğmesine birden çok kez ulaşarak, 10 saniye boyunca aynı zamanı görürsünüz.</span><span class="sxs-lookup"><span data-stu-id="ec849-128">If you repeatedly invoke the `Index()` action by entering the URL /Data/Index into the address bar of your browser and hitting the Refresh button multiple times, then you will see the same time for 10 seconds.</span></span> <span data-ttu-id="ec849-129">`Index()` eyleminin çıkışı 10 saniye için önbelleğe alınır (bkz. Şekil 1).</span><span class="sxs-lookup"><span data-stu-id="ec849-129">The output of the `Index()` action is cached for 10 seconds (see Figure 1).</span></span>

<span data-ttu-id="ec849-130">[![önbelleğe alınmış zaman](understanding-action-filters-cs/_static/image2.png)](understanding-action-filters-cs/_static/image1.png)</span><span class="sxs-lookup"><span data-stu-id="ec849-130">[![Cached time](understanding-action-filters-cs/_static/image2.png)](understanding-action-filters-cs/_static/image1.png)</span></span>

<span data-ttu-id="ec849-131">**Şekil 01**: önbelleğe alınan süre ([tam boyutlu görüntüyü görüntülemek için tıklayın](understanding-action-filters-cs/_static/image3.png))</span><span class="sxs-lookup"><span data-stu-id="ec849-131">**Figure 01**: Cached time ([Click to view full-size image](understanding-action-filters-cs/_static/image3.png))</span></span>

<span data-ttu-id="ec849-132">Liste 1 ' de, tek bir eylem filtresi – `OutputCache` Action filtresi – `Index()` yöntemine uygulanır.</span><span class="sxs-lookup"><span data-stu-id="ec849-132">In Listing 1, a single action filter – the `OutputCache` action filter – is applied to the `Index()` method.</span></span> <span data-ttu-id="ec849-133">Gerekirse, aynı eyleme birden çok eylem filtresi uygulayabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="ec849-133">If you need, you can apply multiple action filters to the same action.</span></span> <span data-ttu-id="ec849-134">Örneğin, hem `OutputCache` hem de `HandleError` eylem filtrelerini aynı eyleme uygulamak isteyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="ec849-134">For example, you might want to apply both the `OutputCache` and `HandleError` action filters to the same action.</span></span>

<span data-ttu-id="ec849-135">Liste 1 ' de `OutputCache` eylem filtresi `Index()` eylemine uygulanır.</span><span class="sxs-lookup"><span data-stu-id="ec849-135">In Listing 1, the `OutputCache` action filter is applied to the `Index()` action.</span></span> <span data-ttu-id="ec849-136">Ayrıca, bu özniteliği `DataController` sınıfının kendisi için de uygulayabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="ec849-136">You also could apply this attribute to the `DataController` class itself.</span></span> <span data-ttu-id="ec849-137">Bu durumda, denetleyici tarafından sunulan herhangi bir eylem tarafından döndürülen sonuç 10 saniye için önbelleğe alınır.</span><span class="sxs-lookup"><span data-stu-id="ec849-137">In that case, the result returned by any action exposed by the controller would be cached for 10 seconds.</span></span>

### <a name="the-different-types-of-filters"></a><span data-ttu-id="ec849-138">Farklı filtre türleri</span><span class="sxs-lookup"><span data-stu-id="ec849-138">The Different Types of Filters</span></span>

<span data-ttu-id="ec849-139">ASP.NET MVC çerçevesi dört farklı filtre türünü destekler:</span><span class="sxs-lookup"><span data-stu-id="ec849-139">The ASP.NET MVC framework supports four different types of filters:</span></span>

1. <span data-ttu-id="ec849-140">Yetkilendirme filtreleri – `IAuthorizationFilter` özniteliğini uygular.</span><span class="sxs-lookup"><span data-stu-id="ec849-140">Authorization filters – Implements the `IAuthorizationFilter` attribute.</span></span>
2. <span data-ttu-id="ec849-141">Eylem filtreleri – `IActionFilter` özniteliğini uygular.</span><span class="sxs-lookup"><span data-stu-id="ec849-141">Action filters – Implements the `IActionFilter` attribute.</span></span>
3. <span data-ttu-id="ec849-142">Sonuç filtreleri – `IResultFilter` özniteliğini uygular.</span><span class="sxs-lookup"><span data-stu-id="ec849-142">Result filters – Implements the `IResultFilter` attribute.</span></span>
4. <span data-ttu-id="ec849-143">Özel durum filtreleri – `IExceptionFilter` özniteliğini uygular.</span><span class="sxs-lookup"><span data-stu-id="ec849-143">Exception filters – Implements the `IExceptionFilter` attribute.</span></span>

<span data-ttu-id="ec849-144">Filtreler yukarıda listelenen sırayla yürütülür.</span><span class="sxs-lookup"><span data-stu-id="ec849-144">Filters are executed in the order listed above.</span></span> <span data-ttu-id="ec849-145">Örneğin, Yetkilendirme filtreleri her zaman eylem filtreleri ve özel durum filtreleri her zaman diğer filtre türlerinin ardından yürütülene kadar yürütülür.</span><span class="sxs-lookup"><span data-stu-id="ec849-145">For example, authorization filters are always executed before action filters and exception filters are always executed after every other type of filter.</span></span>

<span data-ttu-id="ec849-146">Yetkilendirme filtreleri, denetleyici eylemlerine yönelik kimlik doğrulama ve yetkilendirme uygulamak için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="ec849-146">Authorization filters are used to implement authentication and authorization for controller actions.</span></span> <span data-ttu-id="ec849-147">Örneğin, yetkilendirme filtresi, yetkilendirme filtresine bir örnektir.</span><span class="sxs-lookup"><span data-stu-id="ec849-147">For example, the Authorize filter is an example of an Authorization filter.</span></span>

<span data-ttu-id="ec849-148">Eylem filtreleri, bir denetleyici eylemi yürütmeden önce ve sonra yürütülen mantığı içerir.</span><span class="sxs-lookup"><span data-stu-id="ec849-148">Action filters contain logic that is executed before and after a controller action executes.</span></span> <span data-ttu-id="ec849-149">Bir denetleyici eyleminin döndürdüğü verileri değiştirmek için bir eylem filtresi (örneğin,) kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="ec849-149">You can use an action filter, for instance, to modify the view data that a controller action returns.</span></span>

<span data-ttu-id="ec849-150">Sonuç filtreleri, bir görünüm sonucu yürütülmeden önce ve sonra yürütülen mantığı içerir.</span><span class="sxs-lookup"><span data-stu-id="ec849-150">Result filters contain logic that is executed before and after a view result is executed.</span></span> <span data-ttu-id="ec849-151">Örneğin, görünüm tarayıcıya işlenmeden önce bir görünüm sonucunu değiştirmek isteyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="ec849-151">For example, you might want to modify a view result right before the view is rendered to the browser.</span></span>

<span data-ttu-id="ec849-152">Özel durum filtreleri, çalıştırılacak filtrenin son türüdür.</span><span class="sxs-lookup"><span data-stu-id="ec849-152">Exception filters are the last type of filter to run.</span></span> <span data-ttu-id="ec849-153">Denetleyici eylemleriniz veya denetleyici eylem sonuçlarınız tarafından oluşturulan hataları işlemek için bir özel durum filtresi kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="ec849-153">You can use an exception filter to handle errors raised by either your controller actions or controller action results.</span></span> <span data-ttu-id="ec849-154">Hataları günlüğe kaydetmek için özel durum filtreleri de kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="ec849-154">You also can use exception filters to log errors.</span></span>

<span data-ttu-id="ec849-155">Her farklı filtre türü belirli bir sırada yürütülür.</span><span class="sxs-lookup"><span data-stu-id="ec849-155">Each different type of filter is executed in a particular order.</span></span> <span data-ttu-id="ec849-156">Aynı türdeki filtrelerin yürütüldüğü sırayı denetlemek isterseniz, bir filtrenin Order özelliğini ayarlayabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="ec849-156">If you want to control the order in which filters of the same type are executed then you can set a filter's Order property.</span></span>

<span data-ttu-id="ec849-157">Tüm eylem filtrelerinin temel sınıfı `System.Web.Mvc.FilterAttribute` sınıfıdır.</span><span class="sxs-lookup"><span data-stu-id="ec849-157">The base class for all action filters is the `System.Web.Mvc.FilterAttribute` class.</span></span> <span data-ttu-id="ec849-158">Belirli bir tür filtre uygulamak istiyorsanız, temel filtre sınıfından devralan bir sınıf oluşturmanız ve bir veya daha fazla `IAuthorizationFilter`, `IActionFilter`, `IResultFilter`veya `IExceptionFilter` arabirimi uygulamanız gerekir.</span><span class="sxs-lookup"><span data-stu-id="ec849-158">If you want to implement a particular type of filter, then you need to create a class that inherits from the base Filter class and implements one or more of the `IAuthorizationFilter`, `IActionFilter`, `IResultFilter`, or `IExceptionFilter` interfaces.</span></span>

### <a name="the-base-actionfilterattribute-class"></a><span data-ttu-id="ec849-159">Temel ActionFilterAttribute sınıfı</span><span class="sxs-lookup"><span data-stu-id="ec849-159">The Base ActionFilterAttribute Class</span></span>

<span data-ttu-id="ec849-160">Özel bir eylem filtresi uygulamanızı kolaylaştırmak için, ASP.NET MVC çerçevesi bir temel `ActionFilterAttribute` sınıfı içerir.</span><span class="sxs-lookup"><span data-stu-id="ec849-160">In order to make it easier for you to implement a custom action filter, the ASP.NET MVC framework includes a base `ActionFilterAttribute` class.</span></span> <span data-ttu-id="ec849-161">Bu sınıf hem `IActionFilter` hem de `IResultFilter` arabirimlerini uygular ve `Filter` sınıfından devralır.</span><span class="sxs-lookup"><span data-stu-id="ec849-161">This class implements both the `IActionFilter` and `IResultFilter` interfaces and inherits from the `Filter` class.</span></span>

<span data-ttu-id="ec849-162">Buradaki terminoloji tamamen tutarlı değildir.</span><span class="sxs-lookup"><span data-stu-id="ec849-162">The terminology here is not entirely consistent.</span></span> <span data-ttu-id="ec849-163">Teknik olarak, ActionFilterAttribute sınıfından devralan bir sınıf hem eylem filtresi hem de sonuç filtresidir.</span><span class="sxs-lookup"><span data-stu-id="ec849-163">Technically, a class that inherits from the ActionFilterAttribute class is both an action filter and a result filter.</span></span> <span data-ttu-id="ec849-164">Ancak, gevşek anlamda Word eylem filtresi, ASP.NET MVC çerçevesindeki herhangi bir filtre türüne başvurmak için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="ec849-164">However, in the loose sense, the word action filter is used to refer to any type of filter in the ASP.NET MVC framework.</span></span>

<span data-ttu-id="ec849-165">Temel `ActionFilterAttribute` sınıfı, geçersiz kılabileceğiniz aşağıdaki yöntemlere sahiptir:</span><span class="sxs-lookup"><span data-stu-id="ec849-165">The base `ActionFilterAttribute` class has the following methods that you can override:</span></span>

- <span data-ttu-id="ec849-166">OnActionExecuting – bu yöntem, bir denetleyici eylemi yürütülmeden önce çağrılır.</span><span class="sxs-lookup"><span data-stu-id="ec849-166">OnActionExecuting – This method is called before a controller action is executed.</span></span>
- <span data-ttu-id="ec849-167">Onactionyürütüldü – bu yöntem bir denetleyici eylemi yürütüldükten sonra çağrılır.</span><span class="sxs-lookup"><span data-stu-id="ec849-167">OnActionExecuted – This method is called after a controller action is executed.</span></span>
- <span data-ttu-id="ec849-168">OnResultExecuting – bu yöntem, bir denetleyici eylem sonucu yürütülmeden önce çağrılır.</span><span class="sxs-lookup"><span data-stu-id="ec849-168">OnResultExecuting – This method is called before a controller action result is executed.</span></span>
- <span data-ttu-id="ec849-169">OnResultExecuted – bu yöntem bir denetleyici eylemi sonucu yürütüldükten sonra çağrılır.</span><span class="sxs-lookup"><span data-stu-id="ec849-169">OnResultExecuted – This method is called after a controller action result is executed.</span></span>

<span data-ttu-id="ec849-170">Sonraki bölümde, bu farklı yöntemlerin her birini nasıl uygulayabileceğinizi görüyoruz.</span><span class="sxs-lookup"><span data-stu-id="ec849-170">In the next section, we'll see how you can implement each of these different methods.</span></span>

### <a name="creating-a-log-action-filter"></a><span data-ttu-id="ec849-171">Günlük eylemi filtresi oluşturma</span><span class="sxs-lookup"><span data-stu-id="ec849-171">Creating a Log Action Filter</span></span>

<span data-ttu-id="ec849-172">Özel bir eylem filtresi oluşturmayı göstermek için, bir denetleyici eylemini işleme aşamalarını, Visual Studio çıktı penceresine kaydeden bir özel eylem filtresi oluşturacağız.</span><span class="sxs-lookup"><span data-stu-id="ec849-172">In order to illustrate how you can build a custom action filter, we'll create a custom action filter that logs the stages of processing a controller action to the Visual Studio Output window.</span></span> <span data-ttu-id="ec849-173">`LogActionFilter`, liste 2 ' de yer alır.</span><span class="sxs-lookup"><span data-stu-id="ec849-173">Our `LogActionFilter` is contained in Listing 2.</span></span>

<span data-ttu-id="ec849-174">**Listeleme 2 – `ActionFilters\LogActionFilter.cs`**</span><span class="sxs-lookup"><span data-stu-id="ec849-174">**Listing 2 – `ActionFilters\LogActionFilter.cs`**</span></span>

[!code-csharp[Main](understanding-action-filters-cs/samples/sample2.cs)]

<span data-ttu-id="ec849-175">Liste 2 ' de, `OnActionExecuting()`, `OnActionExecuted()`, `OnResultExecuting()`ve `OnResultExecuted()` yöntemlerinin hepsi, `Log()` yöntemini çağırır.</span><span class="sxs-lookup"><span data-stu-id="ec849-175">In Listing 2, the `OnActionExecuting()`, `OnActionExecuted()`, `OnResultExecuting()`, and `OnResultExecuted()` methods all call the `Log()` method.</span></span> <span data-ttu-id="ec849-176">Yöntemin adı ve geçerli yol verileri `Log()` yöntemine geçirilir.</span><span class="sxs-lookup"><span data-stu-id="ec849-176">The name of the method and the current route data is passed to the `Log()` method.</span></span> <span data-ttu-id="ec849-177">`Log()` yöntemi, Visual Studio çıktı penceresine bir ileti yazar (bkz. Şekil 2).</span><span class="sxs-lookup"><span data-stu-id="ec849-177">The `Log()` method writes a message to the Visual Studio Output window (see Figure 2).</span></span>

<span data-ttu-id="ec849-178">[Visual Studio çıktı penceresine ![yazma](understanding-action-filters-cs/_static/image5.png)](understanding-action-filters-cs/_static/image4.png)</span><span class="sxs-lookup"><span data-stu-id="ec849-178">[![Writing to the Visual Studio Output window](understanding-action-filters-cs/_static/image5.png)](understanding-action-filters-cs/_static/image4.png)</span></span>

<span data-ttu-id="ec849-179">**Şekil 02**: Visual Studio çıktı penceresine yazma ([tam boyutlu görüntüyü görüntülemek için tıklayın](understanding-action-filters-cs/_static/image6.png))</span><span class="sxs-lookup"><span data-stu-id="ec849-179">**Figure 02**: Writing to the Visual Studio Output window ([Click to view full-size image](understanding-action-filters-cs/_static/image6.png))</span></span>

<span data-ttu-id="ec849-180">Listeleme 3 ' teki giriş denetleyicisi, tüm denetleyici sınıfına günlük eylemi filtresini nasıl uygulayacağınızı gösterir.</span><span class="sxs-lookup"><span data-stu-id="ec849-180">The Home controller in Listing 3 illustrates how you can apply the Log action filter to an entire controller class.</span></span> <span data-ttu-id="ec849-181">Giriş denetleyicisi tarafından kullanıma sunulan eylemlerden herhangi biri çağrıldığında `Index()` yöntemi veya `About()` yöntemi – eylemi işleme aşamaları Visual Studio çıktı penceresine kaydedilir.</span><span class="sxs-lookup"><span data-stu-id="ec849-181">Whenever any of the actions exposed by the Home controller are invoked – either the `Index()` method or the `About()` method – the stages of processing the action are logged to the Visual Studio Output window.</span></span>

<span data-ttu-id="ec849-182">**Listeleme 3 – `Controllers\HomeController.cs`**</span><span class="sxs-lookup"><span data-stu-id="ec849-182">**Listing 3 – `Controllers\HomeController.cs`**</span></span>

[!code-csharp[Main](understanding-action-filters-cs/samples/sample3.cs)]

### <a name="summary"></a><span data-ttu-id="ec849-183">Özet</span><span class="sxs-lookup"><span data-stu-id="ec849-183">Summary</span></span>

<span data-ttu-id="ec849-184">Bu öğreticide, ASP.NET MVC eylem filtreleri ' ne sunulmuştur.</span><span class="sxs-lookup"><span data-stu-id="ec849-184">In this tutorial, you were introduced to ASP.NET MVC action filters.</span></span> <span data-ttu-id="ec849-185">Dört farklı filtre türü hakkında bilgi edindiniz: Yetkilendirme filtreleri, eylem filtreleri, sonuç filtreleri ve özel durum filtreleri.</span><span class="sxs-lookup"><span data-stu-id="ec849-185">You learned about the four different types of filters: authorization filters, action filters, result filters, and exception filters.</span></span> <span data-ttu-id="ec849-186">Temel `ActionFilterAttribute` sınıfı hakkında da bilgi edindiniz.</span><span class="sxs-lookup"><span data-stu-id="ec849-186">You also learned about the base `ActionFilterAttribute` class.</span></span>

<span data-ttu-id="ec849-187">Son olarak, basit bir eylem filtresinin nasıl uygulanacağını öğrendiniz.</span><span class="sxs-lookup"><span data-stu-id="ec849-187">Finally, you learned how to implement a simple action filter.</span></span> <span data-ttu-id="ec849-188">Bir denetleyici eylemini işleme aşamalarını, Visual Studio çıktı penceresine kaydeden bir günlük eylemi filtresi oluşturduk.</span><span class="sxs-lookup"><span data-stu-id="ec849-188">We created a Log action filter that logs the stages of processing a controller action to the Visual Studio Output window.</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="ec849-189">[Önceki](asp-net-mvc-routing-overview-cs.md)
> [İleri](improving-performance-with-output-caching-cs.md)</span><span class="sxs-lookup"><span data-stu-id="ec849-189">[Previous](asp-net-mvc-routing-overview-cs.md)
[Next](improving-performance-with-output-caching-cs.md)</span></span>
