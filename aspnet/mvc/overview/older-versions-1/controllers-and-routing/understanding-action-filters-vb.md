---
uid: mvc/overview/older-versions-1/controllers-and-routing/understanding-action-filters-vb
title: (VB) eylem filtrelerini anlama | Microsoft Docs
author: microsoft
description: Bu öğreticide eylem filtrelerini açıklamak için hedefidir. Bir eyleme eylem filtresi bir denetleyici eylemi--ya da tüm bir denetleyiciye uygulanan bir özniteliktir...
ms.author: riande
ms.date: 10/16/2008
ms.assetid: e83812f2-c53e-4a43-a7c1-d64c59ecf694
msc.legacyurl: /mvc/overview/older-versions-1/controllers-and-routing/understanding-action-filters-vb
msc.type: authoredcontent
ms.openlocfilehash: 0116306afdf21cb24a374013bb54ada54e5699ea
ms.sourcegitcommit: 24b1f6decbb17bb22a45166e5fdb0845c65af498
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 03/01/2019
ms.locfileid: "57069510"
---
<a name="understanding-action-filters-vb"></a><span data-ttu-id="414dd-104">Eylem Filtrelerini Anlama (VB)</span><span class="sxs-lookup"><span data-stu-id="414dd-104">Understanding Action Filters (VB)</span></span>
====================
<span data-ttu-id="414dd-105">tarafından [Microsoft](https://github.com/microsoft)</span><span class="sxs-lookup"><span data-stu-id="414dd-105">by [Microsoft](https://github.com/microsoft)</span></span>

[<span data-ttu-id="414dd-106">PDF'yi indirin</span><span class="sxs-lookup"><span data-stu-id="414dd-106">Download PDF</span></span>](http://download.microsoft.com/download/e/f/3/ef3f2ff6-7424-48f7-bdaa-180ef64c3490/ASPNET_MVC_Tutorial_14_VB.pdf)

> <span data-ttu-id="414dd-107">Bu öğreticide eylem filtrelerini açıklamak için hedefidir.</span><span class="sxs-lookup"><span data-stu-id="414dd-107">The goal of this tutorial is to explain action filters.</span></span> <span data-ttu-id="414dd-108">Bir eyleme eylem filtresi bir denetleyici eylemi--veya eylemin yürütüldüğü şeklini değiştirir tüm controller--uygulayabileceğiniz bir özniteliktir.</span><span class="sxs-lookup"><span data-stu-id="414dd-108">An action filter is an attribute that you can apply to a controller action -- or an entire controller -- that modifies the way in which the action is executed.</span></span>


## <a name="understanding-action-filters"></a><span data-ttu-id="414dd-109">Eylem filtrelerini anlama</span><span class="sxs-lookup"><span data-stu-id="414dd-109">Understanding Action Filters</span></span>

<span data-ttu-id="414dd-110">Bu öğreticide eylem filtrelerini açıklamak için hedefidir.</span><span class="sxs-lookup"><span data-stu-id="414dd-110">The goal of this tutorial is to explain action filters.</span></span> <span data-ttu-id="414dd-111">Bir eyleme eylem filtresi bir denetleyici eylemi--veya eylemin yürütüldüğü şeklini değiştirir tüm controller--uygulayabileceğiniz bir özniteliktir.</span><span class="sxs-lookup"><span data-stu-id="414dd-111">An action filter is an attribute that you can apply to a controller action -- or an entire controller -- that modifies the way in which the action is executed.</span></span> <span data-ttu-id="414dd-112">ASP.NET MVC çerçevesi, birkaç eylem filtrelerini içerir:</span><span class="sxs-lookup"><span data-stu-id="414dd-112">The ASP.NET MVC framework includes several action filters:</span></span>

- <span data-ttu-id="414dd-113">OutputCache – belirtilen bir süre için bir denetleyici eylemi çıktısı bu eylem filtresi önbelleğe alır.</span><span class="sxs-lookup"><span data-stu-id="414dd-113">OutputCache – This action filter caches the output of a controller action for a specified amount of time.</span></span>
- <span data-ttu-id="414dd-114">HandleError – Bu eylem filtresi, bir denetleyici eylemi yürütüldüğünde yükseltilmiş hatalarını ele alır.</span><span class="sxs-lookup"><span data-stu-id="414dd-114">HandleError – This action filter handles errors raised when a controller action executes.</span></span>
- <span data-ttu-id="414dd-115">Yetkilendirme: Bu eylem filtresi, belirli kullanıcı veya rol için erişimi kısıtlamak sağlar.</span><span class="sxs-lookup"><span data-stu-id="414dd-115">Authorize – This action filter enables you to restrict access to a particular user or role.</span></span>

<span data-ttu-id="414dd-116">Kendi özel eylem filtreleri de oluşturabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="414dd-116">You also can create your own custom action filters.</span></span> <span data-ttu-id="414dd-117">Örneğin, bir özel kimlik doğrulama sistemi uygulamak için bir özel eylem filtresi oluşturmak isteyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="414dd-117">For example, you might want to create a custom action filter in order to implement a custom authentication system.</span></span> <span data-ttu-id="414dd-118">Veya, denetleyici eylem tarafından döndürülen görünüm verileri değiştiren bir eylem filtresi oluşturmak isteyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="414dd-118">Or, you might want to create an action filter that modifies the view data returned by a controller action.</span></span>

<span data-ttu-id="414dd-119">Bu öğreticide, bir eyleme eylem filtresi sıfırdan oluşturmak nasıl öğrenin.</span><span class="sxs-lookup"><span data-stu-id="414dd-119">In this tutorial, you learn how to build an action filter from the ground up.</span></span> <span data-ttu-id="414dd-120">Farklı işleme eylem aşamalarında Visual Studio çıkış penceresine oturum günlüğü bir eylem filtresi oluştururuz.</span><span class="sxs-lookup"><span data-stu-id="414dd-120">We create a Log action filter that logs different stages of the processing of an action to the Visual Studio Output window.</span></span>

### <a name="using-an-action-filter"></a><span data-ttu-id="414dd-121">Bir eylem filtresini kullanma</span><span class="sxs-lookup"><span data-stu-id="414dd-121">Using an Action Filter</span></span>

<span data-ttu-id="414dd-122">Bir eyleme eylem filtresi bir özniteliktir.</span><span class="sxs-lookup"><span data-stu-id="414dd-122">An action filter is an attribute.</span></span> <span data-ttu-id="414dd-123">Ayrı ayrı denetleyicisinin eylem ya da tüm bir denetleyicinin eylem filtrelerin çoğu uygulayabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="414dd-123">You can apply most action filters to either an individual controller action or an entire controller.</span></span>

<span data-ttu-id="414dd-124">Örneğin, adlı bir eylem listeleme 1 veri denetleyicisi sunan `Index()` geçerli saati döndürür.</span><span class="sxs-lookup"><span data-stu-id="414dd-124">For example, the Data controller in Listing 1 exposes an action named `Index()` that returns the current time.</span></span> <span data-ttu-id="414dd-125">Bu eylem ile donatılmış `OutputCache` eylem filtresi.</span><span class="sxs-lookup"><span data-stu-id="414dd-125">This action is decorated with the `OutputCache` action filter.</span></span> <span data-ttu-id="414dd-126">Bu filtre, 10 saniye boyunca önbelleğe alınacak eylem tarafından döndürülen değeri neden olur.</span><span class="sxs-lookup"><span data-stu-id="414dd-126">This filter causes the value returned by the action to be cached for 10 seconds.</span></span>

<span data-ttu-id="414dd-127">**Kod 1 – `Controllers\DataController.vb`**</span><span class="sxs-lookup"><span data-stu-id="414dd-127">**Listing 1 – `Controllers\DataController.vb`**</span></span>

[!code-vb[Main](understanding-action-filters-vb/samples/sample1.vb)]

<span data-ttu-id="414dd-128">Art arda çağırma `Index()` URL/Data/dizin tarayıcınızın adres çubuğuna girerek ve yenileme eylemi düğmesine birden çok kez aynı anda 10 saniye boyunca görür.</span><span class="sxs-lookup"><span data-stu-id="414dd-128">If you repeatedly invoke the `Index()` action by entering the URL /Data/Index into the address bar of your browser and hitting the Refresh button multiple times, then you will see the same time for 10 seconds.</span></span> <span data-ttu-id="414dd-129">Çıkışı `Index()` eylem 10 (bkz. Şekil 1) saniye için önbelleğe alınır.</span><span class="sxs-lookup"><span data-stu-id="414dd-129">The output of the `Index()` action is cached for 10 seconds (see Figure 1).</span></span>


<span data-ttu-id="414dd-130">[![Önbelleğe alınan saati](understanding-action-filters-vb/_static/image2.png)](understanding-action-filters-vb/_static/image1.png)</span><span class="sxs-lookup"><span data-stu-id="414dd-130">[![Cached time](understanding-action-filters-vb/_static/image2.png)](understanding-action-filters-vb/_static/image1.png)</span></span>

<span data-ttu-id="414dd-131">**Şekil 01**: Önbelleğe alınan süresi ([tam boyutlu görüntüyü görmek için tıklatın](understanding-action-filters-vb/_static/image3.png))</span><span class="sxs-lookup"><span data-stu-id="414dd-131">**Figure 01**: Cached time ([Click to view full-size image](understanding-action-filters-vb/_static/image3.png))</span></span>


<span data-ttu-id="414dd-132">1 ' bir tek eylem filtresi – listeleme `OutputCache` eylem filtresi – uygulanan `Index()` yöntemi.</span><span class="sxs-lookup"><span data-stu-id="414dd-132">In Listing 1, a single action filter – the `OutputCache` action filter – is applied to the `Index()` method.</span></span> <span data-ttu-id="414dd-133">Gerekirse, aynı eylemi birden çok eylem filtre uygulayabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="414dd-133">If you need, you can apply multiple action filters to the same action.</span></span> <span data-ttu-id="414dd-134">Örneğin, her ikisini de uygulamak isteyebilirsiniz `OutputCache` ve `HandleError` aynı eylem için eylem filtreleri.</span><span class="sxs-lookup"><span data-stu-id="414dd-134">For example, you might want to apply both the `OutputCache` and `HandleError` action filters to the same action.</span></span>

<span data-ttu-id="414dd-135">1 ' listeleme `OutputCache` eylem filtresi uygulandığı `Index()` eylem.</span><span class="sxs-lookup"><span data-stu-id="414dd-135">In Listing 1, the `OutputCache` action filter is applied to the `Index()` action.</span></span> <span data-ttu-id="414dd-136">Bu öznitelik için de uygulanabilir `DataController` sınıfının kendisini.</span><span class="sxs-lookup"><span data-stu-id="414dd-136">You also could apply this attribute to the `DataController` class itself.</span></span> <span data-ttu-id="414dd-137">Bu durumda, denetleyici tarafından kullanıma sunulan herhangi bir eylem tarafından döndürülen sonuç 10 saniye boyunca önbelleğe alınması.</span><span class="sxs-lookup"><span data-stu-id="414dd-137">In that case, the result returned by any action exposed by the controller would be cached for 10 seconds.</span></span>

### <a name="the-different-types-of-filters"></a><span data-ttu-id="414dd-138">Farklı türde filtreler</span><span class="sxs-lookup"><span data-stu-id="414dd-138">The Different Types of Filters</span></span>

<span data-ttu-id="414dd-139">ASP.NET MVC çerçevesi, filtre dört farklı türlerini destekler:</span><span class="sxs-lookup"><span data-stu-id="414dd-139">The ASP.NET MVC framework supports four different types of filters:</span></span>

1. <span data-ttu-id="414dd-140">Yetkilendirme filtreleri – uygular `IAuthorizationFilter` özniteliği.</span><span class="sxs-lookup"><span data-stu-id="414dd-140">Authorization filters – Implements the `IAuthorizationFilter` attribute.</span></span>
2. <span data-ttu-id="414dd-141">Eylem filtreleri – uygular `IActionFilter` özniteliği.</span><span class="sxs-lookup"><span data-stu-id="414dd-141">Action filters – Implements the `IActionFilter` attribute.</span></span>
3. <span data-ttu-id="414dd-142">Sonuç filtreleri – uygular `IResultFilter` özniteliği.</span><span class="sxs-lookup"><span data-stu-id="414dd-142">Result filters – Implements the `IResultFilter` attribute.</span></span>
4. <span data-ttu-id="414dd-143">Özel durum filtreleri – uygular `IExceptionFilter` özniteliği.</span><span class="sxs-lookup"><span data-stu-id="414dd-143">Exception filters – Implements the `IExceptionFilter` attribute.</span></span>

<span data-ttu-id="414dd-144">Filtreler, yukarıda listelenen sırayla yürütülür.</span><span class="sxs-lookup"><span data-stu-id="414dd-144">Filters are executed in the order listed above.</span></span> <span data-ttu-id="414dd-145">Örneğin, yetkilendirme filtreleri önce eylem filtreleri her zaman yürütülür ve özel durum filtreleri filtre her başka bir türden sonra her zaman yürütülür.</span><span class="sxs-lookup"><span data-stu-id="414dd-145">For example, authorization filters are always executed before action filters and exception filters are always executed after every other type of filter.</span></span>

<span data-ttu-id="414dd-146">Yetkilendirme filtreleri, kimlik doğrulama ve yetkilendirme denetleyici eylemleri uygulamak için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="414dd-146">Authorization filters are used to implement authentication and authorization for controller actions.</span></span> <span data-ttu-id="414dd-147">Örneğin, Authorize filtre bir yetkilendirme filtresi örneğidir.</span><span class="sxs-lookup"><span data-stu-id="414dd-147">For example, the Authorize filter is an example of an Authorization filter.</span></span>

<span data-ttu-id="414dd-148">Eylem filtreleri önce ve denetleyici Eylem yürütüldükten sonra yürütülen mantığı içerir.</span><span class="sxs-lookup"><span data-stu-id="414dd-148">Action filters contain logic that is executed before and after a controller action executes.</span></span> <span data-ttu-id="414dd-149">Bir denetleyici eylemi döndüren görünüm verileri değiştirmek için bir eyleme eylem filtresi örneği için kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="414dd-149">You can use an action filter, for instance, to modify the view data that a controller action returns.</span></span>

<span data-ttu-id="414dd-150">Sonuç filtreleri önce ve bir görünüm sonucu yürütüldükten sonra yürütülen mantığı içerir.</span><span class="sxs-lookup"><span data-stu-id="414dd-150">Result filters contain logic that is executed before and after a view result is executed.</span></span> <span data-ttu-id="414dd-151">Örneğin, bir görünüm sonucu değiştirmek isteyebilirsiniz görünümü tarayıcıya işlenmeden önce sağ.</span><span class="sxs-lookup"><span data-stu-id="414dd-151">For example, you might want to modify a view result right before the view is rendered to the browser.</span></span>

<span data-ttu-id="414dd-152">Özel durum filtreleri çalıştırmak için filtre son türüdür.</span><span class="sxs-lookup"><span data-stu-id="414dd-152">Exception filters are the last type of filter to run.</span></span> <span data-ttu-id="414dd-153">Denetleyici eylem veya denetleyici eylem sonuçlarını tarafından oluşturulan hataları işlemek için bir özel durum filtresini kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="414dd-153">You can use an exception filter to handle errors raised by either your controller actions or controller action results.</span></span> <span data-ttu-id="414dd-154">Özel durum filtreleri, hataları günlüğe kaydetmek için de kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="414dd-154">You also can use exception filters to log errors.</span></span>

<span data-ttu-id="414dd-155">Farklı her filtre türü, belirli bir düzende yürütülür.</span><span class="sxs-lookup"><span data-stu-id="414dd-155">Each different type of filter is executed in a particular order.</span></span> <span data-ttu-id="414dd-156">Aynı türde filtrelerinin yürütüldüğü sırayı denetlemek istiyorsanız, filtrenin sırası özelliği ayarlayabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="414dd-156">If you want to control the order in which filters of the same type are executed then you can set a filter's Order property.</span></span>

<span data-ttu-id="414dd-157">Tüm eylem filtrelerini temel sınıfı olan `System.Web.Mvc.FilterAttribute` sınıfı.</span><span class="sxs-lookup"><span data-stu-id="414dd-157">The base class for all action filters is the `System.Web.Mvc.FilterAttribute` class.</span></span> <span data-ttu-id="414dd-158">Belirli türde bir filtre uygulamak istiyorsanız, temel filtreleme sınıfından devralır ve bir veya daha fazla IAuthorizationFilter, IActionFilter IResultFilter uygulayan bir sınıf oluşturmak için gereken veya ExceptionFilter arabirimleri.</span><span class="sxs-lookup"><span data-stu-id="414dd-158">If you want to implement a particular type of filter, then you need to create a class that inherits from the base Filter class and implements one or more of the IAuthorizationFilter, IActionFilter, IResultFilter, or ExceptionFilter interfaces.</span></span>

### <a name="the-base-actionfilterattribute-class"></a><span data-ttu-id="414dd-159">Temel ActionFilterAttribute sınıfı</span><span class="sxs-lookup"><span data-stu-id="414dd-159">The Base ActionFilterAttribute Class</span></span>

<span data-ttu-id="414dd-160">ASP.NET MVC çerçevesi, bir özel eylem filtresi uygulamak daha kolay hale getirmek için bir temel içerir `ActionFilterAttribute` sınıfı.</span><span class="sxs-lookup"><span data-stu-id="414dd-160">In order to make it easier for you to implement a custom action filter, the ASP.NET MVC framework includes a base `ActionFilterAttribute` class.</span></span> <span data-ttu-id="414dd-161">Her ikisi de bu sınıfın uyguladığı `IActionFilter` ve `IResultFilter` arabirimleri ve devralınan `Filter` sınıfı.</span><span class="sxs-lookup"><span data-stu-id="414dd-161">This class implements both the `IActionFilter` and `IResultFilter` interfaces and inherits from the `Filter` class.</span></span>

<span data-ttu-id="414dd-162">Terimleri burada tamamen tutarlı değil.</span><span class="sxs-lookup"><span data-stu-id="414dd-162">The terminology here is not entirely consistent.</span></span> <span data-ttu-id="414dd-163">Teknik olarak ActionFilterAttribute sınıfından devralan bir sınıf hem bir eyleme eylem filtresi hem de bir sonuç Filtresi ' dir.</span><span class="sxs-lookup"><span data-stu-id="414dd-163">Technically, a class that inherits from the ActionFilterAttribute class is both an action filter and a result filter.</span></span> <span data-ttu-id="414dd-164">Ancak, gevşek anlamda kelime eylem filtresi herhangi bir türde ASP.NET MVC çerçevesi bir filtrede başvurmak için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="414dd-164">However, in the loose sense, the word action filter is used to refer to any type of filter in the ASP.NET MVC framework.</span></span>

<span data-ttu-id="414dd-165">Temel ActionFilterAttribute sınıfı, geçersiz kılabilirsiniz aşağıdaki yöntemleri vardır:</span><span class="sxs-lookup"><span data-stu-id="414dd-165">The base ActionFilterAttribute class has the following methods that you can override:</span></span>

- <span data-ttu-id="414dd-166">Bir denetleyici eylemi yürütülmeden önce OnActionExecuting – bu yöntem çağrılır.</span><span class="sxs-lookup"><span data-stu-id="414dd-166">OnActionExecuting – This method is called before a controller action is executed.</span></span>
- <span data-ttu-id="414dd-167">Denetleyici Eylem yürütüldükten sonra OnActionExecuted – bu yöntem çağrılır.</span><span class="sxs-lookup"><span data-stu-id="414dd-167">OnActionExecuted – This method is called after a controller action is executed.</span></span>
- <span data-ttu-id="414dd-168">Denetleyici eylem sonucu yürütülmeden önce OnResultExecuting – bu yöntem çağrılır.</span><span class="sxs-lookup"><span data-stu-id="414dd-168">OnResultExecuting – This method is called before a controller action result is executed.</span></span>
- <span data-ttu-id="414dd-169">Denetleyici eylem sonucu yürütüldükten sonra OnResultExecuted – bu yöntem çağrılır.</span><span class="sxs-lookup"><span data-stu-id="414dd-169">OnResultExecuted – This method is called after a controller action result is executed.</span></span>

<span data-ttu-id="414dd-170">Sonraki bölümde, bu farklı yöntemlerinin her birini nasıl uygulayabileceğiniz göreceğiz.</span><span class="sxs-lookup"><span data-stu-id="414dd-170">In the next section, we'll see how you can implement each of these different methods.</span></span>

### <a name="creating-a-log-action-filter"></a><span data-ttu-id="414dd-171">Günlük eylem filtresi oluşturma</span><span class="sxs-lookup"><span data-stu-id="414dd-171">Creating a Log Action Filter</span></span>

<span data-ttu-id="414dd-172">Bir özel eylem filtresi nasıl oluşturabileceğinizi göstermek için Visual Studio çıkış penceresine bir denetleyici eylemi işleme aşamalarını günlükleri bir özel eylem filtresi oluşturacağız.</span><span class="sxs-lookup"><span data-stu-id="414dd-172">In order to illustrate how you can build a custom action filter, we'll create a custom action filter that logs the stages of processing a controller action to the Visual Studio Output window.</span></span> <span data-ttu-id="414dd-173">Bizim `LogActionFilter` listeleme 2'de yer alır.</span><span class="sxs-lookup"><span data-stu-id="414dd-173">Our `LogActionFilter` is contained in Listing 2.</span></span>

<span data-ttu-id="414dd-174">**Kod 2 – `ActionFilters\LogActionFilter.vb`**</span><span class="sxs-lookup"><span data-stu-id="414dd-174">**Listing 2 – `ActionFilters\LogActionFilter.vb`**</span></span>

[!code-vb[Main](understanding-action-filters-vb/samples/sample2.vb)]

<span data-ttu-id="414dd-175">Listeleme 2 `OnActionExecuting()`, `OnActionExecuted()`, `OnResultExecuting()`, ve `OnResultExecuted()` tüm yöntemleri çağırmak `Log()` yöntemi.</span><span class="sxs-lookup"><span data-stu-id="414dd-175">In Listing 2, the `OnActionExecuting()`, `OnActionExecuted()`, `OnResultExecuting()`, and `OnResultExecuted()` methods all call the `Log()` method.</span></span> <span data-ttu-id="414dd-176">Yöntemin adı ve geçerli bir rota verilerini geçirilir `Log()` yöntemi.</span><span class="sxs-lookup"><span data-stu-id="414dd-176">The name of the method and the current route data is passed to the `Log()` method.</span></span> <span data-ttu-id="414dd-177">`Log()` Yöntemi, Visual Studio çıkış penceresinde bir ileti yazar (bkz: Şekil 2).</span><span class="sxs-lookup"><span data-stu-id="414dd-177">The `Log()` method writes a message to the Visual Studio Output window (see Figure 2).</span></span>


<span data-ttu-id="414dd-178">[![Visual Studio çıkış penceresine yazma](understanding-action-filters-vb/_static/image5.png)](understanding-action-filters-vb/_static/image4.png)</span><span class="sxs-lookup"><span data-stu-id="414dd-178">[![Writing to the Visual Studio Output window](understanding-action-filters-vb/_static/image5.png)](understanding-action-filters-vb/_static/image4.png)</span></span>

<span data-ttu-id="414dd-179">**Şekil 02**: Visual Studio çıkış penceresine yazma ([tam boyutlu görüntüyü görmek için tıklatın](understanding-action-filters-vb/_static/image6.png))</span><span class="sxs-lookup"><span data-stu-id="414dd-179">**Figure 02**: Writing to the Visual Studio Output window ([Click to view full-size image](understanding-action-filters-vb/_static/image6.png))</span></span>


<span data-ttu-id="414dd-180">Giriş denetleyicisine listeleme 3'te nasıl bir tüm denetleyicinin sınıf için günlüğü eylem filtresi uygulayabilirsiniz gösterilmektedir.</span><span class="sxs-lookup"><span data-stu-id="414dd-180">The Home controller in Listing 3 illustrates how you can apply the Log action filter to an entire controller class.</span></span> <span data-ttu-id="414dd-181">Her giriş denetleyici tarafından kullanıma sunulan eylemlerden herhangi birini çağrılır – ya da `Index()` yöntemi veya `About()` yöntemi – Visual Studio çıkış penceresinde eylem günlüğe kaydedilen işleme aşamalarını.</span><span class="sxs-lookup"><span data-stu-id="414dd-181">Whenever any of the actions exposed by the Home controller are invoked – either the `Index()` method or the `About()` method – the stages of processing the action are logged to the Visual Studio Output window.</span></span>

<span data-ttu-id="414dd-182">**Kod 3 – `Controllers\HomeController.vb`**</span><span class="sxs-lookup"><span data-stu-id="414dd-182">**Listing 3 – `Controllers\HomeController.vb`**</span></span>

[!code-vb[Main](understanding-action-filters-vb/samples/sample3.vb)]

### <a name="summary"></a><span data-ttu-id="414dd-183">Özet</span><span class="sxs-lookup"><span data-stu-id="414dd-183">Summary</span></span>

<span data-ttu-id="414dd-184">Bu öğreticide, ASP.NET MVC eylem filtreleri için eklenmiştir.</span><span class="sxs-lookup"><span data-stu-id="414dd-184">In this tutorial, you were introduced to ASP.NET MVC action filters.</span></span> <span data-ttu-id="414dd-185">Filtreler dört farklı türleri hakkında bilgi edindiniz: Yetkilendirme filtreleri, eylem filtreleri, sonuç filtreleri ve özel durum filtreleri.</span><span class="sxs-lookup"><span data-stu-id="414dd-185">You learned about the four different types of filters: authorization filters, action filters, result filters, and exception filters.</span></span> <span data-ttu-id="414dd-186">Ayrıca temel hakkında bilgi edindiniz `ActionFilterAttribute` sınıfı.</span><span class="sxs-lookup"><span data-stu-id="414dd-186">You also learned about the base `ActionFilterAttribute` class.</span></span>

<span data-ttu-id="414dd-187">Son olarak, basit bir eylem filtresinin uygulama hakkında bilgi edindiniz.</span><span class="sxs-lookup"><span data-stu-id="414dd-187">Finally, you learned how to implement a simple action filter.</span></span> <span data-ttu-id="414dd-188">Visual Studio çıkış penceresine bir denetleyici eylemi işleme aşamalarını günlükleri bir günlüğü eylem filtresi oluşturduk.</span><span class="sxs-lookup"><span data-stu-id="414dd-188">We created a Log action filter that logs the stages of processing a controller action to the Visual Studio Output window.</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="414dd-189">[Önceki](asp-net-mvc-routing-overview-vb.md)
> [İleri](improving-performance-with-output-caching-vb.md)</span><span class="sxs-lookup"><span data-stu-id="414dd-189">[Previous](asp-net-mvc-routing-overview-vb.md)
[Next](improving-performance-with-output-caching-vb.md)</span></span>
