---
uid: web-forms/overview/getting-started/getting-started-with-aspnet-45-web-forms/url-routing
title: URL yönlendirme | Microsoft Docs
author: Erikre
description: Bu öğretici serisinin ASP.NET 4.5 ve Visual Studio 2013 Express için kullandığımız bir ASP.NET Web Forms uygulaması oluşturmaya yönelik temel bilgiler sağlanır...
ms.author: riande
ms.date: 09/08/2014
ms.assetid: 4f4bf092-c400-471f-a876-78fda0417890
msc.legacyurl: /web-forms/overview/getting-started/getting-started-with-aspnet-45-web-forms/url-routing
msc.type: authoredcontent
ms.openlocfilehash: f8a374be79a41b34dc8f17fa8d44c6e0680984d7
ms.sourcegitcommit: 51b01b6ff8edde57d8243e4da28c9f1e7f1962b2
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 05/06/2019
ms.locfileid: "65108615"
---
# <a name="url-routing"></a><span data-ttu-id="adb28-103">URL Yönlendirme</span><span class="sxs-lookup"><span data-stu-id="adb28-103">URL Routing</span></span>

<span data-ttu-id="adb28-104">tarafından [Erik Reitan](https://github.com/Erikre)</span><span class="sxs-lookup"><span data-stu-id="adb28-104">by [Erik Reitan](https://github.com/Erikre)</span></span>

<span data-ttu-id="adb28-105">[Wingtip Toys örnek projeyi (C#) indirin](http://go.microsoft.com/fwlink/?LinkID=389434&clcid=0x409) veya [indirme E-kitabı (PDF)](http://download.microsoft.com/download/0/F/B/0FBFAA46-2BFD-478F-8E56-7BF3C672DF9D/Getting%20Started%20with%20ASP.NET%204.5%20Web%20Forms%20and%20Visual%20Studio%202013.pdf)</span><span class="sxs-lookup"><span data-stu-id="adb28-105">[Download Wingtip Toys Sample Project (C#)](http://go.microsoft.com/fwlink/?LinkID=389434&clcid=0x409) or [Download E-book (PDF)](http://download.microsoft.com/download/0/F/B/0FBFAA46-2BFD-478F-8E56-7BF3C672DF9D/Getting%20Started%20with%20ASP.NET%204.5%20Web%20Forms%20and%20Visual%20Studio%202013.pdf)</span></span>

> <span data-ttu-id="adb28-106">Bu öğretici serisinin Web için ASP.NET 4.5 ve Visual Studio 2013 Express kullanarak bir ASP.NET Web Forms uygulaması oluşturmaya yönelik temel bilgiler sağlanır.</span><span class="sxs-lookup"><span data-stu-id="adb28-106">This tutorial series will teach you the basics of building an ASP.NET Web Forms application using ASP.NET 4.5 and Microsoft Visual Studio Express 2013 for Web.</span></span> <span data-ttu-id="adb28-107">Bir Visual Studio 2013'ün [C# kaynak kodu ile proje](https://go.microsoft.com/fwlink/?LinkID=389434&clcid=0x409) Bu öğretici serisinin eşlik etmek üzere hazırdır.</span><span class="sxs-lookup"><span data-stu-id="adb28-107">A Visual Studio 2013 [project with C# source code](https://go.microsoft.com/fwlink/?LinkID=389434&clcid=0x409) is available to accompany this tutorial series.</span></span>

<span data-ttu-id="adb28-108">Bu öğreticide, Wingtip Toys örnek uygulama, URL yönlendirme desteklemek için değiştirir.</span><span class="sxs-lookup"><span data-stu-id="adb28-108">In this tutorial, you will modify the Wingtip Toys sample application to support URL routing.</span></span> <span data-ttu-id="adb28-109">Yönlendirme kolay, unutmayın daha kolay ve daha iyi arama motorları tarafından desteklenen URL'lerini kullanacak şekilde web uygulamanızı sağlar.</span><span class="sxs-lookup"><span data-stu-id="adb28-109">Routing enables your web application to use URLs that are friendly, easier to remember, and better supported by search engines.</span></span> <span data-ttu-id="adb28-110">Bu öğreticide, önceki öğreticide "Üyelik ve yönetim" oluşturur ve Wingtip Toys öğretici serisinin bir parçasıdır.</span><span class="sxs-lookup"><span data-stu-id="adb28-110">This tutorial builds on the previous tutorial "Membership and Administration" and is part of the Wingtip Toys tutorial series.</span></span>

## <a name="what-youll-learn"></a><span data-ttu-id="adb28-111">Öğrenecekleriniz:</span><span class="sxs-lookup"><span data-stu-id="adb28-111">What you'll learn:</span></span>

- <span data-ttu-id="adb28-112">Nasıl bir ASP.NET Web Forms uygulaması için rotalar kaydedin.</span><span class="sxs-lookup"><span data-stu-id="adb28-112">How to register routes for an ASP.NET Web Forms application.</span></span>
- <span data-ttu-id="adb28-113">Yollar bir web sayfasına ekleme.</span><span class="sxs-lookup"><span data-stu-id="adb28-113">How to add routes to a web page.</span></span>
- <span data-ttu-id="adb28-114">Nasıl yollar desteklemek için bir veritabanından verileri seçin.</span><span class="sxs-lookup"><span data-stu-id="adb28-114">How to select data from a database to support routes.</span></span>

## <a name="aspnet-routing-overview"></a><span data-ttu-id="adb28-115">ASP.NET yönlendirmeye genel bakış</span><span class="sxs-lookup"><span data-stu-id="adb28-115">ASP.NET Routing Overview</span></span>

<span data-ttu-id="adb28-116">URL yönlendirme kabul etmek için bir uygulama yapılandırmanıza olanak tanır fiziksel dosyaları eşlemeyin URL'leri isteyin.</span><span class="sxs-lookup"><span data-stu-id="adb28-116">URL routing allows you to configure an application to accept request URLs that do not map to physical files.</span></span> <span data-ttu-id="adb28-117">Yalnızca web sitenizde bir sayfayı bulmak için kendi tarayıcıya kullanıcının girdiği URL bir istek URL'sidir.</span><span class="sxs-lookup"><span data-stu-id="adb28-117">A request URL is simply the URL a user enters into their browser to find a page on your web site.</span></span> <span data-ttu-id="adb28-118">Anlamsal olarak kullanıcılar için anlamlı olan ve arama motoru iyileştirmesi (SEO) yardımcı olan URL'ler tanımlamak için yönlendirme kullanır.</span><span class="sxs-lookup"><span data-stu-id="adb28-118">You use routing to define URLs that are semantically meaningful to users and that can help with search-engine optimization (SEO).</span></span>

<span data-ttu-id="adb28-119">Varsayılan olarak, Web Forms şablonu içerir [ASP.NET kolay URL'leri](http://www.nuget.org/packages/Microsoft.AspNet.FriendlyUrls/).</span><span class="sxs-lookup"><span data-stu-id="adb28-119">By default, the Web Forms template includes [ASP.NET Friendly URLs](http://www.nuget.org/packages/Microsoft.AspNet.FriendlyUrls/).</span></span> <span data-ttu-id="adb28-120">Temel yönlendirme işin çoğunu kullanarak uygulanacak *kolay URL'leri*.</span><span class="sxs-lookup"><span data-stu-id="adb28-120">Much of the basic routing work will be implemented by using *Friendly URLs*.</span></span> <span data-ttu-id="adb28-121">Ancak, bu öğreticide, özelleştirilmiş yönlendirme özelliklerini ekleyeceksiniz.</span><span class="sxs-lookup"><span data-stu-id="adb28-121">However, in this tutorial you will add customized routing capabilities.</span></span>

<span data-ttu-id="adb28-122">Wingtip Toys örnek uygulama, URL yönlendirme özelleştirmeden önce aşağıdaki URL'yi kullanarak bir ürüne bağlayabilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="adb28-122">Before customizing URL routing, the Wingtip Toys sample application can link to a product using the following URL:</span></span>

`https://localhost:44300/ProductDetails.aspx?productID=2`

<span data-ttu-id="adb28-123">URL yönlendirme özelleştirerek Wingtip Toys örnek uygulamanın bir okunmasını URL'yi kullanarak aynı ürün için bağlantı:</span><span class="sxs-lookup"><span data-stu-id="adb28-123">By customizing URL routing, the Wingtip Toys sample application will link to the same product using an easier to read URL:</span></span>

`https://localhost:44300/Product/Convertible%20Car`

### <a name="routes"></a><span data-ttu-id="adb28-124">Yollar</span><span class="sxs-lookup"><span data-stu-id="adb28-124">Routes</span></span>

<span data-ttu-id="adb28-125">Bir rota için bir işleyici eşlenmiş bir URL deseni şudur.</span><span class="sxs-lookup"><span data-stu-id="adb28-125">A route is a URL pattern that is mapped to a handler.</span></span> <span data-ttu-id="adb28-126">İşleyici, bir Web Forms uygulaması bir .aspx dosyası gibi bir fiziksel dosya olabilir.</span><span class="sxs-lookup"><span data-stu-id="adb28-126">The handler can be a physical file, such as an .aspx file in a Web Forms application.</span></span> <span data-ttu-id="adb28-127">Bir işleyici isteği işleyen bir sınıf da olabilir.</span><span class="sxs-lookup"><span data-stu-id="adb28-127">A handler can also be a class that processes the request.</span></span> <span data-ttu-id="adb28-128">Bir rota tanımlamak için URL deseni, işleyici ve isteğe bağlı olarak yol için bir ad belirterek rota sınıfının bir örneğini oluşturun.</span><span class="sxs-lookup"><span data-stu-id="adb28-128">To define a route, you create an instance of the Route class by specifying the URL pattern, the handler, and optionally a name for the route.</span></span>

<span data-ttu-id="adb28-129">Ekleyerek uygulamayı rotası ekleyin `Route` statik nesne `Routes` özelliği `RouteTable` sınıfı.</span><span class="sxs-lookup"><span data-stu-id="adb28-129">You add the route to the application by adding the `Route` object to the static `Routes` property of the `RouteTable` class.</span></span> <span data-ttu-id="adb28-130">Yollar özelliği bir `RouteCollection` uygulama için tüm yolları depolayan bir nesne.</span><span class="sxs-lookup"><span data-stu-id="adb28-130">The Routes property is a `RouteCollection` object that stores all the routes for the application.</span></span>

### <a name="url-patterns"></a><span data-ttu-id="adb28-131">URL desenleri</span><span class="sxs-lookup"><span data-stu-id="adb28-131">URL Patterns</span></span>

<span data-ttu-id="adb28-132">Bir URL deseni, değişmez değerler ve değişken yer tutucuları (URL parametreleri adlandırılır) içerebilir.</span><span class="sxs-lookup"><span data-stu-id="adb28-132">A URL pattern can contain literal values and variable placeholders (referred to as URL parameters).</span></span> <span data-ttu-id="adb28-133">Değişmez değerler ve yer tutucu URL'nin eğik çizgiyle ayrılmış segmenti bulunur (`/`) karakter.</span><span class="sxs-lookup"><span data-stu-id="adb28-133">The literals and placeholders are located in segments of the URL which are delimited by the slash (`/`) character.</span></span>

<span data-ttu-id="adb28-134">Web uygulamanız için bir istek yapıldığında, URL kesimleri ve yer tutucu ayrıştırılır ve değişken değerleri için istek işleyicisi sağlanır.</span><span class="sxs-lookup"><span data-stu-id="adb28-134">When a request to your web application is made, the URL is parsed into segments and placeholders, and the variable values are provided to the request handler.</span></span> <span data-ttu-id="adb28-135">Bu işlem, bir sorgu dizesi verileri ayrıştırılır ve istek işleyicisine geçirilen şekilde benzerdir.</span><span class="sxs-lookup"><span data-stu-id="adb28-135">This process is similar to the way the data in a query string is parsed and passed to the request handler.</span></span> <span data-ttu-id="adb28-136">Her iki durumda da, değişken bilgileri URL'de bulunan ve anahtar-değer çiftleri biçiminde işleyicisine geçirilen.</span><span class="sxs-lookup"><span data-stu-id="adb28-136">In both cases, variable information is included in the URL and passed to the handler in the form of key-value pairs.</span></span> <span data-ttu-id="adb28-137">Sorgu için anahtarlar ve değerler hem URL'de dizelerdir.</span><span class="sxs-lookup"><span data-stu-id="adb28-137">For query strings, both the keys and the values are in the URL.</span></span> <span data-ttu-id="adb28-138">Yollar için URL deseninde tanımlanan yer tutucu adlarını anahtarlarıdır ve yalnızca URL'ye değerlerdir.</span><span class="sxs-lookup"><span data-stu-id="adb28-138">For routes, the keys are the placeholder names defined in the URL pattern, and only the values are in the URL.</span></span>

<span data-ttu-id="adb28-139">Bir URL deseni yer tutucuları küme ayraçları içine alarak tanımladığınız ( `{` ve `}` ).</span><span class="sxs-lookup"><span data-stu-id="adb28-139">In a URL pattern, you define placeholders by enclosing them in braces ( `{` and `}` ).</span></span> <span data-ttu-id="adb28-140">Birden fazla yer tutucu bir segmente tanımlayabilirsiniz, ancak yer tutucuları değişmez değer ayrılmış olması gerekir.</span><span class="sxs-lookup"><span data-stu-id="adb28-140">You can define more than one placeholder in a segment, but the placeholders must be separated by a literal value.</span></span> <span data-ttu-id="adb28-141">Örneğin, `{language}-{country}/{action}` geçerli bir rota modelidir.</span><span class="sxs-lookup"><span data-stu-id="adb28-141">For example, `{language}-{country}/{action}` is a valid route pattern.</span></span> <span data-ttu-id="adb28-142">Ancak, `{language}{country}/{action}` değişmez değer ya da yer tutucuları bölücüyü olduğundan geçerli bir düzen değil.</span><span class="sxs-lookup"><span data-stu-id="adb28-142">However, `{language}{country}/{action}` is not a valid pattern, because there is no literal value or delimiter between the placeholders.</span></span> <span data-ttu-id="adb28-143">Bu nedenle, yönlendirme değeri dil yer tutucusu için için Ülke yer tutucu değerini ayırmak yeri belirlenemiyor.</span><span class="sxs-lookup"><span data-stu-id="adb28-143">Therefore, routing cannot determine where to separate the value for the language placeholder from the value for the country placeholder.</span></span>

### <a name="mapping-and-registering-routes"></a><span data-ttu-id="adb28-144">Eşleştirme ve yollar kaydediliyor</span><span class="sxs-lookup"><span data-stu-id="adb28-144">Mapping and Registering Routes</span></span>

<span data-ttu-id="adb28-145">Wingtip Toys örnek uygulamanın sayfaları yolları içerebilir önce uygulama başladığında yolları kaydetmeniz gerekir.</span><span class="sxs-lookup"><span data-stu-id="adb28-145">Before you can include routes to pages of the Wingtip Toys sample application, you must register the routes when the application starts.</span></span> <span data-ttu-id="adb28-146">Yollar kaydetmek için değiştireceğiniz `Application_Start` olay işleyicisi.</span><span class="sxs-lookup"><span data-stu-id="adb28-146">To register the routes, you will modify the `Application_Start` event handler.</span></span>

1. <span data-ttu-id="adb28-147">İçinde **Çözüm Gezgini**Visual Studio bulma ve açma *Global.asax.cs* dosya.</span><span class="sxs-lookup"><span data-stu-id="adb28-147">In **Solution Explorer**of Visual Studio, find and open the *Global.asax.cs* file.</span></span>
2. <span data-ttu-id="adb28-148">Sarı ile vurgulanmış kodu ekleyin *Global.asax.cs* aşağıdaki gibi:</span><span class="sxs-lookup"><span data-stu-id="adb28-148">Add the code highlighted in yellow to the *Global.asax.cs* file as follows:</span></span>   

    [!code-csharp[Main](url-routing/samples/sample1.cs?highlight=30-31,34-46)]

<span data-ttu-id="adb28-149">Wingtip Toys örneği uygulama başladığında, çağrı `Application_Start` olay işleyicisi.</span><span class="sxs-lookup"><span data-stu-id="adb28-149">When the Wingtip Toys sample application starts, it calls the `Application_Start` event handler.</span></span> <span data-ttu-id="adb28-150">Bu olay işleyicisi sonunda `RegisterCustomRoutes` yöntemi çağrılır.</span><span class="sxs-lookup"><span data-stu-id="adb28-150">At the end of this event handler, the `RegisterCustomRoutes` method is called.</span></span> <span data-ttu-id="adb28-151">`RegisterCustomRoutes` Yöntemi çağırarak her bir rota ekler `MapPageRoute` yöntemi `RouteCollection` nesne.</span><span class="sxs-lookup"><span data-stu-id="adb28-151">The `RegisterCustomRoutes` method adds each route by calling the `MapPageRoute` method of the `RouteCollection` object.</span></span> <span data-ttu-id="adb28-152">Yol, yönlendirme URL'sini ve fiziksel bir URL, bir rota adı kullanılarak tanımlanır.</span><span class="sxs-lookup"><span data-stu-id="adb28-152">Routes are defined using a route name, a route URL and a physical URL.</span></span>

<span data-ttu-id="adb28-153">İlk parametresi ("`ProductsByCategoryRoute`") rota adı.</span><span class="sxs-lookup"><span data-stu-id="adb28-153">The first parameter ("`ProductsByCategoryRoute`") is the route name.</span></span> <span data-ttu-id="adb28-154">Rota gerektiğinde çağırmak için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="adb28-154">It is used to call the route when it is needed.</span></span> <span data-ttu-id="adb28-155">İkinci parametresi ("`Category/{categoryName}`") dinamik URL tabanlı kodu kolay değiştirme tanımlar.</span><span class="sxs-lookup"><span data-stu-id="adb28-155">The second parameter ("`Category/{categoryName}`") defines the friendly replacement URL that can be dynamic based on code.</span></span> <span data-ttu-id="adb28-156">Bir veri denetim verilerini temel alarak oluşturulan bağlantılarla birlikte doldurulurken bu yolu kullanın.</span><span class="sxs-lookup"><span data-stu-id="adb28-156">You use this route when you are populating a data control with links that are generated based on data.</span></span> <span data-ttu-id="adb28-157">Bir rota şu şekilde gösterilir:</span><span class="sxs-lookup"><span data-stu-id="adb28-157">A route is shown as follows:</span></span>

[!code-csharp[Main](url-routing/samples/sample2.cs)]

<span data-ttu-id="adb28-158">Bir dinamik değerin ayraç içinde belirtilen yol'öğesinin ikinci parametresi içerir (`{ }`).</span><span class="sxs-lookup"><span data-stu-id="adb28-158">The second parameter of the route includes a dynamic value specified by braces (`{ }`).</span></span> <span data-ttu-id="adb28-159">Bu durumda, `categoryName` uygun yönlendirme yolunu belirlemek için kullanılan bir değişkendir.</span><span class="sxs-lookup"><span data-stu-id="adb28-159">In this case, the `categoryName` is a variable that will be used to determine the proper routing path.</span></span>

> [!NOTE] 
> 
> <span data-ttu-id="adb28-160">**Optional**</span><span class="sxs-lookup"><span data-stu-id="adb28-160">**Optional**</span></span>
> 
> <span data-ttu-id="adb28-161">Kodunuzu taşıyarak yönetilmesi daha kolay bulabilirsiniz `RegisterCustomRoutes` ayrı bir sınıf için yöntemi.</span><span class="sxs-lookup"><span data-stu-id="adb28-161">You might find it easier to manage your code by moving the `RegisterCustomRoutes` method to a separate class.</span></span> <span data-ttu-id="adb28-162">İçinde *mantıksal* klasörü, ayrı bir oluşturma `RouteActions` sınıfı.</span><span class="sxs-lookup"><span data-stu-id="adb28-162">In the *Logic* folder, create a separate `RouteActions` class.</span></span> <span data-ttu-id="adb28-163">Yukarıdaki taşıma `RegisterCustomRoutes` yönteminden *Global.asax.cs* yeni dosyaya `RoutesActions` sınıfı.</span><span class="sxs-lookup"><span data-stu-id="adb28-163">Move the above `RegisterCustomRoutes` method from the *Global.asax.cs* file into the new `RoutesActions` class.</span></span> <span data-ttu-id="adb28-164">Kullanım `RoleActions` sınıfı ve `createAdmin` yöntemi çağırmak nasıl bir örnek olarak `RegisterCustomRoutes` yönteminden *Global.asax.cs* dosya.</span><span class="sxs-lookup"><span data-stu-id="adb28-164">Use the `RoleActions` class and the `createAdmin` method as an example of how to call the `RegisterCustomRoutes` method from the *Global.asax.cs* file.</span></span>

<span data-ttu-id="adb28-165">Ayrıca fark etmiş `RegisterRoutes` yöntemi kullanarak `RouteConfig` nesne başına `Application_Start` olay işleyicisi.</span><span class="sxs-lookup"><span data-stu-id="adb28-165">You may also have noticed the `RegisterRoutes` method call using the `RouteConfig` object at the beginning of the `Application_Start` event handler.</span></span> <span data-ttu-id="adb28-166">Varsayılan yönlendirme uygulamak için bu çağrı yapılır.</span><span class="sxs-lookup"><span data-stu-id="adb28-166">This call is made to implement default routing.</span></span> <span data-ttu-id="adb28-167">Visual Studio Web Forms şablonunu kullanarak uygulama oluştururken varsayılan kod olarak eklendi.</span><span class="sxs-lookup"><span data-stu-id="adb28-167">It was included as default code when you created the application using Visual Studio's Web Forms template.</span></span>

## <a name="retrieving-and-using-route-data"></a><span data-ttu-id="adb28-168">Alma ve rota verilerini kullanma</span><span class="sxs-lookup"><span data-stu-id="adb28-168">Retrieving and Using Route Data</span></span>

<span data-ttu-id="adb28-169">Yukarıda belirtildiği gibi yollar tanımlanabilir.</span><span class="sxs-lookup"><span data-stu-id="adb28-169">As mentioned above, routes can be defined.</span></span> <span data-ttu-id="adb28-170">Eklediğiniz kod `Application_Start` olay işleyicisinde *Global.asax.cs* tanımlanabilir yolları dosya yükler.</span><span class="sxs-lookup"><span data-stu-id="adb28-170">The code that you added to the `Application_Start` event handler in the *Global.asax.cs* file loads the definable routes.</span></span>

### <a name="setting-routes"></a><span data-ttu-id="adb28-171">Rotalar ayarlama</span><span class="sxs-lookup"><span data-stu-id="adb28-171">Setting Routes</span></span>

<span data-ttu-id="adb28-172">Yollar ek kod eklemenizi gerektirir.</span><span class="sxs-lookup"><span data-stu-id="adb28-172">Routes require you to add additional code.</span></span> <span data-ttu-id="adb28-173">Bu öğreticide, model bağlama almak için kullanacağı bir `RouteValueDictionary` veri denetimi verilerini kullanarak yolları oluştururken kullanılan nesne.</span><span class="sxs-lookup"><span data-stu-id="adb28-173">In this tutorial, you will use model binding to retrieve a `RouteValueDictionary` object that is used when generating the routes using data from a data control.</span></span> <span data-ttu-id="adb28-174">`RouteValueDictionary` Nesne ürünlerin belirli bir kategoriye ait ürün adlarının bir listesini içerir.</span><span class="sxs-lookup"><span data-stu-id="adb28-174">The `RouteValueDictionary` object will contain a list of product names that belong to a specific category of products.</span></span> <span data-ttu-id="adb28-175">Veri ve rota tabanlı her ürün için bir bağlantı oluşturulur.</span><span class="sxs-lookup"><span data-stu-id="adb28-175">A link is created for each product based on the data and route.</span></span>

#### <a name="enable-routes-for-categories-and-products"></a><span data-ttu-id="adb28-176">Kategoriler ve ürünler için rotalar etkinleştir</span><span class="sxs-lookup"><span data-stu-id="adb28-176">Enable Routes for Categories and Products</span></span>

<span data-ttu-id="adb28-177">Ardından, uygulamayı kullanacak şekilde güncelleştireceksiniz `ProductsByCategoryRoute` her bir ürün kategorisini bağlantısı için içerecek şekilde doğru yolu belirlemek için.</span><span class="sxs-lookup"><span data-stu-id="adb28-177">Next, you'll update the application to use the `ProductsByCategoryRoute` to determine the correct route to include for each product category link.</span></span> <span data-ttu-id="adb28-178">Ayrıca güncelleştireceksiniz *ProductList.aspx* sayfasına her ürün için gönderilmiş bir bağlantı içerir.</span><span class="sxs-lookup"><span data-stu-id="adb28-178">You'll also update the *ProductList.aspx* page to include a routed link for each product.</span></span> <span data-ttu-id="adb28-179">Değişiklikten önce oldukları gibi bağlantıları artık URL yönlendirme kullanır ancak bağlantılar görüntülenir.</span><span class="sxs-lookup"><span data-stu-id="adb28-179">The links will be displayed as they were before the change, however the links will now use URL routing.</span></span>

1. <span data-ttu-id="adb28-180">İçinde **Çözüm Gezgini**açın *Site.Master* zaten açık değilse, sayfa.</span><span class="sxs-lookup"><span data-stu-id="adb28-180">In **Solution Explorer**, open the *Site.Master* page if it is not already open.</span></span>
2. <span data-ttu-id="adb28-181">Güncelleştirme **ListView** adlı Denetim "`categoryList`" sarı ile vurgulanmış değişikliklerle birlikte, bu nedenle işaretleme şu şekilde görünür:</span><span class="sxs-lookup"><span data-stu-id="adb28-181">Update the **ListView** control named "`categoryList`" with the changes highlighted in yellow, so the markup appears as follows:</span></span>   

    [!code-aspx[Main](url-routing/samples/sample3.aspx?highlight=7-9)]
3. <span data-ttu-id="adb28-182">İçinde **Çözüm Gezgini**açın *ProductList.aspx* sayfası.</span><span class="sxs-lookup"><span data-stu-id="adb28-182">In **Solution Explorer**, open the *ProductList.aspx* page.</span></span>
4. <span data-ttu-id="adb28-183">Güncelleştirme `ItemTemplate` öğesinin *ProductList.aspx* biçimlendirme gibi görünecek şekilde, sarı ile vurgulanmış güncelleştirmeleri sayfası:</span><span class="sxs-lookup"><span data-stu-id="adb28-183">Update the `ItemTemplate` element of the *ProductList.aspx* page with the updates highlighted in yellow, so the markup appears as follows:</span></span>   

    [!code-aspx[Main](url-routing/samples/sample4.aspx?highlight=6-9,14-16)]
5. <span data-ttu-id="adb28-184">Arka plan kod, açık *ProductList.aspx.cs* ve şu ad alanı vurgulanmış sarı ekleyin:</span><span class="sxs-lookup"><span data-stu-id="adb28-184">Open the code-behind of *ProductList.aspx.cs* and add the following namespace as highlighted in yellow:</span></span>  

    [!code-csharp[Main](url-routing/samples/sample5.cs?highlight=9)]
6. <span data-ttu-id="adb28-185">Değiştirin `GetProducts` arka plan kod yöntemi (*ProductList.aspx.cs*) aşağıdaki kod ile:</span><span class="sxs-lookup"><span data-stu-id="adb28-185">Replace the `GetProducts` method of the code-behind (*ProductList.aspx.cs*) with the following code:</span></span>   

    [!code-csharp[Main](url-routing/samples/sample6.cs)]

#### <a name="add-code-for-product-details"></a><span data-ttu-id="adb28-186">Ürün ayrıntılarını kod ekleyin</span><span class="sxs-lookup"><span data-stu-id="adb28-186">Add Code for Product Details</span></span>

<span data-ttu-id="adb28-187">Şimdi, arka plan kod güncelleştir (*ProductDetails.aspx.cs*) için *ProductDetails.aspx* rota verilerini kullanmak için sayfa.</span><span class="sxs-lookup"><span data-stu-id="adb28-187">Now, update the code-behind (*ProductDetails.aspx.cs*) for the *ProductDetails.aspx* page to use route data.</span></span> <span data-ttu-id="adb28-188">Dikkat yeni `GetProduct` yöntemi de eski kolay olmayan, yönlendirilmeyen URL'yi kullanır bozulmasına neden olan bir bağlantı kullanıcının sahip olduğu durum için bir sorgu dizesi değerini kabul eder.</span><span class="sxs-lookup"><span data-stu-id="adb28-188">Notice that the new `GetProduct` method also accepts a query string value for the case where the user has a link bookmarked that uses the older non-friendly, non-routed URL.</span></span>

1. <span data-ttu-id="adb28-189">Değiştirin `GetProduct` arka plan kod yöntemi (*ProductDetails.aspx.cs*) aşağıdaki kod ile:</span><span class="sxs-lookup"><span data-stu-id="adb28-189">Replace the `GetProduct` method of the code-behind (*ProductDetails.aspx.cs*) with the following code:</span></span>   

    [!code-csharp[Main](url-routing/samples/sample7.cs)]

## <a name="running-the-application"></a><span data-ttu-id="adb28-190">Uygulamayı Çalıştırma</span><span class="sxs-lookup"><span data-stu-id="adb28-190">Running the Application</span></span>

<span data-ttu-id="adb28-191">Güncelleştirilmiş yollarını görmek için uygulamayı şimdi çalıştırabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="adb28-191">You can run the application now to see the updated routes.</span></span>

1. <span data-ttu-id="adb28-192">Tuşuna **F5** Wingtip Toys örnek uygulamayı çalıştırın.</span><span class="sxs-lookup"><span data-stu-id="adb28-192">Press **F5** to run the Wingtip Toys sample application.</span></span>  
 <span data-ttu-id="adb28-193">Tarayıcı açılır ve gösterir *Default.aspx* sayfası.</span><span class="sxs-lookup"><span data-stu-id="adb28-193">The browser opens and shows the *Default.aspx* page.</span></span>
2. <span data-ttu-id="adb28-194">Tıklayın **ürünleri** sayfanın üstündeki bağlantısı.</span><span class="sxs-lookup"><span data-stu-id="adb28-194">Click the **Products** link at the top of the page.</span></span>  
 <span data-ttu-id="adb28-195">Tüm ürünler hakkında görüntülenen *ProductList.aspx* sayfası.</span><span class="sxs-lookup"><span data-stu-id="adb28-195">All products are displayed on the *ProductList.aspx* page.</span></span> <span data-ttu-id="adb28-196">(Bağlantı noktası numaranızı kullanılarak) aşağıdaki URL için tarayıcının görüntülenir:</span><span class="sxs-lookup"><span data-stu-id="adb28-196">The following URL (using your port number) is displayed for the browser:</span></span>  
    `https://localhost:44300/ProductList`
3. <span data-ttu-id="adb28-197">Ardından, **otomobiller** sayfanın üst kısmındaki kategori bağlantı.</span><span class="sxs-lookup"><span data-stu-id="adb28-197">Next, click the **Cars** category link near the top of the page.</span></span>  
 <span data-ttu-id="adb28-198">Yalnızca otomobiller görüntülenir *ProductList.aspx* sayfası.</span><span class="sxs-lookup"><span data-stu-id="adb28-198">Only cars are displayed on the *ProductList.aspx* page.</span></span> <span data-ttu-id="adb28-199">(Bağlantı noktası numaranızı kullanılarak) aşağıdaki URL için tarayıcının görüntülenir:</span><span class="sxs-lookup"><span data-stu-id="adb28-199">The following URL (using your port number) is displayed for the browser:</span></span>  
    `https://localhost:44300/Category/Cars`
4. <span data-ttu-id="adb28-200">İlk araba adını içeren bağlantı sayfada listelenen tıklayın ("**dönüştürülebilir araba**") ürün ayrıntılarını görüntülemek için.</span><span class="sxs-lookup"><span data-stu-id="adb28-200">Click the link containing the name of the first car listed on the page ("**Convertible Car**") to display the product details.</span></span>  
 <span data-ttu-id="adb28-201">(Bağlantı noktası numaranızı kullanılarak) aşağıdaki URL için tarayıcının görüntülenir:</span><span class="sxs-lookup"><span data-stu-id="adb28-201">The following URL (using your port number) is displayed for the browser:</span></span>  
    `https://localhost:44300/Product/Convertible%20Car`
5. <span data-ttu-id="adb28-202">Ardından, tarayıcıya (bağlantı noktası numaranızı kullanılarak) aşağıdaki yönlendirilmeyen URL'yi girin:</span><span class="sxs-lookup"><span data-stu-id="adb28-202">Next, enter the following non-routed URL (using your port number) into the browser:</span></span>  
    `https://localhost:44300/ProductDetails.aspx?productID=2`  
 <span data-ttu-id="adb28-203">Kod, bir kullanıcı işareti bağlantı sahip olduğu durum için bir sorgu dizesi içeren bir URL hala tanır.</span><span class="sxs-lookup"><span data-stu-id="adb28-203">The code still recognizes a URL that includes a query string, for the case where a user has a link bookmarked.</span></span>

## <a name="summary"></a><span data-ttu-id="adb28-204">Özet</span><span class="sxs-lookup"><span data-stu-id="adb28-204">Summary</span></span>

<span data-ttu-id="adb28-205">Bu öğreticide, kategoriler ve ürünler için yollar ekledik.</span><span class="sxs-lookup"><span data-stu-id="adb28-205">In this tutorial, you have added routes for categories and products.</span></span> <span data-ttu-id="adb28-206">Model bağlama kullanan veri denetimleri ile yolların nasıl tümleştirilebilir öğrendiniz.</span><span class="sxs-lookup"><span data-stu-id="adb28-206">You have learned how routes can be integrated with data controls that use model binding.</span></span> <span data-ttu-id="adb28-207">Sonraki öğreticide, genel hata işleme uygular.</span><span class="sxs-lookup"><span data-stu-id="adb28-207">In the next tutorial, you will implement global error handling.</span></span>

## <a name="additional-resources"></a><span data-ttu-id="adb28-208">Ek Kaynaklar</span><span class="sxs-lookup"><span data-stu-id="adb28-208">Additional Resources</span></span>

[<span data-ttu-id="adb28-209">ASP.NET kolay URL'leri</span><span class="sxs-lookup"><span data-stu-id="adb28-209">ASP.NET Friendly URLs</span></span>](http://www.nuget.org/packages/Microsoft.AspNet.FriendlyUrls/)  
[<span data-ttu-id="adb28-210">Azure App Service'e bir üyelik, OAuth ve SQL veritabanı ile güvenli bir ASP.NET Web Forms uygulaması dağıtma</span><span class="sxs-lookup"><span data-stu-id="adb28-210">Deploy a Secure ASP.NET Web Forms App with Membership, OAuth, and SQL Database to Azure App Service</span></span>](https://azure.microsoft.com/documentation/articles/web-sites-dotnet-deploy-aspnet-webforms-app-membership-oauth-sql-database/)  
[<span data-ttu-id="adb28-211">Microsoft Azure - ücretsiz deneme</span><span class="sxs-lookup"><span data-stu-id="adb28-211">Microsoft Azure - Free Trial</span></span>](https://azure.microsoft.com/pricing/free-trial/)

> [!div class="step-by-step"]
> <span data-ttu-id="adb28-212">[Önceki](membership-and-administration.md)
> [İleri](aspnet-error-handling.md)</span><span class="sxs-lookup"><span data-stu-id="adb28-212">[Previous](membership-and-administration.md)
[Next](aspnet-error-handling.md)</span></span>
